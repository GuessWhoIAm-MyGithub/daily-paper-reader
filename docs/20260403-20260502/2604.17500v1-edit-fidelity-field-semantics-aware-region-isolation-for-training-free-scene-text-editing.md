---
title: "Edit Fidelity Field: Semantics-Aware Region Isolation for Training-Free Scene Text Editing"
title_zh: 编辑保真度场：用于无训练场景文本编辑的语义感知区域隔离
authors: "Guandong Li, Mengxia Ye"
date: 2026-04-19
pdf: "https://arxiv.org/pdf/2604.17500v1"
tags: ["query:imgfor"]
score: 9.0
evidence: 提出编辑保真度场用于场景文本编辑中的语义感知区域隔离。
tldr: "场景文本编辑（STE）通过扩散方法取得进展，但存在编辑溢出问题：编辑目标文本时，非目标区域被意外修改，溢出率高达94%。为解决此问题，我们提出Edit Fidelity Field (EFF)，一个语义感知的连续场，通过OCR检测构建四个区域控制每像素编辑保真度，免训练且模型无关。EFF将溢出率降至25%，并显著提高非目标区域保留。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.17500v1/fig-001.webp\", \"caption\": \"Figure 2: Pipeline overview. Given a source image and editing instruction, our method operates in four stages: (1) PARSE: OCR detects all text regions; (2) PLAN: Build the Edit Fidelity Field F with protected zones for non-target text; (3) EDIT: Run the diffusion model freely to produce Iedit; (4) BLEND: Apply field-guided blending to produce the final output. Stages 1–2 and 4 are model-agnostic.\", \"page\": 3, \"index\": 1, \"width\": 1054, \"height\": 300}, {\"url\": \"assets/figures/arxiv/2604.17500v1/fig-002.webp\", \"caption\": \"Figure 6: Accuracy-fidelity trade-off. Baseline (red) has high accuracy but extreme spillover. EFF configurations (blue/green) reduce spillover by 2× with controllable parameters.\", \"page\": 5, \"index\": 2, \"width\": 425, \"height\": 301}, {\"url\": \"assets/figures/arxiv/2604.17500v1/fig-003.webp\", \"caption\": \"Figure 1: Edit spillover in scene text editing. Given a highway gantry with the edit instruction “change the main sign Exit to Entrance”, the Baseline (Qwen-Image-Edit) successfully modifies the target, yet entirely erases the secondary “60 / Exit” sub-sign at the bottom-right (100% per-region spillover). Our Edit Fidelity Field (EFF) derives protected zones from all OCR-detected non-target regions (dark areas in the field map) and applies a post-hoc blend Iout = Isrc ⊙ (1−F ) + Iedit ⊙ F , locking every non-target pixel while fully realizing the target edit. Across our 50-scene benchmark, this reduces dataset-wide spillover from 94% to 25%.\", \"page\": 2, \"index\": 3, \"width\": 1054, \"height\": 300}]"
motivation: "识别并解决场景文本编辑中的编辑溢出问题，因为现有扩散方法有高达94%的溢出率。"
method: 提出Edit Fidelity Field (EFF)，一个语义感知的连续场，通过OCR检测文本区域构建四个区域来控制编辑保真度。
result: "EFF将溢出率从94%降低到25%，并提高非目标区域保留，PSNR增加+91.4 dB。"
conclusion: EFF有效解决了编辑溢出问题，提升了场景文本编辑的准确性和保真度。
---

## 摘要
场景文本编辑（STE）通过基于扩散的方法在准确渲染目标文本方面取得了显著进展。然而，我们发现了一个关键但被忽视的问题：编辑溢出——当编辑目标文本区域时，现有方法无意中修改了非目标区域，特别是相邻文本。通过对四个类别的50个真实场景进行系统评估，我们揭示了最先进的扩散编辑模型表现出94%的溢出率，这意味着在编辑过程中几乎所有的非目标文本区域都被修改。为了解决这个问题，我们提出了编辑保真度场（EFF），这是一个语义感知的连续场，控制每像素的编辑保真度。与二进制掩码不同，EFF利用OCR检测到的文本区域构建一个四区域场：编辑核心（完全可编辑）、过渡区（平滑衰减）、保护区（非目标文本，显式锁定）和背景（严格保留）。EFF作为一个无训练、模型无关的后处理模块，适用于任何基于扩散的STE方法。我们进一步提出了每区域溢出量化，这是一种新颖的评估协议，用于单独测量每个非目标文本区域的编辑泄漏。实验表明，EFF将溢出率从94%降低到25%，同时将非目标区域的保留提高了+91.4 dB PSNR。

## Abstract
Scene text editing (STE) has achieved remarkable progress in accurately rendering target text through diffusion-based methods. However, we identify a critical yet overlooked problem: edit spillover -- when editing a target text region, existing methods inadvertently modify non-target regions, particularly neighboring text. Through systematic evaluation on 50 real-world scenes across four categories, we reveal that state-of-the-art diffusion editing models exhibit a spillover rate of 94%, meaning nearly all non-target text regions are altered during editing. To address this, we propose the Edit Fidelity Field (EFF), a semantics-aware continuous field that controls per-pixel editing fidelity. Unlike binary masks, EFF leverages OCR-detected text regions to construct a four-zone field: Edit Core (fully editable), Transition Zone (smooth decay), Protected Zone (non-target text, explicitly locked), and Background (strictly preserved). EFF operates as a training-free, model-agnostic post-processing module applicable to any diffusion-based STE method. We further propose per-region spillover quantification, a novel evaluation protocol that measures edit leakage at each non-target text region individually. Experiments demonstrate that EFF reduces spillover rate from 94% to 25% while improving non-target region preservation by +91.4 dB PSNR.

---

## 论文详细总结（自动生成）

# 论文总结：编辑保真度场（EFF）

## 1. 核心问题与整体含义
*   **研究动机与背景**：场景文本编辑（STE）旨在修改自然图像中的文本内容，同时保持字体、颜色和背景的一致性。尽管基于扩散模型的方法在准确渲染目标文本方面取得了显著进展，但本文发现了一个被系统性忽视的关键问题：**编辑溢出**。
*   **问题定义**：当扩散模型编辑目标文本区域时，其去噪过程会重建整个图像，不可避免地导致对非目标区域（尤其是邻近的其他文本）的意外修改。这在文本密集的场景（如菜单、名片、收据）中尤为严重。
*   **问题量化**：通过对50个真实场景的系统评估，研究发现最先进的扩散编辑模型表现出高达**94%的溢出率**，即几乎所有非目标文本区域在编辑后都被改变。这揭示了当前方法只优化目标准确性而忽略非目标保留的根本局限。

## 2. 提出的方法论
*   **核心思想**：提出**编辑保真度场（Edit Fidelity Field, EFF）**，这是一个语义感知的连续场，为每个像素分配一个保真度权重 `w ∈ [0, 1]`，以精细控制编辑过程。
*   **关键技术细节**：
    1.  **语义感知**：利用OCR检测图像中的所有文本区域，并显式地将非目标文本标记为**保护区**，无论其与编辑目标的空间距离如何。
    2.  **四区域场构建**：
        *   **编辑核心**：目标文本区域，权重 `w=1`，完全可编辑。
        *   **过渡区**：围绕编辑核心的平滑衰减区域，权重 `0<w<1`。
        *   **保护区**：所有非目标文本区域，权重 `w=0`，被显式锁定。
        *   **背景**：其余区域，权重 `w→0`，严格保留。
    3.  **场生成与混合**：最终输出通过像素级混合公式计算：`Iout = Isrc · (1 − F) + Iedit · F`。其中 `F` 是经过平滑和保护区强化后的EFF场。
*   **算法流程**：分为四个阶段：(1) **解析**：用OCR检测所有文本区域；(2) **规划**：构建EFF场；(3) **编辑**：运行扩散模型生成编辑后的图像；(4) **混合**：使用EFF场引导的混合生成最终输出。该方法是**无训练**且**模型无关**的后处理模块。

## 3. 实验设计
*   **数据集与场景**：在**TBench v2**基准上进行评估，包含50个真实世界图像，涵盖四个类别：户外照片（20张）、应用截图（10张）、标准场景（10张）和收据文档（10张）。每个图像包含多个文本区域。
*   **基准模型**：以**Qwen-Image-Edit-2511**（一个基于DiT的扩散模型）作为基础编辑模型。
*   **对比方法**：
    *   **Baseline**：直接使用基础模型，无任何隔离措施。
    *   **SimpleMask**：使用带有距离羽化的二进制掩码（无OCR保护）。
    *   **EFF (Ours)**：本文提出的完整方法。
*   **评估指标**：目标文本发现率、**溢出率**、平均区域PSNR、最小区域PSNR、背景PSNR。并提出了**每区域溢出量化**的新评估协议。

## 4. 资源与算力
*   论文中**未明确说明**训练或推理所使用的具体GPU型号、数量和时长。
*   EFF作为一个后处理模块，其核心计算（OCR检测、场构建、像素混合）本身不涉及模型训练，因此额外算力开销较小。基础编辑模型使用了FP8量化，表明其在推理时进行了优化。

## 5. 实验数量与充分性
*   **实验数量**：进行了多组实验，包括：
    *   **整体比较**（表1）：在50个场景上对比三种方法。
    *   **分类别分析**（表2）：在四个场景类别上分别评估溢出率。
    *   **消融实验**（表3）：验证OCR驱动的保护区对最差情况保护（最小区域PSNR）的贡献。
    *   **权衡分析**（图6）：探讨目标编辑精度与非目标保留之间的权衡关系。
*   **充分性与公平性**：实验设计较为充分，覆盖了不同场景类别和关键评估维度。对比方法（Baseline和SimpleMask）是合理的基线。评估指标全面，特别是引入了针对溢出问题的新指标。然而，评估仅基于一个基础模型和50个场景，其泛化性有待在更多模型和更大规模数据上验证。

## 6. 主要结论与发现
*   **核心发现**：系统性地识别并量化了场景文本编辑中严重的“编辑溢出”问题（94%溢出率）。
*   **方法有效性**：提出的EFF能将溢出率从94%显著降低至25%，并将非目标区域的平均保留质量（PSNR）提升高达+91.4 dB。
*   **实用性**：EFF是一个即插即用的无训练、模型无关模块，可直接应用于任何基于扩散的STE方法，无需修改原模型。

## 7. 优点
*   **问题洞察深刻**：首次系统性地揭示并量化了STE中被忽视的编辑溢出问题，具有重要实践意义。
*   **方法设计巧妙**：EFF的语义感知设计（特别是保护区）直接针对问题核心，比简单的空间掩码更有效。
*   **实用性强**：无训练、模型无关的特性使其易于集成和应用，具有很高的工程价值。
*   **评估创新**：提出了“每区域溢出量化”的细粒度评估协议，能更准确地衡量编辑泄漏。

## 8. 不足与局限
*   **OCR依赖**：EFF的保护能力完全依赖于OCR的检测精度。对于OCR漏检的小文本或特殊字体，保护会失效。
*   **精度-保真度权衡**：在大幅降低溢出率的同时，目标文本的编辑成功率从84%下降至60%，存在明显的权衡。
*   **场景覆盖有限**：实验仅评估了单次编辑场景。对于需要同时编辑多个文本区域的复杂情况，如何协调多个EFF场尚未探索。
*   **评估基准局限**：所有实验均在TBench v2的50个场景上进行，数据规模和多样性可能不足以全面反映方法在所有真实世界场景中的表现。
*   **应用限制**：当前方法专注于文本区域的保护，对于非文本但语义重要的区域（如人脸、商标）的保护未作考虑。

（完）
