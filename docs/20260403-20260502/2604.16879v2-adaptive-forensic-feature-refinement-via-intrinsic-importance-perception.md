---
title: Adaptive Forensic Feature Refinement via Intrinsic Importance Perception
title_zh: 通过内在重要性感知的自适应取证特征精炼
authors: "Jiazhen Yang, Junjun Zheng, Kejia Chen, Xiangheng Kong, Jie Lei, Zunlei Feng, Bingde Hu, Yang Gao"
date: 2026-04-18
pdf: "https://arxiv.org/pdf/2604.16879v2"
tags: ["query:imgfor"]
score: 9.0
evidence: 使用自适应取证特征改进合成图像检测。
tldr: 随着生成模型和多模态编辑技术发展，合成图像检测（SID）面临跨分布泛化到未知源的挑战。视觉基础模型（VFM）虽能提升泛化能力，但现有适应策略粗粒度，缺乏最优表示层次建模。本文提出I2P框架，通过内在重要性感知自适应识别关键层表示，并约束参数更新在低敏感性子空间，以提高任务特异性同时保留预训练结构，从而增强SID泛化能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.16879v2/fig-001.webp\", \"caption\": \"Table 1: Accuracy (ACC, %) and average precision (AP, %) comparison of I2P and other methods on the Setting 1. All methods are trained on SDv1.4 and evaluated on different test subsets. Bold indicates the best result, and underline denotes the second-best.\", \"page\": 6, \"index\": 1, \"width\": 1044, \"height\": 286}, {\"url\": \"assets/figures/arxiv/2604.16879v2/fig-002.webp\", \"caption\": \"Figure 1: T-SNE visualizations of layer-wise representations extracted on GenImage [59] using a frozen CLIP ViT-L/14 [38] visual encoder. Intermediate layers naturally exhibit a clearer separable structure between real and synthetic embeddings, whereas in early and deep stages the feature space overlap across categories becomes more pronounced.\", \"page\": 2, \"index\": 2, \"width\": 1040, \"height\": 330}, {\"url\": \"assets/figures/arxiv/2604.16879v2/fig-003.webp\", \"caption\": \"Table 2: Accuracy (ACC, %) comparison of I2P and other methods on the Setting 2. All methods are trained on ProGAN and evaluated on different test subsets. Bold indicates the best result, and underline denotes the second-best.\", \"page\": 7, \"index\": 3, \"width\": 1046, \"height\": 321}, {\"url\": \"assets/figures/arxiv/2604.16879v2/fig-004.webp\", \"caption\": \"Figure 3: Layer-wise linear separability of a frozen CLIP ViTL/14. A linear classifier is trained on CLS token features from each layer and evaluated on the GenImage test set, the classification accuracy peaks at intermediate layers.\", \"page\": 3, \"index\": 4, \"width\": 510, \"height\": 240}, {\"url\": \"assets/figures/arxiv/2604.16879v2/fig-005.webp\", \"caption\": \"Figure 8: VFM backbones comparison. Performance of the frozen last-layer baseline and I2P across different VFM.\", \"page\": 8, \"index\": 5, \"width\": 503, \"height\": 256}, {\"url\": \"assets/figures/arxiv/2604.16879v2/fig-006.webp\", \"caption\": \"Figure 5: Random initialization ( ) often necessitates extensive exploration and may converge to sharp, perturbation-sensitive\", \"page\": 4, \"index\": 6, \"width\": 508, \"height\": 277}, {\"url\": \"assets/figures/arxiv/2604.16879v2/fig-007.webp\", \"caption\": \"Figure 6: I2P overall framework. Stage 1: Critical Layer Identification extracts layer-wise CLS token representations from a frozen\", \"page\": 5, \"index\": 7, \"width\": 1056, \"height\": 448}]"
motivation: 解决视觉基础模型在合成图像检测中任务适应与跨模态预训练结构保护之间的矛盾。
method: 提出I2P框架，利用内在重要性感知自适应识别关键表示层，并将参数更新约束在低敏感性子空间中。
result: I2P框架在合成图像检测中提高了任务特异性，同时保留了预训练表示的可迁移结构，增强了泛化能力。
conclusion: I2P为合成图像检测提供了一种有效的自适应方法，平衡了任务适应与泛化能力。
---

## 摘要
随着生成模型和多模态内容编辑技术的快速发展，合成图像检测（SID）面临的关键挑战在于对未知生成源的跨分布泛化。近年来，视觉基础模型（VFM）通过大规模图像-文本对齐预训练获取丰富的视觉先验，已成为提高SID泛化能力的有前途的技术路线。然而，现有的基于VFM的方法在适应策略上仍然相对粗粒度。它们通常要么直接使用VFM的最终层表示，要么简单地融合多层特征，缺乏对可转移伪造线索的最优表示层次结构的显式建模。同时，尽管直接微调VFM可以增强任务适应性，但它也可能损害支持开放集泛化的跨模态预训练结构。为了解决这一任务特定的紧张关系，我们将VFM适应SID重新表述为一个联合优化问题：既需要识别更适合承载伪造判别信息的临界表示层，又需要约束任务知识注入对预训练结构造成的干扰。基于此，我们提出了I2P，一个以内在重要性感知为中心的SID框架。I2P首先自适应地识别对SID最具判别力的临界层表示，然后将任务驱动的参数更新约束在低敏感性参数子空间内，从而在尽可能保留预训练表示的可转移结构的同时提高任务特异性。

## Abstract
With the rapid development of generative models and multimodal content editing technologies, the key challenge faced by synthetic image detection (SID) lies in cross-distribution generalization to unknown generation sources. In recent years, visual foundation models (VFM), which acquire rich visual priors through large scale image-text alignment pretraining, have become a promising technical route for improving the generalization ability of SID. However, existing VFM-based methods remain relatively coarse-grained in their adaptation strategies. They typically either directly use the final layer representations of VFM or simply fuse multi layer features, lacking explicit modeling of the optimal representational hierarchy for transferable forgery cues. Meanwhile, although directly fine-tuning VFM can enhance task adaptation, it may also damage the cross-modal pretrained structure that supports open-set generalization. To address this task specific tension, we reformulate VFM adaptation for SID as a joint optimization problem: it is necessary both to identify the critical representational layer that is more suitable for carrying forgery discriminative information and to constrain the disturbance caused by task knowledge injection to the pretrained structure. Based on this, we propose I2P, an SID framework centered on intrinsic importance perception. I2P first adaptively identifies the critical layer representations that are most discriminative for SID, and then constrains task-driven parameter updates within a low sensitivity parameter subspace, thereby improving task specificity while preserving the transferable structure of pretrained representations as much as possible.

---

## 论文详细总结（自动生成）

# 论文总结：通过内在重要性感知的自适应取证特征精炼

## 1. 核心问题与整体含义
*   **研究背景与动机**：随着AI生成模型（如扩散模型、GAN）的快速发展，合成图像检测（SID）面临的核心挑战是**跨分布泛化**能力不足，即模型难以有效检测来自训练时未见过的生成源的图像。现有基于视觉基础模型（VFM）的方法虽然潜力巨大，但其适应策略存在两个关键问题：
    1.  **特征利用粗粒度**：通常直接使用VFM的最终层特征或简单融合多层特征，忽略了不同网络深度对伪造线索的差异化贡献，可能引入信息稀释。
    2.  **适应与泛化的矛盾**：直接微调VFM虽能增强任务适应性，但可能破坏其通过大规模预训练获得的、支持泛化的跨模态结构，导致表示漂移。
*   **核心问题**：如何在利用VFM进行下游任务适配时，**同时实现**：（1）自适应地定位最适合提取伪造判别信息的**关键表示层**；（2）在注入任务知识时，**最小化对预训练结构的干扰**。

## 2. 提出的方法论
论文提出了名为 **I2P** 的框架，其核心思想是通过“内在重要性感知”来指导VFM的自适应精炼，包含两个连续阶段：

*   **阶段一：临界层识别**
    *   **核心思想**：VFM的中间层特征在保留细粒度伪造线索和抑制低层噪声之间达到了最佳平衡，是最适合SID的表示层。
    *   **技术细节**：
        1.  冻结VFM，提取所有层的CLS token表示。
        2.  设计一个轻量级的**层评分函数**（一个小型MLP），为每一层特征计算一个可学习的分数。
        3.  对分数进行Softmax归一化，得到层间贡献权重分布。
        4.  选择权重最大的层作为**临界层**，后续仅使用该层的特征进行检测。这避免了信息稀释，并可对更深层进行结构化剪枝以节省计算。

*   **阶段二：受控知识注入**
    *   **核心思想**：将下游任务的参数更新限制在预训练参数空间中**低敏感性（低曲率）的子空间**内，以在提升任务性能的同时保护预训练结构。
    *   **技术细节**：
        1.  **参数重要性估计**：采用基于二阶信息的近似方法。对于一个线性层，利用输入激活的二阶矩（`X^T X`）近似Hessian矩阵，并结合权重大小计算每个参数的重要性分数 `S_ij`。重要性高的参数方向对损失变化敏感，更新风险大。
        2.  **选择性更新**：根据重要性分数，选择重要性最低的η%参数构成可更新子集。在微调时，**仅更新该子集和分类头**，其余参数保持冻结。这通过一个二进制掩码矩阵实现。

## 3. 实验设计
*   **数据集与场景**：实验在三个广泛使用的跨域评估设置下进行：
    *   **Setting 1**：在SDv1.4数据集上训练，在8个不同生成器（包括SDv1.5、GLIDE、BigGAN、Midjourney等）的测试集上评估。
    *   **Setting 2**：在ProGAN数据集上训练，在18个生成器（涵盖多种GAN和早期扩散模型）的测试集上评估。
    *   **Setting 3**：在更具挑战性的现实分布偏移（Chameleon）和新兴生成器（COSPY，包含FLUX.1、SD-3等）上评估。
*   **Benchmark**：使用了GenImage、ForenSynths、DIRE、UniversalFake、Chameleon、COSPY等多个公开基准。
*   **对比方法**：与10个强基线方法对比，包括：
    *   **专用SID方法**：LGard, DIRE, NPR, FreqNet, FerretNet。
    *   **基于VFM的SID方法**：UniFD, FatFormer, RINE, ForgeLens, Effort。

## 4. 资源与算力
*   **硬件**：所有实验在**单个NVIDIA RTX A6000 GPU**上进行。
*   **训练细节**：使用Adam优化器，初始学习率1e-4，批量大小16。论文**未明确说明具体的训练时长**。
*   **数据效率**：I2P在训练时仅使用了每个训练集（如GenImage约32.4万张，ProGAN约14.4万张）中**随机采样的1600张图像**，体现了其数据高效性。

## 5. 实验数量与充分性
*   **实验规模**：论文进行了**大量且全面的实验**，包括：
    *   **主实验**：在3个设置、超过30个不同的测试生成器上进行了跨域评估。
    *   **消融研究**：验证了CLI和CKI两个模块各自及联合的有效性；分析了更新率η和使用的CLS token数量的影响。
    *   **泛化性验证**：在6种不同的VFM骨干网络（CLIP, SigLIP2, DINOv3的不同规模版本）上测试了I2P的通用性。
*   **评估充分性与公平性**：
    *   **充分**：实验覆盖了多种生成范式（GAN、扩散模型）、多种数据分布和新兴生成器，结果具有说服力。
    *   **公平**：所有基线方法均使用其原始论文报告的最优设置或官方预训练模型。I2P使用了与多数VFM基线相同的CLIP ViT-L/14骨干，确保了可比性。

## 6. 主要结论与发现
*   **关键发现**：VFM的中间层特征对SID任务最具判别力，其线性可分性最高，且特征空间结构更清晰。
*   **主要结论**：
    1.  I2P框架通过**自适应临界层识别**和**受控知识注入**，有效解决了VFM在SID任务中适应与泛化的矛盾。
    2.  在多个跨域基准上，I2P均取得了**稳定且领先**的泛化性能（例如，在Setting 1上平均ACC达98.22%），尤其在面对训练分布外的生成器时优势明显。
    3.  该方法具有**数据高效性**和**骨干网络通用性**。

## 7. 优点
*   **方法设计新颖**：将“识别关键表示层”和“受控参数更新”统一在一个框架内，从特征和参数两个层面进行优化，思路清晰且互补。
*   **动机与验证结合紧密**：通过T-SNE可视化、线性可分性分析、谱结构分析等，为“中间层更重要”和“需保护预训练结构”这两个核心假设提供了扎实的实证支持。
*   **实验全面且严谨**：跨域评估设置多样，对比基线强且多，消融实验充分，并验证了方法在不同VFM上的泛化性。
*   **实用性强**：仅需少量训练数据，且临界层之后的模块可被剪枝，具有潜在的计算效率优势。

## 8. 不足与局限
*   **理论深度有限**：论文主要基于经验观察和启发式设计，缺乏对方法有效性的严格理论证明或解释。
*   **应用场景局限**：实验主要针对**图像级**的二分类（真/假）检测，未探索其在更细粒度取证任务（如伪造区域定位、生成源归因）上的适用性。
*   **依赖特定VFM架构**：虽然验证了在多种VFM上的通用性，但方法核心（如使用CLS token、层评分函数）仍与Vision Transformer架构紧密相关，对其他架构（如CNN）的适用性未探讨。
*   **超参数敏感**：受控知识注入中的更新率η需要根据具体设置进行调整（论文中通过实验确定了较优值），存在一定的调参需求。

（完）
