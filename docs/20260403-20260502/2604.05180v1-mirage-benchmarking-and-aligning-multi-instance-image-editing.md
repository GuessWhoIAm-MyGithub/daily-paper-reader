---
title: "MIRAGE: Benchmarking and Aligning Multi-Instance Image Editing"
title_zh: MIRAGE：基准测试和对齐多实例图像编辑
authors: "Ziqian Liu, Stephan Alaniz"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.05180v1"
tags: ["query:imgfor"]
score: 9.0
evidence: 多实例图像编辑基准和框架
tldr: 本文针对指令引导图像编辑在多实例复杂场景中的挑战，提出了MIRAGE基准和框架。MIRAGE通过视觉语言模型解析指令，采用多分支并行去噪策略，实现精确的局部编辑并保持背景一致性。实验表明，该方法在多个基准上优于现有技术。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.05180v1/fig-001.webp\", \"caption\": \"Fig. 4: Overview of MIRAGE. (1) A VLM parses the global instruction into instance-level edits and localizes target regions via bounding boxes. (2) Regional branches perform parallel diffusion editing and inject their latents into the global branch; later time steps continue global editing under the global instruction while preserving background latents from the reference trajectory.\", \"page\": 7, \"index\": 1, \"width\": 643, \"height\": 441}, {\"url\": \"assets/figures/arxiv/2604.05180v1/fig-002.webp\", \"caption\": \"Table 3: Results on RefEdit-Bench. EditScore [20] for Prompt Following (PF) and Consistency (Cons), and GPT-5.1 evaluation of Perceptual Quality (PQ) in the range from 0 (lowest) to 10 (highest).\", \"page\": 10, \"index\": 2, \"width\": 723, \"height\": 215}, {\"url\": \"assets/figures/arxiv/2604.05180v1/fig-003.webp\", \"caption\": \"Table 2: Results on MIRA-Bench. EditScore [20] for Prompt Following (PF) and Consistency (Cons), and GPT-5.1 evaluation of Perceptual Quality (PQ) in the range from 0 (lowest) to 10 (highest).\", \"page\": 10, \"index\": 3, \"width\": 723, \"height\": 237}, {\"url\": \"assets/figures/arxiv/2604.05180v1/fig-004.webp\", \"caption\": \"Fig. 8: Performance under increasing instruction complexity. Results on MIRA-Bench using a replacement interval of [0.6T, T ].\", \"page\": 13, \"index\": 4, \"width\": 717, \"height\": 195}, {\"url\": \"assets/figures/arxiv/2604.05180v1/fig-005.webp\", \"caption\": \"Fig. 2: MIRA-Bench construction. (a) Images are generated from source prompts via FLUX.2 [4]. (b) A VLM extracts editing instructions and referring expressions. (c) VLM-predicted boxes are refined by SAM2 [32] into masks. Final benchmark triplets (image, instruction, mask) are human-validated.\", \"page\": 4, \"index\": 5, \"width\": 717, \"height\": 205}, {\"url\": \"assets/figures/arxiv/2604.05180v1/fig-006.webp\", \"caption\": \"Fig. 6: Effect of latent replacement time step ρ on MIRA-Bench. Results obtained with FLUX.2 [Dev]. Red circles highlight artifacts or over-editing regions.\", \"page\": 12, \"index\": 6, \"width\": 723, \"height\": 235}, {\"url\": \"assets/figures/arxiv/2604.05180v1/fig-007.webp\", \"caption\": \"Fig. 7: Effect of target and background replacement strategies on MIRABench. These results are obtained with FLUX.2 [Dev] using a replacement interval of [0.6T, T]. Red circles indicate over-editing and local detail inconsistencies.\", \"page\": 12, \"index\": 7, \"width\": 721, \"height\": 186}, {\"url\": \"assets/figures/arxiv/2604.05180v1/fig-008.webp\", \"caption\": \"Fig. 3: Example images and instructions from MIRA-Bench. The multiple similar instances and compositional instructions pose a challenge to SOTA models which introduce unintended modification in these complex scenarios. Through MIRAGE, we achieve precise instance-level editing while preserving background consistency.\", \"page\": 5, \"index\": 8, \"width\": 717, \"height\": 273}]"
motivation: 现有图像编辑模型在处理多个相似实例时存在过度编辑和空间错位问题，需要新的评估基准和方法。
method: 提出MIRAGE框架，利用视觉语言模型解析指令并采用多分支并行去噪策略进行精确编辑。
result: 在MIRA-Bench和RefEdit-Bench上，MIRAGE显著提升了实例级编辑的精确性和背景一致性。
conclusion: MIRAGE为多实例图像编辑提供了有效的基准和解决方案，推动了该领域的发展。
---

## 摘要
指令引导的图像编辑在FLUX.2和Qwen-Image-Edit等模型上取得了显著进展，然而在复杂场景中，当有多个相似实例且每个实例都需要单独编辑时，它们仍然面临困难。我们观察到，当面对多个相同实例和复合指令时，最先进的模型会遭受严重的过度编辑和空间错位。为此，我们引入了一个全面的基准测试，专门设计用于评估多实例和多指令设置中的细粒度一致性。为了解决在我们的基准测试中观察到的现有方法的失败，我们提出了通过引导编辑实现多实例区域对齐（MIRAGE），这是一个无需训练的框架，能够实现精确的局部编辑。通过利用视觉语言模型将复杂指令解析为区域子集，MIRAGE采用了多分支并行去噪策略。这种方法将目标区域的潜在表示注入全局表示空间，同时通过参考轨迹保持背景完整性。在MIRA-Bench和RefEdit-Bench上的广泛评估表明，我们的框架在实现精确的实例级修改同时保持背景一致性方面显著优于现有方法。我们的基准测试和代码可在https://github.com/ZiqianLiu666/MIRAGE获取。

## Abstract
Instruction-guided image editing has seen remarkable progress with models like FLUX.2 and Qwen-Image-Edit, yet they still struggle with complex scenarios with multiple similar instances each requiring individual edits. We observe that state-of-the-art models suffer from severe over-editing and spatial misalignment when faced with multiple identical instances and composite instructions. To this end, we introduce a comprehensive benchmark specifically designed to evaluate fine-grained consistency in multi-instance and multi-instruction settings. To address the failures of existing methods observed in our benchmark, we propose Multi-Instance Regional Alignment via Guided Editing (MIRAGE), a training-free framework that enables precise, localized editing. By leveraging a vision-language model to parse complex instructions into regional subsets, MIRAGE employs a multi-branch parallel denoising strategy. This approach injects latent representations of target regions into the global representation space while maintaining background integrity through a reference trajectory. Extensive evaluations on MIRA-Bench and RefEdit-Bench demonstrate that our framework significantly outperforms existing methods in achieving precise instance-level modifications while preserving background consistency. Our benchmark and code are available at https://github.com/ZiqianLiu666/MIRAGE.

---

## 论文详细总结（自动生成）

### 论文总结：MIRAGE: Benchmarking and Aligning Multi-Instance Image Editing

#### 1. 核心问题与整体含义
*   **研究动机**：当前基于指令的图像编辑模型（如FLUX.2、Qwen-Image-Edit）在处理包含多个相似实例且每个实例需要独立编辑的复杂场景时，表现出严重的**过度编辑**和**空间错位**问题。例如，当指令要求修改最左侧物体时，模型可能会错误地修改所有相似物体。
*   **背景与挑战**：现有图像编辑基准（如MagicBrush、RefEdit-Bench）主要关注单对象编辑或简单的引用表达，缺乏针对多实例、多指令组合场景的系统性评估工具，无法有效诊断上述细粒度错误。

#### 2. 提出的方法论
*   **核心思想**：提出一个**无需训练**的框架MIRAGE，通过将复杂的全局编辑指令分解为针对特定区域的子任务，并采用多分支并行去噪策略，实现对多个实例的精确、局部化编辑，同时保持背景完整性。
*   **关键技术细节**：
    1.  **指令解析与目标定位**：利用视觉语言模型（VLM，如Qwen3-VL）作为解析器，将一条复合指令 `I` 分解为一组对齐的子任务 `{(rk, Ik)}`，其中 `rk` 是目标的指代表达，`Ik` 是对应的原子编辑指令。随后，VLM根据 `rk` 在图像 `x` 中定位目标区域，生成边界框 `bk`，并裁剪出子区域 `xk`。
    2.  **局部化图像编辑（多分支并行去噪）**：
        *   **早期阶段 (t > ρT)**：启动 `K` 个区域编辑分支，每个分支独立对裁剪出的区域 `(xk, Ik)` 进行去噪，生成局部潜在表示 `zkt`。同时，通过前向扩散过程获得原始图像的背景潜在表示 `zref_t`。将 `zkt` 通过空间映射 `Π(·, bk)` 注入到 `zref_t` 的对应位置，形成融合的潜在表示 `zf_t`，用于下一步去噪。
        *   **晚期阶段 (t ≤ ρT)**：终止所有区域分支，仅使用一个全局分支，以原始图像 `x` 和全局指令 `I` 为条件进行去噪。在此阶段，仅更新目标区域的潜在表示，背景区域则继续使用来自参考轨迹的 `zref_t`。
    *   **关键公式**：融合过程通过掩码 `Mk`（对应边界框 `bk`）实现，确保目标区域被编辑，而背景区域被保留。

#### 3. 实验设计
*   **数据集/场景**：
    *   **MIRA-Bench**：本文提出的**新基准**，包含100个高质量样本。每个样本的图像包含3、4或5个相似实例，并配有5条同时执行的组合编辑指令。该基准通过自动化流程（使用LLM生成描述、VLM生成指令、SAM2生成掩码）并结合人工验证构建。
    *   **RefEdit-Bench**：一个已有的基准，包含200个图像-指令对，侧重于基于引用表达的单指令编辑。
*   **对比方法**：与多种代表性及SOTA模型进行比较，包括FLUX.2 [Dev/Klein]、Qwen-Image-Edit-2511、RefEdit(SD3)、MagicBrush以及GPT-Image-1.5。
*   **评估指标**：采用EditScore评估**指令遵循度**、**一致性**（背景保持）和**感知质量**，并计算综合得分。同时，也使用了PSNR、LPIPS、SSIM等传统图像相似度指标作为补充。

#### 4. 资源与算力
*   **算力**：所有实验在**单个NVIDIA A100 40GB GPU**上进行。
*   **训练**：MIRAGE是一个**无需训练**的框架，因此不涉及模型训练。其计算开销主要来自VLM的指令解析与目标定位（约11.3秒/图像）以及扩散模型的推理。由于在区域编辑阶段处理的潜在表示尺寸更小，MIRAGE的整体推理时间甚至略低于基础模型。

#### 5. 实验数量与充分性
*   **实验数量**：进行了大量实验，包括：
    *   在MIRA-Bench和RefEdit-Bench两个基准上的**主实验**（定量与定性比较）。
    *   **消融实验**：对MIRAGE的关键超参数（潜在替换时间步 `ρ`）和策略（目标/背景替换）进行分析。
    *   **指令复杂度分析**：测试模型在1、3、5条指令下的性能变化。
    *   **运行时与性能分析**：对比了标准推理、顺序推理和MIRAGE推理的效率与效果。
    *   **VLM选择消融**：测试了不同VLM（Qwen3-VL-8B/4B, RegionReasoner）对性能的影响。
*   **充分性与公平性**：实验设计较为充分和客观。在多个基准、多种基线模型上进行了系统评估。消融实验清晰地验证了框架各组件的有效性。评估指标结合了自动化模型（EditScore）和GPT-5.1，力求全面。

#### 6. 主要结论与发现
*   MIRAGE能**有效解决**多实例编辑场景中的过度编辑和空间错位问题。
*   将MIRAGE集成到现有SOTA模型（如FLUX.2、Qwen-Image-Edit）后，在MIRA-Bench和RefEdit-Bench上均能**显著提升指令遵循度和背景一致性**，且不损害（甚至略微提升）计算效率。
*   MIRAGE的性能对VLM的选择具有一定的鲁棒性，但依赖于VLM的定位精度。
*   潜在替换时间步 `ρ` 的选择需要在编辑准确性和视觉质量之间取得平衡，`[0.6T, T]` 是一个较优的区间。

#### 7. 优点
*   **无需训练**：即插即用，可直接增强现有扩散模型，无需额外训练成本。
*   **框架设计巧妙**：通过VLM解析实现指令与区域的精准对齐，通过多分支并行去噪和参考轨迹机制，在编辑目标的同时严格保护背景。
*   **评估基准有价值**：提出的MIRA-Bench填补了多实例复杂编辑评估的空白，为社区提供了重要的诊断工具。
*   **高效**：由于区域编辑阶段处理的潜在表示更小，推理效率高于基础模型。

#### 8. 不足与局限
*   **依赖VLM精度**：框架的性能上限受限于所使用的VLM进行指令解析和目标定位的准确性。
*   **对基础模型有要求**：当基础编辑模型（如Qwen-Image-Edit）在内部对输入图像进行强制尺寸缩放时，MIRAGE在早期阶段对局部区域的裁剪和编辑可能导致细节模糊或丢失，影响感知质量。
*   **评估指标局限**：尽管使用了多种指标，但自动评估（尤其是感知质量）仍可能无法完全替代人类判断。
*   **场景覆盖**：虽然MIRA-Bench已具挑战性，但现实世界中的多实例编辑场景可能更加复杂多样。

（完）
