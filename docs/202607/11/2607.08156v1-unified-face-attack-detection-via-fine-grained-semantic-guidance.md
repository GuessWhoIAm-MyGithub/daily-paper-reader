---
title: Unified Face Attack Detection via Fine-Grained Semantic Guidance
title_zh: 基于细粒度语义指导的统一人脸攻击检测
authors: "Ning Jiang, Shijie Yu, Dingheng Zeng, Haiyang Yi, Yanhong Liu, Haifeng Shen, Ying Li"
date: 2026-07-09
pdf: "https://arxiv.org/pdf/2607.08156v1"
tags: ["query:imgfor"]
score: 9.0
evidence: 直接解决图像操纵检测问题，提出了一种新网络用于伪造线索
tldr: 随着面部识别系统应用增多，安全威胁多样化，现有方法缺乏细粒度文本描述，导致检测效果有限。本文基于MS-UFAD数据集添加细粒度文本描述，并提出Dual Alignment Forgery Network (DAF-Net)网络，通过双对齐方式整合视觉和文本信息，提取更通用和语义有意义的伪造表示，实验表明优于现有方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.08156v1/fig-001.webp\", \"caption\": \"Fig. 2. The pipeline for generating descriptions. Initially, ScoreCAM [9] is employed to localize manipulated regions using a pre-trained model. These regions are then compared with corresponding genuine face images to quantify differences in attributes such as color and blur. The prompt built upon the differences and artifact corpus guides InternVL3 [10] to produce a detailed description of the forgery cues present in the attack image.\", \"page\": 2, \"index\": 1, \"width\": 445, \"height\": 470}, {\"url\": \"assets/figures/arxiv/2607.08156v1/fig-002.webp\", \"caption\": \"Fig. 3. The comparison between the descriptions from MS-UFAD dataset (black) and our generated descriptions (blue and green).\", \"page\": 2, \"index\": 2, \"width\": 446, \"height\": 300}, {\"url\": \"assets/figures/arxiv/2607.08156v1/fig-003.webp\", \"caption\": \"Fig. 4. An overview of the proposed Dual Alignment Forgery Network (DAF-Net). The architecture takes face attack images and the corresponding textual artifact descriptions as input. A vision encoder and a text encoder process the respective inputs. The Visual and Textual Semantic Forgery Aggregation modules then extract and refine forgery-specific features. Then the aligned features are finally pooled and processed by the visual and textual classifiers to produce the final prediction. During inference, the textual branch will be removed.\", \"page\": 3, \"index\": 3, \"width\": 1060, \"height\": 300}]"
motivation: 现有面部攻击检测方法忽视细粒度文本信息，限制了检测的通用性和语义理解。
method: 构建细粒度文本描述数据集，并提出DAF-Net来整合视觉和文本特征。
result: 实验表明，该方法提取的伪造表示更通用和语义有意义，优于仅视觉方法和粗粒度描述方法。
conclusion: 细粒度语义指导能有效提升面部攻击检测的性能和泛化能力。
---

## 摘要
随着人脸识别系统应用的日益增长，安全威胁也日益多样化。现有数据集缺乏对伪造线索的详细文本描述，导致大多数先前方法主要将人脸攻击检测视为视觉识别任务。在本文中，基于包含超过800万攻击图像的大规模MS-UFAD数据集，我们为每张图像添加了细粒度的伪造线索文本描述。此外，我们提出了一个双重对齐伪造网络（DAF-Net），以更好地利用这些文本信息。大量实验证明，我们的方法从攻击图像中提取了更具泛化性和语义意义的伪造表示，优于仅基于视觉的方法和基于粗粒度描述的方法。

## Abstract
The growing applications of facial recognition systems are accompanied by increasingly diverse security threats. Existing datasets lack detailed textual descriptions of forgery cues, leading most prior methods to treat face attack detection primarily as a visual recognition task. In this paper, building upon the large-scale MS-UFAD dataset which contains over 8 million attack images, we enrich each image with a fine-grained textual description of forgery cues. Furthermore, we propose a Dual Alignment Forgery Network(DAF-Net) to better leverage these textual information. Extensive experiments demonstrate that our approach extracts more generalizable and semantically meaningful forgery representations from attack images, outperforming both vision-only methods and approaches based on coarse-grained descriptions.

---

## 论文详细总结（自动生成）

# 论文分析总结：基于细粒度语义指导的统一人脸攻击检测

## 1. 论文的核心问题与整体含义
*   **研究背景与动机**：随着人脸识别系统应用日益广泛，其面临的安全威胁（如Deepfake、对抗攻击、呈现攻击等）也愈发多样化。现有的大多数人脸攻击检测方法将问题主要视为一个**纯视觉识别任务**。
*   **核心问题**：现有数据集普遍缺乏对**伪造线索的详细、细粒度的文本描述**。即使是尝试引入文本的方法，也多使用类别级（如“这是假脸”）或粗粒度描述，无法精确指导模型学习可泛化、语义有意义的伪造特征。
*   **整体含义**：本文的核心主张是，通过为攻击图像引入**细粒度的文本描述**来指导检测模型，能够学习到更具泛化能力和语义可解释性的伪造表征，从而提升统一人脸攻击检测的性能。

## 2. 论文提出的方法论
*   **核心思想**：构建一个**视觉-语言对齐框架**，利用细粒度文本作为“语义指南针”，引导视觉模型关注并学习图像中具体的、可解释的伪造痕迹。
*   **关键技术细节**：
    1.  **细粒度文本生成流程**：
        *   首先，利用预训练检测模型和`ScoreCAM`热力图**定位图像中的篡改区域**。
        *   接着，将篡改区域与对应的真实人脸区域对比，**量化颜色、模糊度等差异**。
        *   然后，构建包含针对不同攻击类型（如FaceSwap）的**伪造线索语料库**。
        *   最后，将量化差异和语料库作为提示，输入**多模态大语言模型**（如InternVL3），生成针对每张图像的、描述具体伪造细节的自然语言描述。
    2.  **双重对齐伪造网络（DAF-Net）**：
        *   **架构**：包含对称的视觉和文本编码分支（基于BLIP骨干网络）。
        *   **语义伪造聚合模块**：在每个分支后插入一个轻量模块，使用一组可学习的查询向量，通过交叉注意力机制，将离散的视觉块或文本词元聚合成语义上更完整的**伪造区域/短语**。
        *   **对齐与损失函数**：
            *   **全局对齐**：在聚合后的视觉和文本表征上应用对比损失。
            *   **细粒度对齐**：计算视觉区域与文本短语之间的词元级相似性矩阵，并利用跨模态后期交互和三元组损失进行优化。
            *   **总损失**：`L = L_cls + λ(L_global + L_fine)`，其中`L_cls`为分类损失。
*   **关键点**：**推理阶段仅使用视觉分支**，文本描述仅在训练时作为监督信号。

## 3. 实验设计
*   **数据集与场景**：
    *   **主要数据集**：在**MS-UFAD**大规模数据集（约800万张图像）上进行实验。论文为其**新增了超过800万条细粒度文本描述**。
    *   **划分**：训练集来自2000个身份和10种生成方法（83万张图像），测试集涵盖30种生成方法（使用其中20万张图像-文本对的子集）。
*   **Benchmark与对比方法**：
    *   **评估指标**：ACER（平均分类错误率）、准确率（ACC）、F1分数。
    *   **对比基线**：
        *   **纯视觉方法**：ResNet50、ViT、BLIP-ViT、以及加入SFAM的BLIP-ViT（验证增益非来自模块本身）。
        *   **文本引导方法**：
            *   使用类别级文本的DAF-Net。
            *   使用MS-UFAD原始粗粒度描述的DAF-Net。
            *   **本文方法**：使用细粒度描述的DAF-Net。

## 4. 资源与算力
*   **硬件资源**：使用**4块NVIDIA RTX 5090 GPU**进行训练。
*   **训练时长**：模型训练**10个epochs**。
*   **其他细节**：输入图像尺寸为224x224，优化器为AdamW，学习率采用预热-余弦退火策略。

## 5. 实验数量与充分性
*   **实验数量**：论文进行了**系统性的实验**：
    *   **主实验**：在统一的测试协议下，对比了7种不同配置（包括各种基线）的方法。
    *   **消融实验**：进行了5组实验，分别验证了全局对齐损失、细粒度对齐损失、视觉SFAM、文本SFAM以及两者结合的影响。
    *   **可视化分析**：通过注意力图对比展示了方法的可解释性。
*   **充分性与公平性评估**：实验设计**较为充分和公平**。对比了不同模态（纯视觉、粗文本、细文本）和不同技术组件（有无SFAM）的贡献。使用了统一的评估指标和划分好的测试集。消融实验清晰地分离了各模块的作用。使用了大规模、多样化的数据集，增强了结论的可信度。

## 6. 论文的主要结论与发现
*   **主要结论**：细粒度的文本语义指导能够显著提升统一人脸攻击检测模型的性能和泛化能力。
*   **关键发现**：
    1.  相比纯视觉方法和基于类别级文本的方法，本文方法在ACER、ACC和F1上均有**显著提升**。
    2.  相比使用原始粗粒度描述的方法，细粒度描述带来了**进一步的性能增益**（ACER降低2.94%），证明了描述精度的重要性。
    3.  **语义伪造聚合模块**和**细粒度对齐损失**对模型学习至关重要。移除任何一个都会导致性能明显下降。
    4.  可视化表明，文本引导使模型的注意力**更准确地集中在图像中的实际篡改区域**，提升了可解释性。

## 7. 优点
*   **方法创新性**：提出了从“粗粒度/无文本”到“细粒度文本”指导的范式转变，并设计了完整的生成与建模流水线。
*   **数据资源贡献**：构建并开源了一个包含800万条**细粒度伪造线索描述**的大规模多模态数据集，是本领域的重要资源。
*   **模型设计合理**：DAF-Net架构设计简洁有效，其SFAM模块解决了视觉块与文本词元之间的粒度不对齐问题，双重对齐策略兼顾了全局和细粒度匹配。
*   **实验全面**：实验设计系统，包含充分的对比和消融研究，结论有力。
*   **可解释性**：通过文本描述和可视化注意力图，增强了模型决策的透明度。

## 8. 不足与局限
*   **数据集生成的依赖与局限**：
    *   细粒度描述的质量依赖于预训练检测模型和MLLM的性能，可能存在**描述不准确或引入偏差**的风险（尽管论文采取了缓解措施）。
    *   数据集基于特定的MS-UFAD构建，其**泛化性**需在其他数据集或更开放的场景中进一步验证。
*   **方法假设的局限性**：
    *   训练阶段需要成对的“攻击图像-文本描述”，这**增加了数据准备的成本**。在仅有图像而缺乏文本描述的场景下，无法进行增量学习。
    *   模型主要针对**数字篡改/生成**的伪造痕迹设计，对于**物理呈现攻击**（如打印照片、3D面具）的细粒度描述和效果，文中未深入探讨。
*   **实验覆盖范围**：
    *   主要对比的是视觉或视觉-语言模型，未与一些**基于噪声、频率分析或自监督**的先进纯视觉方法进行更广泛的对比。
    *   测试集的生成方法虽多（30种），但均来自MS-UFAD，对于**“野外”未知攻击方法**的泛化能力有待验证。

（完）
