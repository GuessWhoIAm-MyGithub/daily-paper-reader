---
title: "Watermark Forensics for Generative Models: An Information-Theoretic Perspective"
title_zh: 生成模型的水印取证：信息论视角
authors: "Xiaoyu Li, Zheng Gao, Xiaoyan Feng, Jiaojiao Jiang, Yulei Sui, Jiankun Hu"
date: 2026-07-14
pdf: "https://arxiv.org/pdf/2607.13003v1"
tags: ["query:imgfor"]
score: 9.0
evidence: 用于定位编辑部分的水印取证
tldr: 研究生成模型水印的法医应用，如用户归因、载荷提取和内容定位，基于信息论视角引入信息剖面ν(t)来量化令牌对秘密的信息贡献，建立多用户归因的精确熵率法则为Θ(log N/h)个令牌，并通过GPT-2等模型实验验证预测常数。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.13003v1/fig-001.webp\", \"caption\": \"Figure 1: The forensic ladder, and the one measure beneath it. (a) The four forensic questions form a sample-complexity ladder. Detection is cheap and mechanism-blind; but attribution is not free (naming one of N users costs Θ(log N/∆) or Θ(log N/h) in the appropriate per-token currency, the jump from detection to attribution), and extraction and crop-robust localization sit higher still. (b) The rungs above detection are read off a single object, the information profile ν(t) = I(S; Xt | X<t). The literature’s two quality models are two incomparable caps on the same ν: a biasing mark spends the mass thinly under an L∞ height cap ∆ (subtle on every token, full support), an embedding mark concentrates it under an L0 width cap m (a short, loud stamp). Same shaded mass ∥ν∥1 = I(S; X), two shapes.\", \"page\": 1, \"index\": 1, \"width\": 1093, \"height\": 932}, {\"url\": \"assets/figures/arxiv/2607.13003v1/fig-002.webp\", \"caption\": \"Figure 4: The forensic-recovery budget (Theorem 3.1). The secret reaches the carrier only through the key, so S → K → X is a Markov chain, and three forced steps (data processing, then the golden formula, then the quality budget) bound the recoverable information Φ(n) = I(S; X) by the distortion actually spent, n∆. Nothing about embedding versus biasing enters: it is one ledger, and every nat it holds is shared between attribution and extraction (Proposition 3.13).\", \"page\": 11, \"index\": 2, \"width\": 838, \"height\": 318}, {\"url\": \"assets/figures/arxiv/2607.13003v1/fig-003.webp\", \"caption\": \"Table 2: Where deployed watermarks fall, with the footprint criterion applied uniformly. “Basis” is the representation the extractor reads; “Footprint” is the per-secret footprint T (Corollary 3.4), full (Ω(n)) or small (o(n)) (footprint read on per-secret kernels; Section C.8); “Model” is the operative quality model; “Readout” is global (whole-carrier statistic) or fine (per-region); “Levels” lists the forensic rungs the base construction reaches, with parenthesized rungs reached only by descendant or extension variants (multi-bit, many-user, or message-channel versions), not the base scheme. Almost every deployed scheme is full-support (biasing); the genuine o(n)-footprint cases are Tree-Ring (a ring in the Fourier basis) and the theoretical digit-stamp. Level-3 localizers are full-support with fine readout, exactly as Theorem 3.12 requires.\", \"page\": 25, \"index\": 3, \"width\": 1010, \"height\": 548}, {\"url\": \"assets/figures/arxiv/2607.13003v1/fig-004.webp\", \"caption\": \"Figure 6: The surprisal threshold does both jobs (Theorem 3.6). The decoder accepts a consistent key only when the realized surprisal ı(X) = − log pnat(X) exceeds τ = log(N/δ), and that single threshold is forced, not tuned. (ii) The marked text is itself pnat-typical, so ı(X)/n → h (Shannon–McMillan–Breiman) and clears τ once n ≥ τ/h: this is why the rate is the entropy h. (i) An innocent key produces one fixed string, which coincides with X only with pnat-mass e−ı < e−τ = δ/N : the per-innocent guarantee is exact and pointwise. To frame no innocent above δ/N one must reject every candidate below τ , and completeness then only asks that the true text clear the same bar.\", \"page\": 15, \"index\": 4, \"width\": 1010, \"height\": 262}, {\"url\": \"assets/figures/arxiv/2607.13003v1/fig-005.webp\", \"caption\": \"Figure 11: The collision trap on the deployed EMS (Gumbel-max) distortion-free watermark, GPT-2. (a) The marked text’s Shannon (h) and Rényi-2 (r2) rates coincide with unmarked sampling (dotted): the watermark is distortion-free, so the entropy column of Theorem 3.6 applies. (b) To implicate no innocent user above δ/N , the consistency-list rule needs log(N/δ)/r2 tokens (amber) while surprisal thresholding needs log(N/δ)/h (blue); the shaded overcharge is the factor h/r2 = 1.61, widening as Θ(log N).\", \"page\": 21, \"index\": 5, \"width\": 988, \"height\": 352}, {\"url\": \"assets/figures/arxiv/2607.13003v1/fig-006.webp\", \"caption\": \"Figure 12: Detection is not attribution on the deployed green-list watermark, GPT-2. (a) Detection power saturates by n ≈ 28 tokens while attribution accuracy (shown for N = 220) lags; in the shaded window the text is detectable yet unattributable. (b) The attribution length (the 50%-accuracy crossing) grows linearly in log N (+2.3 tokens per doubling of N); the detection threshold is flat in N . The finite-N form of the Θ(log N) gap of Theorem 3.2.\", \"page\": 22, \"index\": 6, \"width\": 985, \"height\": 352}, {\"url\": \"assets/figures/arxiv/2607.13003v1/fig-007.webp\", \"caption\": \"Figure 3: The results at a glance. Every result is a reading of the information profile ν. The forensic-recovery budget (Theorem 3.1) caps its mass ∥ν∥1 = I(S; X); the four results left of the divider concern that mass budget (the detect/attribute separation; the two quality models; the closed entropy column; the shared rate region), while Theorem 3.12 reads its shape. Solid arrows mark a direct dependence, dashed arrows a secondary use (Theorem 3.2 supplies the dichotomy’s lower bound; Theorem 3.3(a) supplies the rate region’s achievability). Corollaries and the collision-trap proposition hang under their parents; the imported and textbook facts each proof draws on are catalogued in Section C.1.\", \"page\": 10, \"index\": 7, \"width\": 879, \"height\": 344}, {\"url\": \"assets/figures/arxiv/2607.13003v1/fig-008.webp\", \"caption\": \"Table 3: Notation used throughout the paper.\", \"page\": 33, \"index\": 8, \"width\": 956, \"height\": 998}, {\"url\": \"assets/figures/arxiv/2607.13003v1/fig-009.webp\", \"caption\": \"Figure 8: Why a small footprint cannot localize at fine resolution (Theorem 3.12). Under the edit coupling (Assumption 2.3), a crop window that misses the dilated footprint R+ sees text distributed exactly as pnat, so a detector reading only that window tests pnat against pnat and cannot beat chance. Every length-w window must therefore meet R+, which forces the marks no more than w apart: |R| w = Ω(n). Few marks buy only coarse resolution; fine resolution forces full support, that is, biasing.\", \"page\": 18, \"index\": 9, \"width\": 1010, \"height\": 225}, {\"url\": \"assets/figures/arxiv/2607.13003v1/fig-010.webp\", \"caption\": \"Figure 13: The overcharge h/r2 across model families. On GPT-2, Pythia-410M, and Qwen2.5-0.5B the absolute rates differ, but r2 < h with a stable ratio ≈ 1.6: the consistency-list rule costs ∼ 60% more tokens than surprisal thresholding on every model. The collision trap is a property of natural-language entropy.\", \"page\": 23, \"index\": 10, \"width\": 805, \"height\": 372}, {\"url\": \"assets/figures/arxiv/2607.13003v1/fig-011.webp\", \"caption\": \"Figure 5: The bridge is one-directional (Theorem 3.3, Corollary 3.4). Crossing “footprint” with “quality model used” leaves exactly one cheap, ∆-free cell, the stamp, and it needs both a small footprint and actually using the footprint model E(m). A small footprint (a localization map) enables the cheap model but does not decide the cost: a small mark that still obeys the per-token cap B(∆) stays ∆-coupled (top-left). A full-support biasing scheme has no small footprint, so it is confined to B(∆) (bottom-left); the ∆-free corner is simply unreachable for it (bottom-right). This is a one-way bridge, not an “embedding ⇔ ∆-free” equivalence.\", \"page\": 13, \"index\": 11, \"width\": 687, \"height\": 262}, {\"url\": \"assets/figures/arxiv/2607.13003v1/fig-012.webp\", \"caption\": \"Figure 2: One profile, read two ways (Proposition 2.6). The information profile ν(t) = I(S; Xt | X<t) is a measure on positions, and a measure has a total and a support. The forensic tasks above detection are exactly these two functionals: attribution and extraction read the mass ∥ν∥1 = I(S; X) = Φ(n), which the forensic-recovery budget caps (Theorem 3.1); localization reads the shape supp ν; detection is the mechanism-blind baseline, priced by presence. The two quality models of the literature are then two caps on the same ν: a height cap ν(t) ≤ ∆ (L∞, Proposition 2.6(ii), biasing) and a support cap | supp ν| ≤ m (L0, Proposition 2.6(iii), embedding).\", \"page\": 8, \"index\": 12, \"width\": 1010, \"height\": 354}, {\"url\": \"assets/figures/arxiv/2607.13003v1/fig-013.webp\", \"caption\": \"Figure 9: Two rate regions, one shape (Proposition 3.13). Attribution and extraction draw on the single budget of Theorem 3.1, so their rates trade off along a slope-(−1) frontier. (a) Under the per-token cap B(∆) the frontier is soft, order-tight Θ(n∆), lying between the achievable nC and the converse n∆ (C ≈ ∆/4 in the binary witness of Section 3.6). (b) Under the footprint cap E(m) it is sharp and constant-exact, Ratt + Rext = m log q, met with equality and zero error by the stamp. The same simplex, with a soft boundary on one side and a hard one on the other.\", \"page\": 19, \"index\": 13, \"width\": 793, \"height\": 411}, {\"url\": \"assets/figures/arxiv/2607.13003v1/fig-014.webp\", \"caption\": \"Table 1: The forensic ladder. Symbols: ∆ per-token KL budget; h per-token entropy rate; λ detection soundness (FP ≤ 2−λ); q = |Σ| alphabet size; m footprint; w crop window; N users; ℓ payload bits. In the distributional column the lower bounds (Ω) are unconditional; the matching upper bounds (Θ, shown in brackets) hold for regular, strongly-mixing pnat (Theorem 3.3) and are conjectural for general q-ary pnat. The embedding column is two-sided (Θ, exact construction); the distortion-free (h) sub-column is now two-sided at Levels 1–2 (Theorem 3.6: stationary-ergodic source, i.i.d. keys, decoder knowing pnat and the key set). The bounded Level-0 rate Θ(1/∆) is the imported baseline (Cai et al., 2024; Huang et al., 2023; Li et al., 2025b); the distortion-free Level-0 rate Θ(λ/h) (with λ the entropy needed for 2−λ false-positive security) is from Christ et al. (2024); the bounded-regime attribution converse is the single-user Fano specialization of Moulin (2008); Somekh-Baruch and Merhav (2005). Level 3 (crop-robust localization to resolution w, Theorem 3.12) turns on a different axis, readout resolution: it forces full support, so the small-footprint embedding column cannot reach it.\", \"page\": 17, \"index\": 14, \"width\": 1010, \"height\": 555}, {\"url\": \"assets/figures/arxiv/2607.13003v1/fig-015.webp\", \"caption\": \"Figure 14: The two orthogonal axes of the taxonomy. The footprint |T | (horizontal; small gold dots = embedding, full-support blue dots = biasing) governs the ∆-free decoupling of Theorem 3.3; the readout resolution (vertical) governs localization (Theorem 3.12). Being independent, the two axes let deployed schemes spread across three quadrants, and the localizers (SEAL, EditGuard, AudioSeal) reach Level 3 by fine readout, not a small footprint. The fourth quadrant, small footprint with fine resolution, is empty for a reason: Theorem 3.12 forbids it, m w = Ω(n).\", \"page\": 24, \"index\": 15, \"width\": 671, \"height\": 403}, {\"url\": \"assets/figures/arxiv/2607.13003v1/fig-016.webp\", \"caption\": \"Figure 10: Seeded Monte-Carlo confirmation of the separation (binary tilting code, s = 0.08, so the per-token budget is ∆ = KL(pwm ∥ pnat) /n and the channel capacity C = I(b; X) ≈ ∆/4). (a) At N = 4096, detection power saturates to 1 while attribution accuracy is still near chance over a wide window (shaded): one can detect long before one can attribute. (b) The attribution threshold grows linearly in log N (slope matching ∆/C, dashed), while the detection threshold is flat in N ; their ratio is Θ(log N).\", \"page\": 20, \"index\": 16, \"width\": 984, \"height\": 361}, {\"url\": \"assets/figures/arxiv/2607.13003v1/fig-017.webp\", \"caption\": \"Figure 7: The collision trap (Proposition 3.9). The obvious decoder answers with any key consistent with the text; its list of falsely consistent innocents, (N − 1)e−nr2 , clears only at n = log N/r2. So a count-based analysis predicts that rate. But the surprisal threshold decides already at n = log N/h, deep inside the window where the list is still exponentially large (≈ e n(h−r2)): it singles out the true key within the list rather than waiting for the list to shrink. The Rényi-2 rate r2 prices the candidate list; the Shannon rate h prices the decision, and r2 < h makes the gap h/r2 unbounded.\", \"page\": 16, \"index\": 17, \"width\": 913, \"height\": 346}]"
motivation: 现有水印技术仅用于检测机器生成，但法医应用如用户归因、载荷提取和定位的成本未知，需要从信息论角度系统分析其理论极限。
method: "采用信息剖面ν(t)=I(S;X_t|X_{<t})建模水印信息分布，分析归因、提取和定位任务在样本长度n上的理论成本。"
result: 建立了多用户归因的熵率法则，成本为Θ(log N/h)个令牌，首次提供精确熵率常数，并发现归因窗口和不确定性原理两个真实差距。
conclusion: 水印法医分析的信息论框架揭示了不同任务的成本规律，为生成模型水印设计提供了理论指导。
---

## 摘要
通常，生成模型输出中的水印仅用于判断文本是否由机器生成。同一个标记可以做更多事情：归因到生成它的用户、提取隐藏的负载，或定位在编辑后幸存的部分。这些构成了一个取证阶梯，我们询问每个阶梯在样本长度$n$中的成本。一个对象组织了这些答案。设$S$为标记携带的秘密（用户身份或负载），令信息剖面$ν(t)=I(S;X_t\mid X_{<t})$记录第$t$个标记在给定前序标记的情况下揭示了多少关于$S$的信息。它的总质量用于支付归因和提取；质量如何分布用于支付定位；而检测单独由存在性支付，即标记分布与未标记分布之间的距离。文献中的两个质量模型，一个在每个标记上细微，另一个在少数标记上大声盖印，是限制这个剖面的两种不可比方式。我们的主要定理解决了阶梯的熵列。对于统计无失真方案，将文本归因到$N$个用户之一，在每个熵率为$h$的平稳遍历源上花费$Θ(\log N/h)$个标记，精确到$(1+o(1))$因子：据我们所知，这是多用户归因（通过精确对齐）的第一个紧熵率定律。自然的碰撞计数分析无界地过度收费；只有解码器通过其自身实现的惊奇度阈值化每个候选者才能达到该速率，同时几乎从不牵连无辜用户。一个匹配的逆定理使定律双向成立，提取$\ell$位负载花费$Θ(\ell/h)$。两个差距是真实的，不是建模产物：一个$Θ(\log N)$个标记的窗口，其中文本可证明是机器生成的但无法归因，以及一个足迹-分辨率不确定性原理。在GPT-2、Pythia-410M和Qwen2.5上的实验恢复了预测的常数。

## Abstract
A watermark in a generative model's output is usually asked only whether a text is machine-made. The same mark can do more: attribute it to the user who produced it, extract a hidden payload, or localize the part that survives editing. These form a forensic ladder, and we ask what each rung costs in the sample length $n$.   One object organizes the answers. Let $S$ be the secret the mark carries (a user's identity or payload), and let the information profile $ν(t)=I(S;X_t\mid X_{<t})$ record how much the $t$-th token reveals about $S$ given the earlier ones. Its total mass pays for attribution and extraction; how that mass is spread pays for localization; and detection alone is paid for not by information but by presence, the distance from the marked to the unmarked distribution. The literature's two quality models, a mark subtle on every token and one that stamps a few tokens loudly, are two incomparable ways of capping this profile.   Our main theorem settles the ladder's entropy column. For statistically distortion-free schemes, attributing a text to one of $N$ users costs $Θ(\log N/h)$ tokens over every stationary-ergodic source of entropy rate $h$, sharp to a $(1+o(1))$ factor: to our knowledge the first tight entropy-rate law for multi-user attribution (via exact alignment). The natural collision-counting analysis overcharges without bound; only a decoder thresholding each candidate by its own realized surprisal attains the rate while almost never implicating an innocent user. A matching converse makes the law two-sided, and extraction of an $\ell$-bit payload costs $Θ(\ell/h)$. Two gaps are real, not modeling artifacts: a $Θ(\log N)$-token window in which a text is provably machine-made yet unattributable, and a footprint-resolution uncertainty principle. Experiments on GPT-2, Pythia-410M, and Qwen2.5 recover the predicted constants.

---

## 论文详细总结（自动生成）

### 论文总结：生成模型的水印取证：信息论视角

#### 1. 核心问题与整体含义
*   **研究动机**：现有对生成模型水印的研究主要集中于检测（即判断内容是否由AI生成）。然而，水印在法医应用上潜力更大，包括：**用户归因**（追踪内容到特定用户）、**负载提取**（恢复隐藏信息）和**内容定位**（识别编辑后仍存在的标记部分）。这些任务构成一个“取证阶梯”，但其理论代价（需要多长的内容序列）尚不明确。
*   **背景与意义**：随着生成模型（如LLM）的普及，水印正成为内容溯源的基础设施。监管机构开始要求机器生成内容带有可追溯的标记。本文旨在从信息论角度，为水印的不同法医能力建立一个统一的理论框架和代价分析，为水印设计与法规评估提供理论基础。

#### 2. 方法论
*   **核心思想**：引入一个统一的度量对象——**信息剖面** (Information Profile) `ν(t) = I(S; X_t | X_{<t})`。它量化了在已知前文 `X_{<t}` 的情况下，生成的第 `t` 个token `X_t` 关于秘密 `S`（如用户ID、负载）所提供的信息量（单位：纳特）。
*   **关键技术与分析**：
    1.  **取证阶梯模型**：将水印法医问题建模为四个层级的任务：检测 (Level 0)、归因 (Level 1)、提取 (Level 2)、定位 (Level 3)。
    2.  **信息剖面的两个函数**：
        *   **质量** (`∥ν∥_1 = I(S; X)`)：剖面的总质量，是支撑归因和提取任务的信息预算上限（**取证恢复预算**）。
        *   **形状** (`supp ν`)：剖面的支撑集，与载体上的编辑耦合共同决定定位能力。
    3.  **两个质量模型**：将文献中两种主要的水印质量约束统一为对信息剖面的范数约束：
        *   **分布上限** `B(Δ)`（偏置型）：每个token的条件KL散度不超过 `Δ`，对应信息剖面的 `L∞` 上界。
        *   **足迹上限** `E(m)`（嵌入型）：水印仅作用于 `m` 个位置，对应信息剖面的 `L0` 上界（支撑集大小 ≤ m）。
    4.  **核心理论结果**：
        *   **下界推导**：主要工具是数据处理不等式和Fano不等式。证明了归因任务需要的信息量至少为 `log N`，而每个token最多提供 `Δ`（或受源熵率 `h` 限制）的信息，因此序列长度 `n` 必须满足 `n = Ω(log N / Δ)` 或 `Ω(log N / h)`。
        *   **上界/可达性构造**：针对分布上限，设计了基于**指数倾斜**的条件编码方案；针对无失真（`h`受限）情况，设计了**惊奇度阈值解码器**。
        *   **关键算法（惊奇度阈值解码器）**：用于无失真归因。解码器知道自然分布 `p_nat` 和所有注册密钥。对于给定文本 `X`，计算其实现惊奇度 `ı(X) = -log p_nat(X)`。只有当 `ı(X) > τ = log(N/δ)` 时，解码器才接受与 `X` 一致的密钥，否则宣布失败。该阈值是自然导出的，而非调优所得。

#### 3. 实验设计
*   **场景与数据集**：
    *   主要在**语言模型生成文本**的场景下进行验证。
    *   使用了三个预训练语言模型作为生成器：**GPT-2**、**Pythia-410M**、**Qwen2.5-0.5B**。
    *   使用了两种具体的水印方案进行实证：**偏置型的绿色列表（green-list）** 和 **无失真的指数最小采样（EMS/Gumbel-max）**。
*   **基准**：
    *   理论预测的核心常数：归因成本应为 `Θ(log N / h)`，其中 `h` 是源的熵率。
    *   特别验证了“碰撞陷阱”：朴素的一致性解码器分析会错误预测归因需要 `log N / r_2` 个token（`r_2`为Rényi-2熵率），而 `h/r_2` 这个过度收费因子应约为1.6。
*   **对比方法**：
    *   在数值实验中，对比了本文提出的**惊奇度阈值解码器**与朴素的**一致性解码器**（仅检查密钥是否与文本一致）。
    *   验证了不同水印方案（绿色列表 vs EMS）下，检测与归因能力的分离现象。

#### 4. 资源与算力
*   论文中**未明确提及**实验所用的具体GPU型号、数量及训练时长。实验主要分为两部分：
    1.  **受控数值实验**：在简单的二元源上进行，算力需求不大。
    2.  **语言模型实证**：使用预训练模型进行推理和水印生成/检测，这些模型本身不算大，未提及微调，因此主要算力消耗在于生成大量文本进行统计分析。

#### 5. 实验数量与充分性
*   **实验组数**：论文进行了三类核心实验：
    1.  **二元倾斜码的数值模拟**：系统性地验证了检测与归因的分离、归因成本与 `log N` 的关系。
    2.  **GPT-2上的碰撞陷阱验证**：具体测量了EMS水印方案下 `h` 和 `r_2` 的值，并对比了两种解码器的保证长度。
    3.  **跨模型过度收费常数测量**：在GPT-2、Pythia、Qwen三个模型上，测量了自然语言的 `h/r_2` 比值。
*   **充分性与公平性**：实验设计较为充分和客观。它不仅验证了主要理论预测（如 `log N` 代价、`h/r_2` 常数），还通过在不同模型和水印方案上的测量，验证了结论的普适性。实验使用了受控的随机种子，确保可复现性。未涉及与其他信息论框架的直接定量对比，因为本文主要贡献在于建立理论框架和闭合证明，而非提出一个新方案。

#### 6. 主要结论与发现
1.  **建立了取法阶梯的精确代价**：
    *   **归因**：对于统计无失真方案，精确归因 `N` 个用户所需的文本长度为 `Θ(log N / h)`。首次给出了紧的熵率定律。
    *   **提取**：提取 `ℓ` 位负载需要 `Θ(ℓ / h)` 个token。
2.  **揭示了“检测不等于归因”**：存在一个长度为 `Θ(log N)` 的窗口，在此窗口内，水印是可检测的（机器生成），但理论上无法归因到具体用户。
3.  **发现了碰撞陷阱**：朴素的基于碰撞计数的分析会高估归因代价（过度收费因子 `h/r_2 ≈ 1.6`）。惊奇度阈值解码器能以更低的代价（`log N/h`）实现归因，同时严格控制对无辜用户的误指。
4.  **提出了足迹-分辨率不确定性原理**：要实现对裁剪鲁棒的精细定位（分辨率 `w`），水印在载体中的足迹 `|R|` 必须满足 `|R| * w = Ω(n)`。这意味着小足迹的嵌入型水印无法实现精细的裁剪鲁棒定位。

#### 7. 优点
*   **理论创新性强**：提出并系统运用“信息剖面” `ν(t)` 作为核心分析工具，统一了之前分散的质量模型和法医任务分析，框架清晰优雅。
*   **理论贡献完整**：不仅给出了下界（逆定理），还构造了可达方案，特别是为无失真归因给出了闭合的、双向紧致的 `Θ(log N / h)` 定律，并明确了其适用范围（精确对齐、知晓自然分布和注册密钥）。
*   **洞察深刻**：清晰地区分了信息剖面的“质量”和“形状”对不同任务的影响，以及“偏置”与“嵌入”两种水印范式在理论代价上的根本差异。
*   **实验设计巧妙**：通过精心设计的受控实验和语言模型实证，有效地验证了理论预测的常数和现象，使理论分析落地。

#### 8. 不足与局限
*   **理论假设的限制**：主要的紧致理论结果（如定理3.6）依赖于较强的假设，如**精确对齐**（水印方案是逆采样等无失真方案，且解码器知道精确的自然分布 `p_nat` 和注册密钥集）。这忽略了实践中计算上的不可区分性、以及面对编辑（如改写、裁剪）时的鲁棒性问题。
*   **小足迹方案不实用**：证明中 `Δ` 无关的归因代价 `Θ(log N / log q)` 依赖于一个理论上的“明文数字盖印”方案，该方案完全不具备任何编辑鲁棒性。
*   **开放问题的核心**：论文自身指出的最尖锐开放问题是：是否存在一个**既对负载实现 `Δ`-无关的低价归因，又能对定位层支付 `m*w = Ω(n)` 税收的、可部署的（不可感知、编辑鲁棒）水印方案**。目前的理论构造无法实现。
*   **语言模型外的推广**：理论框架主要针对自回归序列生成。对于非自回归或扩散模型等其它生成范式，信息剖面的定义和分析可能需要调整。
*   **算力描述缺失**：实验部分未提供详细的计算资源信息，不利于复现和评估实验规模。

（完）
