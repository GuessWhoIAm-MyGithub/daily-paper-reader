---
title: "CR-Refiner: An Object-Centric Optimal Transport Reranker for Edit-Conditioned 3D Scene Retrieval"
title_zh: CR-Refiner：一种面向对象的最优传输重排序器，用于编辑条件的三维场景检索
authors: "Hao Wu, Jinjing Zhu, Nanyu Wu, Qianyi Cai, Heyi Lin, Hao Wang, Hui Xiong"
date: 2026-07-21
pdf: "https://arxiv.org/pdf/2607.19115v1"
tags: ["query:imgfor"]
score: 7.0
evidence: 涉及基于编辑条件的3D场景检索，与图像编辑和定位分析相关。
tldr: "本研究针对编辑条件3D场景检索任务，现有方法如2D图像检索、3D对象编码和场景定位均存在不足。提出CR-Refiner，一个无需训练的重新排序器，通过LLM解析编辑、不平衡最优传输评分、结构先验和LLM验证器来改进基础检索器的候选项。同时发布3D-CER基准数据集，包含4,963个查询。实验显示CR-Refiner在硬子集R@1和mAP@10上一致提升性能。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.19115v1/fig-001.webp\", \"caption\": \"Table 1: Comparison with related benchmarks. Edit: pairs a reference and a modification text to retrieve a target. Multi: ≥ 2 valid GTs per query. Hard: per-query distractor pool (CIRR-style). Zero: adversarial queries with no valid GT, requiring abstention. 3D-CER is the only benchmark that combines all four.\", \"page\": 6, \"index\": 1, \"width\": 714, \"height\": 182}, {\"url\": \"assets/figures/arxiv/2607.19115v1/fig-002.webp\", \"caption\": \"Table 2: Main results on 3D-CER test, hard-subset reranking (n=1,274 with-GT). Top block: trainingfree baselines spanning dense embedding (Caption-MPNet), LLM rewrite (CIReVL), and additive composition (Pic2Word). Bottom block: cumulative components of CR-Refiner. Solver: OT solver variant (add. = additive cost, bal. = balanced Sinkhorn, unb. = unbalanced Sinkhorn). Prior: structural prior with router strategy (bbox = bbox-only on every query, axis = axis-conditional). Ver.: LLM verifier on the top three. Bold marks the full method.\", \"page\": 7, \"index\": 2, \"width\": 618, \"height\": 246}, {\"url\": \"assets/figures/arxiv/2607.19115v1/fig-003.webp\", \"caption\": \"Table 5: Ablation studies (R@1, n=1,274). Top: drop-one component, with the other two retained. Bottom: router variants. Bbox-only forces the bbox branch on every query; oracle uses ground-truth axis labels.\", \"page\": 8, \"index\": 3, \"width\": 706, \"height\": 225}, {\"url\": \"assets/figures/arxiv/2607.19115v1/fig-004.webp\", \"caption\": \"Figure 1: Overview of CR-Refiner. Given a reference 3D room R and a natural-language edit t, a base retriever returns a top-K candidate pool P while a frozen LLM parses (R, t) into a structured query entity q. CR-Refiner rescores each candidate via a Coupled-OT Backbone, an axis-conditional Structural Prior, and an LLM Verifier applied to the top-3 candidates after the backbone and prior; the three signals are combined additively into the final reranking score (Eq. 3).\", \"page\": 4, \"index\": 4, \"width\": 798, \"height\": 415}, {\"url\": \"assets/figures/arxiv/2607.19115v1/fig-005.webp\", \"caption\": \"Figure 5: Drop-one ablation on the 1,274 with-GT test queries, visualising Table 5. Each row removes one component from the full method (A: unbalanced OT solver; B: structural prior; B5: LLM verifier). Left: absolute R@1 per edit axis. Right: ∆R@1 against the full method, where negative values mean the removed component was needed and deeper blue means a larger drop. Each module’s largest negative delta concentrates on its design axis (A on MAT, B on SPA and GEO, B5 on STY and CMP), with limited overlap across components.\", \"page\": 17, \"index\": 5, \"width\": 814, \"height\": 252}, {\"url\": \"assets/figures/arxiv/2607.19115v1/fig-006.webp\", \"caption\": \"Figure 2: Qualitative comparison on 3D-CER hard subsets. Source room (column 1) and top-1 retrievals from CR-Refiner (column 2, green border), Caption-MPNet (column 3, red border), and Pic2Word (column 4, red border). The edit axis is labelled at the left of each row; the edit instruction appears in italics below.\", \"page\": 9, \"index\": 6, \"width\": 789, \"height\": 516}]"
motivation: 现有三种工作在编辑条件3D场景检索上各有缺陷，无法有效处理3D对象组合编辑和检索任务。
method: CR-Refiner使用冻结LLM解析查询为结构化实体，通过不平衡最优传输评分候选，并添加轴条件结构先验和LLM验证器进行重新排序。
result: "跨三个不同的基础检索器，CR-Refiner在硬子集R@1和mAP@10指标上一致改进，覆盖所有编辑轴。"
conclusion: CR-Refiner有效解决了编辑条件3D场景检索问题，并贡献了新的3D-CER基准数据集以推动研究。
---

## 摘要
编辑条件的三维场景检索将参考三维房间与自然语言修改配对，并从语料库中检索满足该编辑的房间。三项先前的工作在这项任务上各有不足。二维组合图像检索基于像素级编辑进行推理，没有用于三维对象集的基本单元。三维基础编码器嵌入单个对象，但无法在场景级别进行组合。三维场景定位方法在静态场景中定位参考，而不是在整个语料库中对修改后的房间进行排序。我们提出 CR-Refiner，一个无需训练的重排序器，用三个组件包装任何基础检索器的 top-K 候选项。一个冻结的大型语言模型将编辑解析为结构化查询实体，每个候选项通过一个不平衡最优传输问题在 1xG 成本矩阵上进行评分，该矩阵耦合类别、风格、材质和几何。不平衡求解器允许单实体查询在不相关对象上减少质量，直接建模不对称性。一个轴条件结构先验为几何编辑添加大小关键词提示，为空间编辑添加主题锚方向提示。一个大型语言模型验证器以连续置信度优化前三个候选项。由于没有基准评估三维对象集上的组合匹配，我们额外发布了 3D-CER，包含 4,963 个编辑条件查询，基于 23,381 个房间的室内语料库，跨五个编辑轴，具有多正例 ground truth、CIRR 风格的硬子集和零目标对抗样本。在三个定性不同的基础检索器上，CR-Refiner 在每个编辑轴上一致提高了硬子集的 R@1 和 mAP@10。

## Abstract
Edit-conditioned 3D scene retrieval pairs a reference 3D room with a natural-language modification and retrieves rooms from a corpus that satisfy the edit. Three lines of prior work each fall short on this task. 2D composed image retrieval reasons over pixel-level edits and has no primitive for 3D object sets. 3D foundation encoders embed individual objects but cannot compose at the scene level. 3D scene-grounding methods localize references inside a static scene rather than rank modified rooms across a corpus. We present CR-Refiner, a training-free reranker that wraps any base retriever's top-K candidates with three components. A frozen LLM parses the edit into a structured query entity, and each candidate is scored by an unbalanced optimal-transport problem over a 1xG cost matrix coupling category, style, material, and geometry. The unbalanced solver lets the single-entity query drop mass on irrelevant objects, modelling the asymmetry directly. An axis-conditional structural prior adds size-keyword cues for geometric edits and subject-anchor direction cues for spatial edits. An LLM verifier refines the top three candidates with continuous confidence. Because no benchmark evaluates compositional matching over 3D object sets, we additionally release 3D-CER, 4,963 edit-conditioned queries over a 23,381-room indoor corpus across five edit axes, with multi-positive ground truth, CIRR-style hard subsets, and zero-target adversarials. Across three qualitatively distinct base retrievers, CR-Refiner consistently improves hard-subset R@1 and mAP@10 on every edit axis.