---
title: "InfoDense: Density-Aware Regional Decisive Replay for Memory-Efficient Incremental Face Forgery Detection"
title_zh: "InfoDense: 面向内存高效增量人脸伪造检测的密度感知区域决策重放策略"
authors: "Jikang Cheng, Hao Shen, Xueyi Zhang, Guangcheng Wang, Zhongyuan Wang, Renye Yan, Baojin Huang"
date: 2026-07-18
pdf: "https://arxiv.org/pdf/2607.16873v1"
tags: ["query:imgfor"]
score: 9.0
evidence: 针对人脸伪造操纵的增量检测。
tldr: 随着人脸伪造技术的快速演进，增量人脸伪造检测通过持续学习新数据来应对威胁，但传统回放方法易引发灾难性遗忘且存储效率低。本文提出 InfoDense，一种密度感知区域决定性回放策略，结合 CLIP 嵌入定位伪造关键区域、基于信息密度选择回放片段，并自适应融合重建训练输入，从而减少遗忘并增强泛化。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.16873v1/fig-001.webp\", \"caption\": \"Figure 6: Quantitative analysis of identity privacy protection. Top-1 and Top-3 recall rates denote the probability of retrieving original identities from replay samples via face recognition (e.g., ArcFace, CosFace). Lower rates imply less identity leakage.\", \"page\": 7, \"index\": 1, \"width\": 1039, \"height\": 234}, {\"url\": \"assets/figures/arxiv/2607.16873v1/fig-002.webp\", \"caption\": \"Table 8: Protocol aligned with prior works, including DevFD, with lower-triangular stage-wise matrix.\", \"page\": 14, \"index\": 2, \"width\": 508, \"height\": 308}, {\"url\": \"assets/figures/arxiv/2607.16873v1/fig-003.webp\", \"caption\": \"Table 6: Upper half: ablation on λ in Eq. (4), where λ weights representativeness and 1 − λ weights decisive-patch density. Lower half: “Frag=2” denotes selecting two different fragments from each image.\", \"page\": 14, \"index\": 3, \"width\": 910, \"height\": 204}, {\"url\": \"assets/figures/arxiv/2607.16873v1/fig-004.webp\", \"caption\": \"Figure 3: The overall pipeline of the proposed InfoDense. We extract informative regional segments via a cut-and-select strategy. These segments are then used in replay learning to mitigate catastrophic forgetting and enhance generalization.\", \"page\": 3, \"index\": 4, \"width\": 1046, \"height\": 506}, {\"url\": \"assets/figures/arxiv/2607.16873v1/fig-005.webp\", \"caption\": \"Figure 8: Sensitivity analysis on the number of decisive patches K.\", \"page\": 13, \"index\": 5, \"width\": 413, \"height\": 316}, {\"url\": \"assets/figures/arxiv/2607.16873v1/fig-006.webp\", \"caption\": \"Figure 7: Performance comparison strictly aligned by the replay buffer size.\", \"page\": 13, \"index\": 6, \"width\": 603, \"height\": 316}, {\"url\": \"assets/figures/arxiv/2607.16873v1/fig-007.webp\", \"caption\": \"Figure 9: Robustness evaluation against test-time common image degradations across five progressive severity levels.\", \"page\": 13, \"index\": 7, \"width\": 1043, \"height\": 218}, {\"url\": \"assets/figures/arxiv/2607.16873v1/fig-008.webp\", \"caption\": \"Table 1: Performance comparison for face forgery detection based on Protocol 1. The two “Avg.” values denote the average detection AUC over the incremental datasets and the cross-dataset evaluation sets, respectively. The best are denoted in bold.\", \"page\": 5, \"index\": 8, \"width\": 1054, \"height\": 284}, {\"url\": \"assets/figures/arxiv/2607.16873v1/fig-009.webp\", \"caption\": \"Table 2: Ablation study on compression strategies. nSeg denotes the average number of decisive patches per replay segment.\", \"page\": 5, \"index\": 9, \"width\": 1052, \"height\": 252}]"
motivation: 传统增量人脸伪造检测中的回放方法易导致灾难性遗忘，且存储完整图像效率低，影响模型学习可迁移伪造特征的能力。
method: 提出 InfoDense 策略，通过定位伪造关键区域、选择高信息密度片段和自适应融合重建来优化回放过程。
result: 实验表明 InfoDense 有效缓解灾难性遗忘并提升跨域泛化能力。
conclusion: InfoDense 成功解决了增量人脸伪造检测中的存储效率和遗忘问题，提高了检测性能。
---

## 摘要
人脸伪造技术的快速发展引入了日益多样化的操纵方式。增量人脸伪造检测（IFFD）通过逐步添加新的伪造数据来微调先前训练的模型，已成为应对不断演变的伪造威胁的一种有前景的方法。然而，传统的基于重放的IFFD方法遭受灾难性遗忘。在有限内存下存储完整的历史图像，往往无法保留微妙的伪造线索或引入领域偏差，从而降低模型学习内在和可迁移操纵特征的能力。在本文中，我们提出了一种密度感知区域决策重放策略，称为InfoDense，以解决这些挑战。InfoDense优先考虑伪影密集和伪造关键区域，在保持高保真伪造证据的同时显著降低存储需求。我们首先介绍InfoDense Cut，使用基于CLIP的嵌入来定位决策补丁。然后，InfoDense Select通过结合潜在空间代表性和决策补丁计数来对候选片段进行排名，确保重放缓冲区的多样性和信息密度。最后，InfoDense Fuse通过自适应地将存储的片段与当前任务样本合并来重构无偏的训练输入，增强知识保留和泛化。在挑战性的增量深度伪造基准上的大量实验表明，InfoDense有效减轻了灾难性遗忘，同时提高了跨域泛化能力。

## Abstract
The rapid evolution of face forgery techniques has introduced an increasing variety of manipulations. Incremental Face Forgery Detection (IFFD), which incrementally adds new forgery data to fine-tune previously trained models, has emerged as a promising approach to handle evolving forgery threats. However, conventional replay-based IFFD methods suffer from catastrophic forgetting. Storing full historical images under limited memory often either fails to preserve subtle forgery cues or introduces domain bias, reducing the model's ability to learn intrinsic and transferable manipulation characteristics. In this paper, we propose a Density-Aware Regional Decisive replay strategy, termed InfoDense, to address these challenges. InfoDense prioritizes artifact-dense and forgery-critical regions, significantly reducing storage requirements while maintaining high-fidelity forgery evidence. We first introduce InfoDense Cut to localize decisive patches using CLIP-based embeddings. Then, InfoDense Select ranks candidate segments by combining latent-space representativeness and decisive patch counts, ensuring both diversity and information density in the replay buffer. Finally, InfoDense Fuse reconstructs unbiased training inputs by adaptively merging stored segments with current-task samples, enhancing knowledge retention and generalization. Extensive experiments on challenging incremental deepfake benchmarks demonstrate that InfoDense effectively mitigates catastrophic forgetting while improving cross-domain generalization.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义

**研究动机与背景：**
*   **核心问题：** 人脸伪造技术快速演进，产生了多样化的操纵手段。为了应对这种动态威胁，**增量人脸伪造检测（IFFD）** 被提出，旨在通过持续学习新的伪造数据来更新模型。然而，现有的基于重放的IFFD方法面临两大挑战：1) **灾难性遗忘**严重；2) 存储完整历史人脸图像导致**内存效率低下**，且可能引入领域偏差，阻碍模型学习内在、可迁移的伪造特征。
*   **关键洞察：** 人脸图像具有强烈的结构规律性（如对称性），且对于伪造检测而言，关键的判别信息（如身份特征、伪造伪影）通常集中在局部区域（如眼睛、嘴巴边界），而大面积区域（如平滑皮肤）信息冗余。
*   **整体含义：** 本文旨在设计一种**高效的区域重放策略**，只存储人脸图像中信息密度高、对伪造判断至关重要的区域，从而在有限的存储预算下，增加可回放样本的多样性，有效缓解遗忘并提升模型的泛化能力。

## 2. 论文提出的方法论：InfoDense

**核心思想：**
提出一个名为 **InfoDense** 的密度感知区域决策重放框架。其核心在于摒弃存储完整人脸图像，转而识别并存储图像中“伪造决策关键且信息密度高”的局部区域，并在学习新任务时，将这些存储的区域与当前数据融合进行训练。

**关键技术细节与流程：**

1.  **InfoDense Cut（关键区域切割）：**
    *   **目标：** 从一张人脸图像中定位出包含最多伪造判别线索的扇形区域。
    *   **方法：** 利用预训练CLIP模型的多模态对齐能力。首先，通过CLIP图像编码器提取图像块（patch）特征。然后，计算每个图像块特征与预设的“伪造”文本嵌入的余弦相似度（**伪造决策分数**），以此衡量该图像块对伪造判断的重要性。保留相似度最高的Top-K个图像块作为“决策性补丁”。
    *   **区域选择：** 采用滑动窗口扫描策略，寻找能覆盖最多“决策性补丁”的扇形区域。这种扇形区域选择贴合人脸对称结构，有助于保留完整的伪造伪影。

2.  **InfoDense Select（信息密度选择）：**
    *   **目标：** 从所有样本的候选区域中，选出信息最丰富、最具代表性的区域存入重放缓冲区。
    *   **方法：** 综合考虑两个指标进行排序：
        *   **潜在空间代表性（`R`）：** 候选区域所在原始图像的特征与当前任务数据分布中心（特征质心）的余弦相似度。代表该样本在整体分布中的典型性。
        *   **决策补丁密度（`D`）：** 候选区域内包含的“决策性补丁”的数量。直接衡量区域的伪造信息密度。
    *   **排序公式：** `Score = λ * norm(R) + (1-λ) * norm(D)`。对所有候选区域按此综合得分降序排序，选择Top-m个存入缓冲区。

3.  **InfoDense Fuse（融合重放）：**
    *   **目标：** 将存储的碎片化区域与当前任务数据融合，重构出可用于训练的完整样本，并避免引入偏差。
    *   **方法：**
        *   **基本融合：** 将存储的扇形区域（`s`）通过二值掩码粘贴到当前任务图像（`x`）的对应位置，生成融合样本。
        *   **多类型融合：** 设计三种组合策略以全面利用信息并防止偏差：
            1.  **任务间融合（`(t-1)&(t)`）：** 将过去任务（t-1）的真实/伪造片段与当前任务（t）的真实/伪造图像进行全组合（4种），强制模型学习完整信息。
            2.  **任务内自融合（`(t)&(t)`）：** 对当前任务数据自身进行随机融合，使模型对融合边界脱敏，防止将融合样本错误地关联到过去任务。
            3.  **多片段融合：** 将同一过去样本的多个片段融合到不同的当前图像上，增加样本多样性。

## 3. 实验设计

*   **数据集与场景：**
    *   **经典数据集：** FaceForensics++ (FF++), DFDCP, Celeb-DF-v2 (CDF), DFD, WildDeepfake (WDF), UADFV。
    *   **新兴伪造数据集：** 使用来自 **DF40** 和 **DiffusionFace** 的基于扩散模型（SDv21, DiT）生成的数据，以评估对最新伪造技术的泛化能力。
    *   **增量协议：**
        *   **协议1：** 训练序列 `{FF++, DFDCP, CDF, SDv21(DiffFace)}`。最终模型在 `FF++, DFDCP, CDF` 上评估**遗忘率**，在 `SDv21(DF40), UADFV, WDF, DFD` 上评估**跨域泛化**能力。
        *   **协议2：** 训练序列 `{DFDCP, SDv21(DiffFace), DiT, CDF}`，模拟更具挑战性的域和架构迁移。
*   **基准与对比方法：**
    *   **基准指标：** 主要采用AUC，并引入**性能下降率（PD）** 量化遗忘。
    *   **对比方法：** 包括多种先进的增量学习与伪造检测方法，如LwF、iCaRL、DER、CoReD、DFIL、HDP、SUR-LID、GPL等。

## 4. 资源与算力

*   **硬件：** 所有实验在单张 **NVIDIA Tesla A100 GPU** 上进行。
*   **软件与训练细节：**
    *   使用PyTorch实现，骨干网络为预训练的 **CLIP ViT-L/14**。
    *   输入图像统一调整为224×224。
    *   使用Adam优化器，初始学习率 `2e-4`，批大小为8。
    *   每个增量任务训练 **5个epoch**。
*   **计算开销：** 论文补充材料指出，InfoDense引入的额外训练开销（用于区域选择和融合）约为 **0.32小时**，占基础训练时间的2.7%，且未增加模型推理时的FLOPs。

## 5. 实验数量与充分性

*   **实验数量与覆盖范围：** 实验设计全面，包括：
    1.  **主实验对比（表1, 表8）：** 在多个协议下与众多SOTA方法对比。
    2.  **压缩策略消融（表2）：** 对比了不同压缩方式（JPEG、CIM）与不同区域形状及选择策略（随机、中心、中心+硬样本、密度感知）。
    3.  **选择与融合策略消融（表3, 表4）：** 单独验证了InfoDense Select和InfoFuse中各组件的有效性。
    4.  **存储效率与Pareto分析（图4, 图7）：** 系统评估了在不同存储预算和压缩率下的性能表现。
    5.  **参数敏感性分析（图8, 表6）：** 分析了决策性补丁数量K和选择权重λ的影响。
    6.  **鲁棒性评估（图9）：** 测试了模型对图像降质的抵抗力。
    7.  **隐私保护分析（图6, 表9）：** 定量评估了存储样本和融合样本的身份泄漏风险。
    8.  **定性可视化（图5, 图10）：** 提供了潜空间和区域选择过程的可视化。
*   **充分性与公平性：** 实验覆盖了**性能、效率、鲁棒性、隐私**等多个维度，对比基线具有代表性，评估协议设计合理。消融实验对方法中的关键设计点进行了充分验证。整体而言，实验设计**全面、客观且公平**。

## 6. 论文的主要结论与发现

*   **主要结论：** InfoDense策略能有效应对增量人脸伪造检测中的灾难性遗忘和存储效率问题。
*   **关键发现：**
    1.  **有效性：** 在多个挑战性基准上，InfoDense在**抗遗忘（PD值最低）** 和**跨域泛化**方面均优于现有SOTA方法。
    2.  **高效性：** 存储仅为原图1/n的扇形区域，就能达到甚至超过存储完整图像的性能，实现了优越的**存储-性能权衡**。
    3.  **优越性：** 相比全局质量压缩（如JPEG），空间区域压缩能在大幅节省存储的同时，**完整保留高频伪造伪影**，这是性能不降反升的关键。
    4.  **隐私性：** 由于存储的是碎片化区域，InfoDense显著**降低了身份信息泄漏的风险**，检索率接近零。
    5.  **必要性：** 实验证明人脸伪造数据存在**内在冗余**，随机提取的局部区域也能保持足够的判别信息，证实了压缩存储的可行性。

## 7. 优点

*   **动机新颖，视角独特：** 从**面部信息冗余**和**伪造关键区域集中性**出发，为IFFD的存储优化提供了新思路。
*   **方法设计系统且精巧：**
    *   **InfoDense Cut** 巧妙利用CLIP的多模态能力定位伪造线索。
    *   **InfoDense Select** 结合了分布代表性和信息密度两个关键维度进行筛选。
    *   **InfoDense Fuse** 的多类型融合策略设计周全，有效避免了偏差并增强了样本多样性。
*   **实验验证全面扎实：** 不仅验证了主方法的有效性，还通过大量消融、分析和可视化，深入理解了方法每个组件的贡献和工作的内在机理。
*   **兼顾多个现实需求：** 方法在提升性能的同时，重点关注了**存储效率**和**隐私保护**，增强了其实用价值。
*   **实现高效：** 附加计算开销小，且未增加模型推理复杂度。

## 8. 不足与局限

*   **依赖预训练CLIP模型：** 方法的区域选择模块依赖于CLIP的特征和对齐能力，这引入了额外的模型依赖和潜在的计算成本。如果伪造伪影与文本描述的“伪造”概念关联不强，可能影响区域选择的准确性。
*   **对伪造区域分布的假设：** 方法假设伪造关键线索集中于局部区域。对于某些旨在全局或频域引入细微痕迹的先进伪造技术，此假设可能不完全成立，可能影响方法的上限。
*   **实验场景的扩展性：** 虽然实验已包含多种数据集，但所有数据均基于人脸。论文未探讨该策略在更一般的图像或视频伪造检测任务中的适用性。
*   **存储与重建的工程开销：** 补充材料中提到的存储策略（存储行级像素值和起始标志）虽有效，但比存储完整图像文件更复杂，可能在实际系统部署时增加一定工程成本。
*   **长期增量学习评估：** 实验中的增量任务数量相对有限（4个任务）。在更长期、更复杂的增量学习场景下，方法的稳定性和性能尚需进一步验证。

（完）
