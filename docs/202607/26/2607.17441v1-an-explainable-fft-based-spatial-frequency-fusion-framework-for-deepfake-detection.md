---
title: An Explainable FFT-Based Spatial-Frequency Fusion Framework for Deepfake Detection
title_zh: 一种用于深伪检测的可解释性基于快速傅里叶变换的空频融合框架
authors: "Pamela Kirui, Cho Hyuk, Qingzhong Liu, Haodi Jiang"
date: 2026-07-19
pdf: "https://arxiv.org/pdf/2607.17441v1"
tags: ["query:imgfor"]
score: 9.0
evidence: 基于FFT的空间-频率融合用于图像级深度伪造检测
tldr: 深度伪造技术对数字媒体真实性构成威胁，本文提出基于快速傅里叶变换（FFT）的空间-频率融合框架MSCA-FFT，结合部分微调的Xception空间分支和FFT频率分支，通过交叉注意力融合特征，并用MLP分类器进行检测。该框架性能优越且提供可解释性，有效识别面部操纵敏感区域。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.17441v1/fig-001.webp\", \"caption\": \"Fig. 3: ROC curves comparing MSCA-FFT with baseline and reference methods on two datasets: (a) Celeb-DF (v2), corresponding to Table I; (b) FaceForensics++, corresponding to Table II.\", \"page\": 7, \"index\": 1, \"width\": 1076, \"height\": 441}, {\"url\": \"assets/figures/arxiv/2607.17441v1/fig-002.webp\", \"caption\": \"Fig. 1: Architecture of the proposed spatial-frequency deepfake face detection framework. The input face image is processed by an Xception-based spatial branch and an FFT-based frequency branch. The spatial branch extracts image-domain texture and structural features, while the frequency branch learns spectral artifacts from the FFT magnitude spectrum. The resulting embeddings are refined by transformer encoders, integrated through a cross-attention fusion module, concatenated, and passed to a classifier for real/fake prediction.\", \"page\": 3, \"index\": 2, \"width\": 1064, \"height\": 679}, {\"url\": \"assets/figures/arxiv/2607.17441v1/fig-003.webp\", \"caption\": \"Fig. 4: Grad-CAM and LIME visual explanations for representative real and fake image pairs. Grad-CAM highlights regions that strongly influence the classifier, while LIME identifies superpixels that support the fake prediction in green and regions that contradict it in red.\", \"page\": 8, \"index\": 3, \"width\": 1076, \"height\": 689}, {\"url\": \"assets/figures/arxiv/2607.17441v1/fig-004.webp\", \"caption\": \"Fig. 2: FFT-based frequency analysis of authentic and forged facial images across low-, mid-, and high-frequency regions. The first two rows show the band-specific frequency responses for real and fake samples, while the third row presents the corresponding difference maps to highlight spectral discrepancies. The final row shows radial energy distributions on a logarithmic scale for each frequency band, illustrating differences in global intensity structure, local texture patterns, and fine-scale details.The reported MSE values correspond to the single real–fake image pair shown.\", \"page\": 4, \"index\": 4, \"width\": 1020, \"height\": 945}]"
motivation: 深度伪造检测需要结合空间和频率特征以提升准确性，现有方法存在融合效率或可解释性不足的问题。
method: 提出MSCA-FFT框架，使用FFT处理频率特征，与空间特征通过transformer编码器和交叉注意力机制进行融合，用于图像级深度伪造检测。
result: 实验表明MSCA-FFT在检测性能上 consistently 优于基于DCT的最新方法和基线模型。
conclusion: 该框架有效融合空间和频率特征，提供互补光谱线索，并通过解释性方法在面部操纵敏感区域给出一致证据。
---

## 摘要
深伪生成技术引发了人们对数字媒体真实性、错误信息、身份欺诈及公众信任的日益担忧。近期研究表明，将空间特征与频率特征结合，比独立使用时产生更强的检测效果。本文提出了MSCA-FFT，一个基于快速傅里叶变换的多尺度交叉注意力框架，用于图像级的深伪检测。该模型将一个部分微调的Xception空间分支与一个基于FFT的频率分支相结合。频率分支通过浅层卷积层处理对数缩放的FFT幅度谱，避免了基于DCT的流程中所使用的从频率域到图像域的逆变换重构。空间和频率表示通过变换编码器进行细化，经交叉注意力融合后，传递至多层感知机分类器以进行真伪预测。实验结果表明，MSCA-FFT的性能持续优于基于DCT的最先进空频融合方法及所对比的基线模型。消融研究进一步表明，基于FFT的频率分支在与空间特征融合时能提供互补的频谱线索。此外，基于FFT的频率分析以及Grad-CAM/LIME解释显示，围绕眼部、口鼻、面部边界等对操作敏感的区域存在一致的证据。

## Abstract
Deepfake generation has raised growing concerns regarding digital media authenticity, misinformation, identity fraud, and public trust. Recent studies show that combining spatial and frequency features leads to stronger detection results than using independently. This paper presents MSCA-FFT, a Fast Fourier Transform (FFT)-based multi-scale cross-attention framework for image-level deepfake detection. The model combines a partially fine-tuned Xception spatial branch with an FFT-based frequency branch. The frequency branch processes the log-scaled FFT magnitude spectrum through shallow convolutional layers, avoiding inverse frequency-to-image reconstruction used in DCT-based pipelines. The spatial and frequency representations are refined by transformer encoders, fused through cross-attention, and passed to an MLP classifier for real/fake prediction. Experimental results show that MSCA-FFT achieves consistently higher performance than the DCT-based state-of-the-art spatial-frequency fusion method and the compared baseline models. The ablation study further indicates that the FFT-based frequency branch provides complementary spectral cues when fused with spatial features. In addition, FFT-based frequency analysis and Grad-CAM/LIME explanations show consistent evidence around manipulation-sensitive facial regions, including the eyes, mouth, nose, and facial boundaries.

---

## 论文详细总结（自动生成）

## 论文总结：一种用于深伪检测的可解释性基于快速傅里叶变换的空频融合框架

### 1. 核心问题与整体含义
*   **研究背景与动机**：深度伪造（Deepfake）技术，特别是由GANs和扩散模型生成的内容，因其高度逼真性而引发了关于数字媒体真实性、错误信息传播和身份欺诈的严重担忧。现有的基于深度学习的检测方法主要分为基于空间（图像像素域）和基于频率（如傅里叶、小波变换）两类。近期研究表明，**融合空间与频率特征**能获得比单独使用更鲁棒的检测性能。然而，现有的多分支融合方法（如基于DCT的方法）在融合前需要将频率信息逆变换重构为类图像表示，增加了处理复杂度。本文旨在设计一个**更简洁、直接且可解释的**空间-频率融合框架。
*   **整体含义**：本文提出了一种名为MSCA-FFT的图像级深伪检测框架，核心创新在于**直接使用快速傅里叶变换（FFT）幅度谱作为频率特征**，避免了逆变换步骤，并通过多尺度交叉注意力机制与空间特征有效融合，从而在保证高性能的同时提升了模型的可解释性。

### 2. 提出的方法论
*   **核心思想**：构建一个双分支架构。**空间分支**负责提取图像中局部的纹理、边界等视觉伪造痕迹。**频率分支**则在频域直接分析可能更明显的频谱异常。两个分支的输出通过**交叉注意力机制**进行深度融合，使彼此的特征能够相互增强和引导。
*   **关键技术细节**：
    1.  **空间分支**：采用预训练的Xception模型，冻结80%的底层参数，微调顶层20%以适应检测任务，输出空间特征图。
    2.  **FFT频率分支**：输入图像经二维FFT变换后，取其**对数缩放后的幅度谱**，直接输入一个由浅层卷积层构成的网络进行特征提取。关键点是**没有**进行逆FFT变换。
    3.  **特征建模与融合**：空间和频率特征图分别经过独立的Transformer编码器以建模长距离依赖。随后，进入**交叉注意力融合模块**，使空间特征能融入频谱信息，频率特征也能获得空间语境的指引。融合后的特征被拼接，并送入一个MLP分类器进行真伪二分类。
    4.  **损失函数与优化**：使用带标签平滑的交叉熵损失，采用AdamW优化器，并结合了梯度裁剪、学习率调度和早停等策略。

### 3. 实验设计
*   **数据集**：
    *   **Celeb-DF (v2)**：作为主要评估基准，包含高质量、自然的伪造视频，已预处理为256x256的人脸图像。
    *   **FaceForensics++**：作为补充基准，包含四种不同的伪造方法（Deepfakes， Face2Face等），用于评估模型的泛化性。
*   **基准与对比方法**：
    *   **核心对比**：与同类别的、近期发表的**基于DCT的空间频率融合方法 (MSCA-DCT)** 进行直接对比。
    *   **基线模型**：与广泛使用的单一空间域CNN模型进行对比，包括Xception、ResNet50、EfficientNetB4。
*   **评估协议**：采用**五折交叉验证**，报告五个评估指标的平均值：准确率、精确率、召回率、F1分数和AUC（曲线下面积）。

### 4. 资源与算力
论文中**未明确提及**实验所使用的GPU型号、数量以及详细的训练时长等信息。只提到了训练时的批量大小（32）、最大轮次（50）和早停耐心值（10）。

### 5. 实验数量与充分性
*   **实验数量**：实验较为充分，主要包括：
    1.  **主实验**：在两个数据集（Celeb-DF， FaceForensics++）上与多个对比方法的性能比较。
    2.  **消融实验**：在Celeb-DF数据集上，分别测试了**完整模型**、仅使用**空间分支**、仅使用**频率分支**的性能，验证了各组件的贡献和融合的必要性。
    3.  **可解释性分析**：使用Grad-CAM和LIME方法，对模型预测进行视觉解释。
*   **充分性与公平性**：实验设计较为客观和公平。所有对比方法使用相同的数据预处理流程、五折交叉验证协议和训练设置。消融实验清晰地分离了空间和频率分支的作用。在**两个不同特性的数据集**上进行评估增强了结论的可靠性。

### 6. 主要结论与发现
1.  **性能优越**：MSCA-FFT在两个基准数据集上的所有评估指标均**持续优于**基于DCT的MSCA-DCT方法和所有基线CNN模型，在Celeb-DF上达到了接近99.98%的AUC。
2.  **融合价值**：消融实验证明，**空间分支是性能的主力**（单独使用AUC达99.91%），而**频率分支单独性能较弱**（AUC 67.78%）。但将两者融合后，性能得到**进一步提升**，表明频率特征提供了有价值的互补信息。
3.  **可解释性支持**：基于FFT的频率分析显示，真实与伪造图像在眼部、嘴部等区域的**频谱存在明显差异**。Grad-CAM和LIME解释表明，模型做决策时**重点关注**眼部、鼻子、嘴巴和面部边界等对伪造操作敏感的区域，这与法医学认知一致，增强了模型的可信度。

### 7. 优点
*   **方法创新性**：提出**直接使用FFT幅度谱**作为频率特征，避免了DCT方法中逆变换重构的步骤，简化了处理流程，同时证明了其有效性。
*   **框架设计**：结合了强大的预训练骨干（Xception）、Transformer建模和交叉注意力融合，形成了一个性能强大的端到端框架。
*   **实验全面**：实验不仅包含性能对比，还包括**消融研究**和**深入的可解释性分析**，使结论更有说服力。
*   **可解释性**：强调了模型的决策依据与已知的法医伪造痕迹（如面部边界、五官区域）相一致，提升了模型在实际应用中的可信度。

### 8. 不足与局限
*   **应用范围**：实验聚焦于**图像级**的真伪分类，未涉及视频序列的时序一致性检测或生成方法溯源等更复杂的任务。
*   **频率分支的独立性弱**：消融实验表明，频率分支单独工作的性能远低于空间分支，说明当前设计的频率特征提取网络可能不够强大，其有效性高度依赖于与空间特征的融合。
*   **可解释性方法的局限**：文中使用的Grad-CAM和LIME是通用的解释方法，其解释结果（如高亮区域）与“真正伪造痕迹”的关联性仍有待更严谨的法医学验证。
*   **未讨论计算成本**：论文未提供模型参数量、推理速度或训练耗时等信息，无法评估该框架与基线模型在计算效率上的比较。
*   **数据与场景局限性**：虽然使用了两个数据集，但伪造类型和图像条件仍有限。模型对**极低质量、高度压缩**或**从未见过的、更先进的伪造技术**的鲁棒性未得到测试。

（完）
