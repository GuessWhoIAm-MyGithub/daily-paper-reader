---
title: Cross-Domain Generalization Limits of Vision Foundation Models in Facial Deepfake Detection
title_zh: 视觉基础模型在面部深度伪造检测中的跨域泛化限制
authors: Ibrahim Delibasoglu
date: 2026-05-24
pdf: "https://arxiv.org/pdf/2605.24965v1"
tags: ["query:imgfor"]
score: 9.0
evidence: 涉及面部深度伪造检测及对未见操纵的泛化能力。
tldr: 本研究针对面部深度伪造检测中检测器泛化能力不足的问题，探讨视觉基础模型作为通用特征提取器的潜力。通过系统性跨域评估三种基础学习范式（全监督、自监督、多教师），使用冻结骨干和线性探测在DF40基准上测试性能。结果揭示了预训练范式与参数规模的权衡，基础模型在全脸合成检测中表现优异，但局部编辑技术暴露了线性探测的局限性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.24965v1/fig-001.webp\", \"caption\": \"Table 2: Performance comparison on the test set. ViT LP, DINOv3 LP and RADIOv4 LP represent downstream linear probes optimized on fully trained frozen foundation features across custom trainig set.\", \"page\": 4, \"index\": 1, \"width\": 823, \"height\": 244}, {\"url\": \"assets/figures/arxiv/2605.24965v1/fig-002.webp\", \"caption\": \"Table 4: Performance metrics for DF40: MidJourney and WhichFaceIsReal datasets (Both Entire Face Synthesis Deepfakes).\", \"page\": 6, \"index\": 2, \"width\": 875, \"height\": 323}, {\"url\": \"assets/figures/arxiv/2605.24965v1/fig-003.webp\", \"caption\": \"Table 3: Performance Metrics for DF40: CollabDiff and StyleCLIP Datasets (Both Face Editing Deepfakes).\", \"page\": 6, \"index\": 3, \"width\": 875, \"height\": 322}, {\"url\": \"assets/figures/arxiv/2605.24965v1/fig-004.webp\", \"caption\": \"Fig. 1: Input preprocessing pipeline: isolating the facial Region of Interest (ROI) from the absolute image domain to remove background confounders.\", \"page\": 3, \"index\": 4, \"width\": 454, \"height\": 265}]"
motivation: 传统深度伪造检测器过拟合特定生成器，泛化能力差，需探索更通用的特征提取方法。
method: 采用跨域评估框架，比较三种视觉基础模型范式，并通过线性探测评估其在未见技术上的性能。
result: 基础模型在全脸合成深度伪造检测中保持高判别能力，但对局部人脸编辑技术的泛化受限。
conclusion: 视觉基础模型为深度伪造检测提供了有潜力的特征提取方式，但线性探测结构在应对局部编辑时存在根本边界。
---

## 摘要
生成模型的快速演进使得超逼真的面部深度伪造成为可能，暴露了现代数字取证中的一个关键漏洞：检测器无法泛化到未见过的操纵技术。传统网络遭受表示崩溃，过度拟合到特定训练生成器的局部伪影指纹。本研究探讨了现代视觉基础模型是否可以作为可泛化的开箱即用的特征提取器，能够追踪跨完全未见的生成流形的取证异常。我们进行了系统性的跨域评估，比较了三种基础学习范式：完全监督的宏语义特征（RoPE-ViT）、纯自监督的几何特征（DINOv3）和多教师聚合表示（NVIDIA C-RADIOv4-H）。通过部署冻结的骨干网络并进行下游线性探测，我们在具有挑战性的DF40基准上映射了这些架构的性能限制。我们的实证研究揭示了预训练范式和参数规模之间的内在权衡，证明了尽管基础模型对整个面部合成保持高判别能力，但局部面部编辑技术暴露了线性探测评估结构中的基本界限。源代码和模型权重可在http://github.com/mribrahim/deepfake获取。

## Abstract
The rapid evolution of generative models has enabled the creation of hyper-realistic facial deepfakes, exposing a critical vulnerability in modern digital forensics: the inability of detectors to generalize to unseen manipulation techniques. Traditional networks suffer from representation collapse, overfitting to localized artifact fingerprints of specific training generators. This work investigates whether modern Vision Foundation Models can serve as generalizable, out-of-the-box feature extractors capable of tracking forensic anomalies across entirely unseen generative manifolds. We conduct a systematic cross-domain evaluation comparing three foundational learning paradigms: fully supervised macro-semantic features (RoPE-ViT), pure self-supervised geometric features (DINOv3), and multi-teacher agglomerative representations (NVIDIA C-RADIOv4-H). By deploying frozen backbones subjected to downstream linear probing, we map the performance limitations of these architectures on the challenging DF40 benchmark. Our empirical findings expose the intrinsic trade-offs between pre-training paradigms and parameter scale, proving that while foundation models retain high discriminative capabilities for entire face synthesis, localized face editing techniques expose fundamental boundaries in linear probe evaluation structures. Source code and model weights are available in http://github.com/mribrahim/deepfake

---

## 论文详细总结（自动生成）

# 论文总结：视觉基础模型在面部深度伪造检测中的跨域泛化限制

## 1. 核心问题与整体含义
*   **研究动机与背景**：随着生成对抗网络（GANs）和扩散模型等生成技术的发展，超逼真的面部深度伪造（Deepfake）对数字信任和安全构成严重威胁。传统检测器通常针对特定生成器（如特定GAN）的伪造痕迹进行学习，导致其在面对**未见过的、新的生成模型**时性能急剧下降，泛化能力差。这被称为“泛化鸿沟”，是当前深度伪造检测领域的核心挑战。
*   **研究问题**：本文旨在探究一个根本性问题：现代化的**视觉基础模型**（Vision Foundation Models, VFMs）能否作为一个通用、开箱即用的特征提取器，有效捕捉跨不同生成技术的篡改痕迹，从而实现更好的跨域泛化？

## 2. 方法论
*   **核心思想**：不微调整个大型基础模型，而是**冻结其参数**，仅将其作为特征提取器，然后在其上训练一个轻量级的**线性探测层**（Linear Probe）进行分类。这种做法旨在**纯粹评估基础模型预训练表征的质量**，而非其适应新任务的能力。
*   **关键技术细节与模型范式**：
    1.  **监督宏语义范式 (RoPE-ViT)**：在ImageNet-1k上通过监督分类预训练，学习全局对象语义特征。
    2.  **自监督几何范式 (DINOv3)**：通过自监督（如掩码图像建模）在大量自然图像上预训练，学习局部空间关系和结构连续性。
    3.  **多教师聚合范式 (NVIDIA C-RADIOv4-H)**：通过知识蒸馏，同时从多个教师模型（包括DINOv3的几何特征、SigLIP2的语言语义特征、SAM3的边缘分割特征）中学习，旨在获得更全面、鲁棒的表征。
*   **算法流程**：
    1.  **预处理**：从输入图像中裁剪出**面部感兴趣区域**，移除背景干扰。
    2.  **特征提取**：将裁剪后的面部图像输入冻结的基础模型骨干网络，提取高维特征向量。
    3.  **线性探测**：将提取的特征向量输入一个单层线性分类器（权重矩阵W和偏置b），经过Sigmoid激活函数输出二分类（真/伪）概率。
    4.  **优化**：使用二元交叉熵损失函数优化该线性探测层。

## 3. 实验设计
*   **数据集与场景**：使用 **DF40 基准**，这是一个专门用于评估跨域泛化能力的深度伪造检测基准。
    *   **训练集**：包含约21，000张真实人脸图像（来自CelebA-HQ, FFHQ, LaPa）和20，000张伪造图像（分为**全脸合成**和**局部人脸编辑**两类）。
    *   **测试集（评估泛化）**：
        *   **全脸合成**：MidJourney， WhichFaceIsReal数据集。
        *   **局部人脸编辑**：CollabDiff， StyleCLIP数据集。
    *   **实验设计逻辑**：分别评估模型在“分布内”测试集和关键的“跨域”测试集（以上四个OoD数据集）上的性能。
*   **对比方法**：
    1.  **本文提出的三种VFM线性探测**（ViT LP， DINOv3 LP， RADIOv4 LP）。
    2.  **传统CNN基线**：EfficientNet-B0， EfficientNet-B2， InceptionResNet。
    3.  **专门设计的检测模型基线**：FreqNet（频域感知）， BNN（二进制神经网络）。

## 4. 资源与算力
*   论文**没有明确说明**训练和实验所使用的硬件资源（如GPU型号、数量）和训练时长。文中仅提供了各基础模型的参数规模（如DINOv3 ~303M， C-RADIOv4-H ~653M），但未报告算力消耗细节。

## 5. 实验数量与充分性
*   **实验数量**：论文进行了多组系统实验，包括：
    *   三种基础模型范式的性能对比。
    *   与五种以上基线方法的对比。
    *   在**分布内测试集**和**四个关键的跨域测试集**（分别代表两种不同伪造类型）上的评估。
    *   针对两种伪造类型（全脸合成 vs. 局部编辑）的结果分别进行分析和讨论。
*   **充分性与公平性评估**：
    *   **充分性**：实验设计较为全面，涵盖了不同的预训练范式、不同的伪造类型和多个基准数据集，足以支撑其关于泛化限制的核心论点。
    *   **公平性**：所有模型都采用相同的预处理流程（面部ROI提取）和相同的评估框架（冻结特征+线性探测），确保了比较的公平性。与基线方法的对比也很合理。
    *   **潜在不足**：评估指标主要为准确率、精确率、召回率和F1值。未探讨更细粒度的分析（如混淆矩阵、错误样本分析）。此外，主要评估了“线性探测”这种简单设置，未探索更复杂的适配方法（如轻量微调）是否能改善其在局部编辑任务上的表现。

## 6. 主要结论与发现
1.  **任务依赖性**：视觉基础模型在深度伪造检测上的泛化能力**严重依赖于伪造任务的类型**。
    *   **优势**：对于**全脸合成**类伪造（如MidJourney），基础模型（特别是监督范式的ViT和自监督范式的DINOv3）展现出强大的判别能力，性能优异。
    *   **劣势**：对于**局部人脸编辑**类伪造（如StyleCLIP），所有模型（包括基础模型）的性能都**严重下降**，线性探测甚至失效，接近随机猜测。
2.  **模型范式比较**：多教师聚合范式（C-RADIOv4-H）在面对极端的域偏移（如低分辨率、局部编辑）时表现出更强的**稳健性**，尽管其在干净数据上可能不如特定范式突出。
3.  **根本局限**：线性探测这种评估方式存在固有瓶颈。它依赖于全局平均池化，**丢失了细粒度的空间和局部纹理信息**，而这些信息对于检测局部编辑痕迹至关重要。

## 7. 优点
1.  **研究问题重要**：直击深度伪造检测中泛化能力不足的核心痛点。
2.  **方法设计清晰**：通过“冻结骨干+线性探测”的设置，巧妙地隔离并评估了不同预训练范式所学表征的**内在判别力**，而非模型的可调性。
3.  **实验设置全面且有针对性**：使用专门的跨域基准DF40，并区分了两种关键的伪造类型（全脸合成与局部编辑）进行分别评估，使得结论更具说服力和解释力。
4.  **模型选择有代表性**：选取的三种基础模型范式覆盖了当前主流的预训练思路，对比分析具有参考价值。

## 8. 不足与局限
1.  **评估结构单一**：核心结论基于线性探测，这本身是一种非常受限的评估方式。论文未能探索更强大的适配器（Adapter）或提示微调（Prompt Tuning）等方法是否能突破线性探测的瓶颈，更好地利用基础模型的特征。
2.  **对低分辨率敏感**：实验暴露了基础模型对低分辨率输入非常敏感，在此类数据上性能崩溃。这限制了其在现实世界中处理网络低质视频的应用。
3.  **特征聚合方式粗糙**：使用全局平均池化进行特征聚合，不可避免地丢失空间信息，而后者对检测局部篡改至关重要。论文在“局限与未来工作”部分也承认了这一点。
4.  **应用范围限制**：研究聚焦于面部区域，对于其他类型的深度伪造（如人体、场景）的泛化能力尚未可知。
5.  **缺少算力分析**：未报告实验的计算成本，使得评估该方法的实际应用可行性（如能否在资源受限环境部署）变得困难。

（完）
