---
title: "EDGER: EDge-Guided with HEatmap Refinement for Generalizable Image Forgery Localization"
title_zh: EDGER：基于边缘引导与热图优化的通用图像伪造定位
authors: "Minh-Khoa Le-Phan, Minh-Hoang Le, Minh-Triet Tran, Trong-Le Do"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.12002v1"
tags: ["query:img-f"]
score: 10.0
evidence: 提出利用边缘引导实现可泛化图像伪造定位的框架。
tldr: 针对文本引导图像修复导致伪造更真实，现有方法跨域定位困难的问题，本文提出EDGER框架。它采用双分支设计：Edge-Guided Segmentation使用频率边缘检测和SegFormer融合特征以强调操作边界，Synthetic Heatmapping利用CLIP-ViT和LoRA标记完全合成补丁。该方法能处理任意分辨率图像，在MediaEval 2025评估中展现出强大的跨域泛化能力和高分辨率适应性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.12002v1/fig-001.webp\", \"caption\": \"Fig. 4. Each group of five images shows, from left to right, the EGS mask, the SH mask, the fuse mask, the ground-truth mask, and the original image. In (a)–(c), showing large images, SH supplies strong region cues that complement EGS. In (d), showing a small image, EGS captures fine details and performs well even without SH. The fused output combines these complementary strengths.\", \"page\": 11, \"index\": 1, \"width\": 652, \"height\": 552}, {\"url\": \"assets/figures/arxiv/2605.12002v1/fig-002.webp\", \"caption\": \"Fig. 1. Overall localization pipeline. A patch-based dual-branch design: (i) the EGS branch injects a frequency-based edge prior (Section 3.1) into a SegFormer decoder; non-overlapping patches, padded to sizes divisible by 32, are stitched to full resolution. (ii) The SH branch applies a CLIP-ViT classifier over sliding windows with Hann blending to form a dense heatmap. The two outputs are fused by pixel-wise maximum, Pfuse = max(PEGS,PSH), and thresholded to yield the final mask.\", \"page\": 4, \"index\": 2, \"width\": 725, \"height\": 385}, {\"url\": \"assets/figures/arxiv/2605.12002v1/fig-003.webp\", \"caption\": \"Fig. 5. Each triplet shows, from left to right, the SH mask, the ground-truth mask, and the original image. In (a)–(b), showing large images, SH captures coarse regions but misses fine boundaries and small structures. In (c)-(d), showing small images, SH produces blurry responses and overlooks many manipulated pixels. This shows that SH alone is insufficient without complementary edge guidance and fusion.\", \"page\": 12, \"index\": 3, \"width\": 725, \"height\": 256}, {\"url\": \"assets/figures/arxiv/2605.12002v1/fig-004.webp\", \"caption\": \"Fig. 2. Pipeline of the Frequency-based Edge Detector. The network extracts highpass and SRM-like residuals, computes fixed Sobel and Laplacian gradients from RGB, and fuses them through a multi-branch convolutional head composed of residual and dilated context blocks with global pooling. The head integrates frequency, gradient, and structural cues to predict soft edge probability maps supervised by multi-scale targets.\", \"page\": 5, \"index\": 4, \"width\": 725, \"height\": 295}]"
motivation: 现有图像伪造定位方法在跨域场景下难以有效识别可疑信号，需要更通用的解决方案。
method: 提出EDGER，一个基于补丁的双分支框架，结合边缘引导分割和合成热图精炼来定位操作区域。
result: 在MediaEval 2025 SynthIM挑战中，该方法成功扩展到多兆像素图像并表现出强跨域泛化能力。
conclusion: 频率边缘线索和补丁级合成先验的互补作用是驱动准确、分辨率无关定位的关键。
---

## 摘要
文本引导修复技术使图像伪造日益逼真，这对源图像检测（SID）和图像伪造定位（IFL）提出了挑战。然而，现有方法往往难以跨域识别可疑信号。为解决此问题，我们提出EDGER——一种基于图像块、双分支的框架，能够在不牺牲原始分辨率的情况下，对任意分辨率图像中的篡改区域进行定位。第一个分支“边缘引导分割”引入基于频率的边缘检测器，以突出篡改边界处的高频不一致性，并微调SegFormer模型融合RGB与边缘特征，从而生成像素级掩码。由于边缘信息仅在图像块同时包含真实与篡改像素时才最具参考价值，我们通过“合成热图分支”对其进行补充——这是一个基于分类的定位器，利用LoRA微调CLIP-ViT图像编码器，以标记完全合成的图像块。合成热图分支提供粗粒度的图像块级合成先验，而边缘引导分割则在部分篡改的图像块中锐化边界，从而实现全面定位。在MediaEval 2025挑战赛的“合成图像挑战”篡改区域定位任务设定下评估表明，该方法可扩展至数百万像素图像，并展现出强大的跨域泛化能力。大量消融实验证明，基于频率的边缘线索与图像块级合成先验在驱动准确、分辨率无关的定位中起到了互补作用。

## Abstract
Text-guided inpainting has made image forgery increasingly realistic, challenging both SID and IFL. However, existing methods often struggle to point out suspicious signals across domains. To address this problem, we propose EDGER, a patch-based, dual-branch framework that localizes manipulated regions in arbitrary resolution images without sacrificing native resolution. The first branch, Edge-Guided Segmentation, introduces a Frequency-based Edge Detector to emphasize high-frequency inconsistencies at manipulation boundaries, and fine-tunes a SegFormer to fuse RGB and edge features for pixel-level masks. Since edge evidence is most informative only when patches contain both authentic and manipulated pixels, we complement Edge-Guided Segmentation with a Synthetic Heatmapping branch, a classification-based localizer that fine-tunes a CLIP-ViT image encoder with LoRA to flag fully synthetic patches. Together, Synthetic Heatmapping provides coarse, patch-level synthetic priors, while Edge-Guided Segmentation sharpens boundaries within partially manipulated patches, yielding comprehensive localization. Evaluated in the MediaEval 2025, SynthIM challenge, Manipulated Region Localization Task's setting, our approach scales to multi-megapixel imagery and exhibits strong cross-domain generalization. Extensive ablations highlight the complementary roles of frequency-based edge cues and patch-level synthetic priors in driving accurate, resolution-agnostic localization.

---

## 论文详细总结（自动生成）

### 论文总结：EDGER - 基于边缘引导与热图优化的通用图像伪造定位

#### 1. 核心问题与整体含义
- **研究背景**：文本引导修复等生成模型使图像伪造日益逼真，对图像源检测与图像伪造定位构成挑战。
- **核心问题**：现有的图像伪造定位方法严重依赖特定域的训练数据，在面对**不同生成模型、数据集和分辨率**时，跨域泛化能力较弱，且难以在处理高分辨率图像时平衡精度与效率。
- **整体含义**：本文旨在提出一个**通用性强、分辨率自适应**的伪造定位框架，以应对生成技术快速演变和现实图像复杂多样的挑战。

#### 2. 提出的方法论
- **核心思想**：提出一个基于图像块、双分支的框架（EDGER），通过融合**边缘边界线索**和**区域合成先验**，实现对篡改区域的全面定位。
- **关键技术细节**：
    1.  **边缘引导分割分支**：
        - **频率边缘检测器**：利用高通滤波和SRM滤波提取高频残差，并与固定的Sobel和Laplacian梯度结合，强调操作边界。
        - **集成方式**：将RGB图像与边缘概率图通过“空间堆叠”方式垂直拼接，输入**SegFormer**解码器进行像素级分割。
        - **训练策略**：边缘检测器冻结，仅训练分割部分；训练时对边缘图进行增强以提高鲁棒性。
    2.  **合成热图分支**：
        - **分类器训练**：使用**LoRA微调CLIP-ViT**图像编码器，将图像分为“完全再生”和“非完全再生”两类。
        - **密集热图生成**：在推理时，使用滑动窗口（带汉宁窗融合）的方式，将分类器应用于图像重叠块，生成像素级的合成概率热图。
    3.  **融合策略**：对两个分支输出的概率图进行**逐像素取最大值**，再进行阈值分割得到最终掩码。

#### 3. 实验设计
- **数据集与场景**：
    - **任务设定**：遵循 **MediaEval 2025 SynthIM挑战赛** 的“操作区域定位”任务。
    - **关注目标**：仅评估“**拼接**”类图像，因为其篡改区域明确。
    - **训练数据**：使用 **TGIF** 数据集（约7.5万样本）。
    - **验证数据**：使用 **SAGI-D** 验证集，包含由6种不同修复模型生成的图像，分辨率范围极广（从~10万像素到超过1亿像素），专门测试跨域和跨分辨率能力。
- **评估指标**：主要使用**交并比**。
- **对比方法**：与 **IML-ViT、MVSSNet、CATNet** 等现有代表方法进行公平比较。
- **消融实验**：系统性地评估了补丁化处理、边缘引导、热图分支以及不同边缘集成策略的作用。

#### 4. 资源与算力
- **硬件**：在单张 **NVIDIA A100 GPU** 上进行训练。
- **未明确说明的部分**：论文未提及具体的**训练总时长**。

#### 5. 实验数量与充分性
- **实验组数**：进行了多组实验，包括：
    - 1组主要对比实验（表1）。
    - 1组核心组件消融实验（表2）。
    - 1组边缘集成策略消融实验（表3）。
    - 详细的定性分析（图4，图5）。
- **充分性与公平性**：实验设计**较为充分和客观**。对比实验在统一框架（IMDLBenco）下进行；消融实验控制变量，清晰展示了各组件的贡献；验证集设计具有挑战性，能有效评估泛化性。

#### 6. 主要结论与发现
- **主要结论**：EDGER框架能有效扩展到多兆像素图像，并展现出强大的跨域泛化能力。
- **关键发现**：
    1.  基于**补丁化**的处理策略是保障高分辨率图像分析精度的关键。
    2.  **频率边缘线索**和**补丁级合成先验**具有高度互补性，二者融合显著提升了定位精度。
    3.  空间堆叠是集成几何边缘先验最有效的策略。

#### 7. 优点
- **方法创新**：
    - **双分支互补设计**：巧妙结合了边界精度（EGS）和区域完整性（SH）。
    - **频率边缘检测器**：专注于低级信号，避免了语义信息干扰。
    - **从分类到定位的转换**：利用训练好的全局分类器生成局部热图，思路新颖高效。
- **工程设计**：
    - **分辨率自适应**：统一的补丁化处理和融合策略，无需改变模型输入分辨率。
    - **实际可行性**：边缘检测器训练后冻结，SH分支复用分类器，计算高效。

#### 8. 不足与局限
- **实验局限**：
    - 验证集虽多样，但仍局限于**特定挑战赛**，未在更广泛的公开基准上测试。
    - 融合策略（像素级最大值）较为简单，可能存在过度依赖某一分支的风险。
- **方法局限**：
    - **计算复杂度**：滑动窗口生成热图的方式对于极高分辨率图像可能仍有一定计算开销。
    - **任务定义**：对于“完全再生”图像，其定义的“操作区域”本身存在模糊性，本文未深入探讨。
    - **单张GPU训练**：可能限制了模型规模或实验规模的进一步扩大。

（完）
