---
title: From Image Hashing to Scene Change Detection
title_zh: 从图像哈希到场景变化检测
authors: "Anh-Kiet Duong, Marie-Claire Iatrides, Petra Gomez-Krämer, Jean-Michel Carozza"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.12259v1"
tags: ["query:img-f"]
score: 9.0
evidence: 基于补丁哈希的局部变化检测与识别
tldr: 本研究针对图像哈希技术只能进行全局比较而无法定位场景变化的局限，提出HashSCD框架，通过补丁级哈希和对比学习，实现高效的全局变化检测与局部化识别，显著降低计算和存储成本。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.12259v1/fig-001.webp\", \"caption\": \"Table 3. Retrieval performance (mAP) comparison on standard image retrieval benchmarks.\", \"page\": 11, \"index\": 1, \"width\": 658, \"height\": 258}, {\"url\": \"assets/figures/arxiv/2605.12259v1/fig-002.webp\", \"caption\": \"Fig. 1. Overview of the proposed patch-wise hashing formulation for scene change detection, which enables efficient global comparison and localized change identification.\", \"page\": 2, \"index\": 2, \"width\": 648, \"height\": 525}, {\"url\": \"assets/figures/arxiv/2605.12259v1/fig-003.webp\", \"caption\": \"Fig. 3. Qualitative examples of scene change detection results.\", \"page\": 12, \"index\": 3, \"width\": 658, \"height\": 603}, {\"url\": \"assets/figures/arxiv/2605.12259v1/fig-004.webp\", \"caption\": \"Fig. 2. Overview of the proposed HashSCD framework for scene change detection. Given two images captured at times T0 and T1, augmented views are processed by a shared backbone to extract patch-wise features, which are mapped to local hash embeddings. An XOR-like aggregation is applied to the local hashes to obtain a compact global hash representation for retrieval. During training, contrastive loss is imposed at both patch and global levels. During inference, global hashes are compared using the Hamming distance to determine whether a change occurs, while patch-wise hash differences are used to generate a change heatmap via upsampling.\", \"page\": 5, \"index\": 4, \"width\": 725, \"height\": 392}]"
motivation: 图像哈希无法直接应用于场景变化检测，因为空间定位至关重要。
method: 提出HashSCD，一个基于补丁的哈希框架，使用对比学习训练，通过XOR-like操作在汉明空间进行变化检测和定位。
result: HashSCD在性能上与最先进的无监督哈希和场景变化检测方法竞争，同时大幅减少计算和存储开销。
conclusion: HashSCD成功将哈希技术扩展到场景变化检测领域，提供高效且局部化的解决方案。
---

## 摘要
图像哈希提供紧凑表示以实现高效存储和检索，但其固有地局限于全局比较，无法推理变化发生的位置。这种限制阻碍了哈希直接应用于场景变化检测，其中空间定位至关重要。在这项工作中，我们从场景变化检测的角度重新审视哈希，并提出HashSCD，一种分块哈希框架，能够同时实现高效的全局变化检测和局部化变化识别。HashSCD将空间对齐的分块编码为紧凑的哈希码，并通过类似XOR的操作进行聚合，允许在汉明空间中直接进行变化检测和定位，而无需对先前图像进行重复推理。该模型以无监督方式训练，使用分块和全局级别的对比学习。实验表明，与最先进的无监督哈希和场景变化检测方法相比，HashSCD实现了有竞争力的性能，同时显著降低了计算成本和存储需求。

## Abstract
Image hashing provides compact representations for efficient storage and retrieval but is inherently limited to global comparison and cannot reason about where changes occur. This limitation prevents hashing from being directly applicable to scene change detection, where spatial localization is essential. In this work, we revisit hashing from a scene change detection perspective and propose HashSCD, a patch-wise hashing framework that enables both efficient global change detection and localized change identification. HashSCD encodes spatially aligned patches into compact hash codes and aggregates them through an XOR-like operation, allowing change detection and localization to be performed directly in the Hamming space without repeated inference on previous images. The model is trained in an unsupervised manner using contrastive learning at both patch and global levels. Experiments demonstrate that HashSCD achieves competitive performance compared to state-of-the-art unsupervised hashing and scene change detection methods, while significantly reducing computational cost and storage requirements.

---

## 论文详细总结（自动生成）

# 论文总结：《From Image Hashing to Scene Change Detection》

## 1. 核心问题与研究背景
- **研究动机**：传统的图像哈希技术将图像编码为紧凑的二进制码，适用于高效的图像检索。然而，其本质局限在于仅能进行**全局图像比较**，无法推理或定位图像中发生变化的具体**空间位置**。
- **核心问题**：场景变化检测任务不仅需要判断“是否发生变化”，更关键的是要“定位变化发生的区域”。现有的连续特征方法虽然有效，但面临**存储开销大、计算成本高**的问题，难以应用于长期、大规模的监控场景。
- **整体含义**：本文旨在弥合图像哈希的**高效性**与场景变化检测对**空间定位能力**的需求之间的鸿沟，探索如何将哈希技术扩展到场景变化检测领域，实现兼顾效率与精度的解决方案。

## 2. 提出的方法论：HashSCD 框架
- **核心思想**：提出一个**分块哈希**框架，将空间对齐的图像块编码为哈希码，并通过类似XOR的聚合操作，在**汉明空间**中直接实现高效的变化检测与定位。
- **关键技术细节**：
    1.  **分块哈希嵌入**：骨干网络提取特征图后，每个空间位置的特征向量通过一个可学习的投影层映射为长度为 `l` 的哈希码 `hi`。
    2.  **XOR-like 聚合**：递归地对所有分块哈希码进行绝对差运算，聚合生成一个全局哈希码 `R`。在二进制域，此操作等价于级联的XOR运算，保证了紧凑性并编码了局部模式的联合配置。
    3.  **软哈希与对比学习优化**：
        - 训练时使用 `tanh` 函数产生连续的软哈希码，以支持梯度优化。
        - 采用**非对称对比损失**，在**分块级别**和**全局级别**同时进行监督。正样本对为同一场景不同时间的图像增强视图，负样本对来自批次内其他样本。
    4.  **基于哈希的变化检测**：
        - **全局检测**：比较两个图像的全局哈希码的汉明距离，判断是否发生变化。
        - **局部定位**：一旦检测到变化，比较对应位置的分块哈希码差异，并通过上采样生成变化热力图。
        - **长期监控**：存储每个观测的全局哈希和分块哈希，后续推理无需重新处理旧图像。

## 3. 实验设计
- **数据集与场景**：
    - **场景变化检测**：`VL-CMU-CD`（城市场景，含噪声）和 `PCD`（包含TSUNAMI灾区子集和Google街景子集，存在视角变化）。
    - **图像检索**：`Oxford Flowers`（细粒度小规模）、`Food101`（细粒度大规模）和 `NUS-WIDE`（广泛类别大规模）。
- **评估指标**：
    - 变化检测：`F1` 分数（阈值0.5）。
    - 检索：`mAP`。
- **对比方法**：
    - **变化检测**：包括监督方法 (`C-3PO`)、零样本方法 (`ZSSCD`)、基于无监督连续特征的方法 (`SSCD`, `D-SSCD`)。
    - **图像检索**：多种前沿的无监督哈希方法 (`UGH`, `ML S3RUDH`, `Bihalf`, `CIBHash`, `SPQ`, `MeCoQ`, `SDC`, `A2-SSL`)。

## 4. 资源与算力
- **硬件**：所有实验在一块 **NVIDIA H100 GPU** 上进行。
- **软件**：基于 **PyTorch** 实现。
- **训练细节**：使用 `Adam` 优化器，训练 `100` 个 epochs。
- **未明确说明项**：论文**未提及**训练的具体耗时、使用的GPU数量以及是否进行多卡训练。

## 5. 实验数量与充分性
- **实验规模**：进行了多组实验，包括：
    - 在**两个**场景变化检测数据集上的性能对比。
    - 在**三个**图像检索数据集上的性能对比。
    - **详细的消融研究**：探究了全局损失、温度参数、不同骨干网络（VGG16, ResNet50, DINOv2 ViT）的影响。
    - **定性分析**：展示了变化热力图的可视化结果。
    - **效率分析**：对比了存储和计算开销，并提供了CPU/GPU推理速度。
- **充分性与公平性**：实验设置较为充分和客观。对比方法覆盖了不同技术路线。在检索任务中，与多种SOTA方法对比；在变化检测中，与监督、无监督及零样本方法对比。使用了标准的评估协议和指标。

## 6. 主要结论与发现
- HashSCD 是**首个**将哈希技术应用于场景变化检测的框架，能同时支持全局比较和局部定位。
- 在变化检测任务上，其性能**与基于连续特征的先进无监督方法具有竞争力**，且**大幅超越**了零样本方法。
- 在细粒度图像检索任务上，其性能**超越了现有的无监督哈希方法**。
- 相较于存储连续特征，HashSCD 能实现高达 **256 倍**的存储和计算成本节约，非常适合长期、大规模监控。

## 7. 优点
- **创新性**：首次提出并实现了分块哈希用于场景变化检测的范式，桥接了哈希的效率和变化检测的空间定位需求。
- **高效性**：核心贡献在于极低的存储（二进制码）和计算开销（汉明距离），使得长期监控成为可能。
- **端到端训练**：通过设计软哈希和双级对比损失，实现了模型的无监督端到端训练。
- **实验全面**：在变化检测和检索两个任务上均进行了系统评估，并进行了消融分析。

## 8. 不足与局限
- **复杂场景处理能力有限**：论文在结论中指出，对于存在**大幅度视角变化、相机运动或未完美对齐**的复杂场景，当前方法性能有限，仍是该领域的挑战。
- **性能差距**：在部分基准（如 `VL-CMU-CD`）上，其变化检测的F1分数与顶级的监督方法或基于连续特征的无监督方法相比，仍存在一定差距，这是以效率换精度的固有折衷。
- **应用限制**：方法假设图像在空间上是对齐的，对于未配准或非刚性形变严重的场景可能不适用。
- **骨干网络依赖**：实验表明性能对骨干网络敏感（如 `DINOv2` 效果更好），这意味着模型的表现力受限于预训练模型的特征提取能力。

（完）
