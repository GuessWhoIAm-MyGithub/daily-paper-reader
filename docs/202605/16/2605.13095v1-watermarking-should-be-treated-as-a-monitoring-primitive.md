---
title: Watermarking Should Be Treated as a Monitoring Primitive
title_zh: 应将水印视为监控原语
authors: "Toluwani Aremu, Nils Lukas, Jie Zhang"
date: 2026-05-13
pdf: "https://arxiv.org/pdf/2605.13095v1"
tags: ["query:imgfor"]
score: 9.0
evidence: 用于检测和归属图像篡改的水印技术
tldr: 论文针对水印在生成模型中的应用，指出当前评估局限于单样本对抗性攻击，主张将水印视为监控原语。作者引入基于观察者的威胁模型，通过聚合水印信号来推断实体级信息。研究发现零比特水印在多键设置下能实现归因，且外部监控可能从键相关统计结构中涌现，揭示了归因和监控之间的双重用途张力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.13095v1/fig-001.webp\", \"caption\": \"Figure 3: Internal attribution performance under zero-bit multi-key watermarking. We report the top-1 attribution accuracy (TPR@1%FPR) as the number of entities increases across watermarking methods.\", \"page\": 6, \"index\": 1, \"width\": 817, \"height\": 606}, {\"url\": \"assets/figures/arxiv/2605.13095v1/fig-002.webp\", \"caption\": \"Figure 1: Comparison of watermarking usage under different observer models. Left: Standard watermarking, where a detector determines whether an output is watermarked and optionally decodes an embedded message. Middle: Internal observer, who has access to watermark keys and performs attribution by identifying which entity generated an output. Right: External observer, who does not have access to keys and instead learns to identify which entity generated an output from publicly observed data by exploiting watermark-induced statistical patterns. This illustrates a shift from per-sample detection to entity-level inference, showing that watermarking can act as a monitoring primitive by enabling user attribution and re-identification over time.\", \"page\": 2, \"index\": 2, \"width\": 820, \"height\": 297}, {\"url\": \"assets/figures/arxiv/2605.13095v1/fig-003.webp\", \"caption\": \"Figure 2: Example scenarios in which watermarking can enable monitoring. The first two arise naturally for internal observers with detector access, while the latter two illustrate how monitoring may also extend to external observers or institutional surveillance settings.\", \"page\": 4, \"index\": 3, \"width\": 829, \"height\": 500}]"
motivation: 水印应被视为监控原语，需评估聚合和观察者能力，超越单样本鲁棒性。
method: 引入基于观察者的威胁模型，聚合水印信号推断实体级信息。
result: 零比特水印在多键设置下能实现归因；外部监控可能从持久统计结构中涌现。
conclusion: 水印在归因和监控之间存在双重用途张力，需更全面评估。
---

## 摘要
水印在生成模型中被广泛提议用于溯源、归属和安全监控，但通常仅在对试图逃避检测或在单个样本层面诱导假阳性的敌方进行评估。我们认为应将水印视为监控原语，并且给定每个实体的归属密钥和消息以及检测器访问，内部监控是不可避免的。我们引入了一种基于观察者的威胁模型，其中观察者可以跨输出聚合水印信号以推断实体级信息，表明即使零比特水印在多密钥设置下也能实现归属。我们进一步表明，外部监控可随时间从持久的、依赖密钥的统计结构中出现，尽管这取决于水印设计，并可通过保持分布或不可检测的方案缓解。我们的研究揭示了归属与监控之间的根本性双重用途紧张关系，促使在评估水印时超越单个样本的鲁棒性，以考虑聚合和基于观察者的能力。

## Abstract
Watermarking is widely proposed for provenance, attribution, and safety monitoring in generative models, yet is typically evaluated only under adversaries who attempt to evade detection or induce false positives at the level of individual samples. We argue that watermarking should be treated as a monitoring primitive, and that internal monitoring is unavoidable given per-entity attribution keys and messages, as well as detector access. We introduce an observer-based threat model in which observers can aggregate watermark signals across outputs to infer entity-level information, showing that even zero-bit watermarking enables attribution under multi-key settings. We further show that external monitoring can emerge over time from persistent, key-dependent statistical structure, although this depends on watermark design and may be mitigated by distribution-preserving or undetectable schemes. Our findings reveal a fundamental dual-use tension between attribution and monitoring, motivating evaluation of watermarking beyond per-sample robustness to account for aggregation and observer-based capabilities.

---

## 论文详细总结（自动生成）

### 论文总结：《应将水印视为监控原语》

#### 1. 核心问题与整体含义
*   **研究动机**：当前对生成模型水印技术的研究与评估，主要局限于其**单样本鲁棒性**，即对抗旨在移除或伪造水印的个体攻击者。然而，论文指出，这种视角忽视了水印在**长期、跨样本部署**中可能带来的更深远的监控能力。
*   **背景与问题**：随着AI生成内容法规（如欧盟AI法案）的推进，水印被日益视为实现溯源、归因和安全监控的关键技术。论文认为，水印系统的实际部署（特别是为不同用户分配不同密钥时）本身就在创造一种**持久的监控原语**。即使是最基础的零比特水印（仅标识是否被标记），在多个输出上聚合时，也能用于推断用户身份、追踪活动模式，而这一关键能力在现有威胁模型中未被系统评估。
*   **整体含义**：水印技术存在一个根本性的**双重用途张力**：其用于合法归因和安全监控的特性，同样可用于未经授权的追踪和用户画像。因此，评估和治理水印系统时，必须超越单样本鲁棒性，充分考虑其作为监控工具的能力和潜在风险。

#### 2. 方法论
*   **核心思想**：提出一种**基于观察者的威胁模型**，将分析焦点从“攻击单个样本”转向“被动聚合跨样本信号”。观察者不修改输出，而是利用水印信号的持久性进行实体级推断。
*   **关键技术细节**：
    1.  **观察者类型**：
        *   **内部观察者**：拥有水印密钥和检测器访问权限。在多密钥部署下，可通过计算输出与所有密钥检测器分数的匹配度，直接归因到特定实体。
        *   **外部观察者**：无密钥访问权限。依赖可从公开输出中提取的统计特征，训练分类器（如BERT、CLIP）来识别生成实体。
    2.  **形式化分析**：
        *   论文将实体重识别问题形式化为判断两个输出是否来自同一实体。
        *   对于内部观察者，归因函数为：`e(x) = argmax_{e ∈ E} D_{k_e}(x)`，即选择检测器响应分数最高的实体。
        *   对于外部观察者，归因函数为：`e(x) = f(φ(x))`，其中 `f` 是基于特征 `φ(x)` 训练的分类器。
    3.  **关键洞见**：即使零比特水印不显式编码身份信息，在多密钥部署下，每个密钥都会在生成过程中引入**持久的、依赖密钥的统计结构**。这种结构内部可通过检测器直接利用，外部则可能通过机器学习被提取和利用。

#### 3. 实验设计
*   **数据集与场景**：
    *   **文本**：使用 **C4** 语料库。采用**提示匹配设置**，所有实体使用相同的提示生成内容，以隔离水印本身的影响。
    *   **图像**：使用 **Stable Diffusion Prompt** 数据集，同样采用提示匹配设置。
    *   **场景**：模拟多用户（实体）使用同一生成模型，每个用户被分配一个唯一的水印密钥。
*   **基准与对比方法**：
    *   **基准**：没有传统的对比方法。实验旨在验证水印的监控能力，因此对比基线主要是**随机猜测**和**控制实验**（无水印、共享密钥）。
    *   **评估的水印方法**：
        *   **文本**：KGW, Unigram, EWD, MorphMark, EXP, IE, SWEET, SEMSTAMP 等（源自MarkLLM工具包）。
        *   **图像**：SynthID, TreeRing, Wind, Gaussian Shading 等（源自MarkDiffusion工具包）。

#### 4. 资源与算力
*   论文**未明确提供**关于GPU型号、数量、训练时长等详细算力信息。
*   可以推断的是，实验涉及对多个大型语言模型（Qwen2.5-14B）和扩散模型（Stable Diffusion v2.1）进行水印嵌入，以及训练外部观察者分类器（BERT-Base用于文本，CLIP-RN50用于图像），这些任务都需要相当的计算资源。

#### 5. 实验数量与充分性
*   **实验数量**：论文进行了大量实验，包括：
    1.  **内部归因实验**：评估了8+种水印方法在不同实体数量（4, 8, 12, 16）下的归因准确率。
    2.  **外部识别实验**：针对文本和图像各一种代表性方法（KGW, Tree-Ring），评估在实体数量（2, 4, 8, 16）和每个实体观察样本量（100-4000）变化下的识别准确率。
    3.  **控制实验**：设立了无水印和共享密钥两个控制组，与多密钥部署（内部/外部观察者）进行对比。
*   **充分性与公平性**：
    *   **充分性**：实验覆盖了文本和图像两种模态，多种水印方法，变化的实体规模和样本量，以及关键的控制实验，论证较为全面。
    *   **公平性**：采用**提示匹配设置**消除了内容差异带来的混淆因素，确保归因和识别能力确实源于水印机制本身，实验设计公平。

#### 6. 主要结论与发现
1.  **内部监控是固有的**：在多密钥部署下，即使使用零比特水印，拥有检测器访问权限的内部观察者也能以很高的准确率将输出归因到特定实体。
2.  **外部监控是涌现的**：无密钥访问的外部观察者，能够通过学习公开输出中的统计模式，在足够多的样本上实现高于随机的实体识别。这表明监控能力可以从水印的持久性设计中“涌现”出来。
3.  **控制实验验证**：无水印和共享密钥的设置均未能展现出显著的监控能力，证实了上述监控能力直接源于**基于不同密钥的水印部署**。
4.  **双重用途张力**：水印用于归因和监控的特性是其设计目的的延伸，但同时也带来了被用于大规模用户监控的风险。因此，水印评估必须包含监控维度。

#### 7. 优点
*   **视角新颖**：将水印研究从传统的“单样本攻防”提升到“系统级监控”的视角，提出了一个具有现实意义的威胁模型。
*   **分析深入**：不仅提出观点，还通过形式化定义和系统的实验（内部/外部观察者、控制实验）进行了严谨的验证。
*   **实验设计严谨**：提示匹配设置有效隔离变量；同时评估多种水印方法和模态，增强了结论的普遍性；控制实验是关键亮点，有力证明了监控能力来源。
*   **具有现实关怀**：论文讨论了该发现对监管、隐私和治理的影响，将技术研究与伦理、社会影响紧密结合。

#### 8. 不足与局限
*   **实验覆盖范围**：
    *   外部观察者的验证仅基于**各模态的一种水印方法**（KGW, Tree-Ring）。需要更多实验来验证该现象在其他水印方案（尤其是分布保持型水印）中的普遍性。
    *   未系统评估**后处理**（如改写、摘要）对外部监控能力的影响。
*   **模型与规模**：实验在特定的生成模型上进行，未探讨模型规模、类型对监控能力的影响。外部识别实验在最多16个实体下进行，大规模场景（如数百万用户）的可行性有待研究。
*   **潜在偏差**：外部观察者依赖于特定分类器（BERT, CLIP）来提取特征，不同特征提取方法可能影响结果，但论文未对此进行消融。
*   **缓解措施讨论**：虽然提及“分布保持型水印”可能缓解外部监控，但仅进行了初步实验，未深入探讨其设计、实现难点及其对内部监控的无效性。

（完）
