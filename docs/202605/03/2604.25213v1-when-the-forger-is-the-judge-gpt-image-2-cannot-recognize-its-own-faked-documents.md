---
title: "When the Forger Is the Judge: GPT-Image-2 Cannot Recognize Its Own Faked Documents"
title_zh: 当伪造者成为裁判：GPT-Image-2无法识别其自身伪造的文档
authors: "Jiaqi Wu, Yuchen Zhou, Dennis Tsang Ng, Xingyu Shen, Kidus Zewde, Ankit Raj, Tommy Duong, Simiao Ren"
date: 2026-04-28
pdf: "https://arxiv.org/pdf/2604.25213v1"
tags: ["query:imgfor"]
score: 9.0
evidence: 基准测试AI伪造文档的取证检测
tldr: 本研究探讨了OpenAI的GPT-Image-2模型在文档图像伪造中的能力，发现其伪造的文档难以被人类和现有计算方法检测。通过发布AIForge-Doc v2数据集和基准测试四种防御方法，研究显示人类准确率接近随机猜测，计算方法准确率略高但仍有局限，突显了检测AI生成伪造的挑战。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.25213v1/fig-001.webp\", \"caption\": \"Figure 2: (a) ROC curves on the full v2 test set. All three judges remain close to the chance diagonal: TruFor extracts a small but statistically significant signal (AUC 0.599); DocTamper recovers a comparable signal (AUC 0.585); the GPT-Image-2 self-judge is statistically indistinguishable from random. (b) Per-source AUC. TruFor’s signal is concentrated in the WildReceipt mobile-photo subset (AUC 0.791); DocTamper’s signal concentrates in SROIE (0.710) and WildReceipt (0.654); both detectors collapse on PDF-rasterised forms (XFUND).\", \"page\": 6, \"index\": 1, \"width\": 959, \"height\": 362}, {\"url\": \"assets/figures/arxiv/2604.25213v1/fig-002.webp\", \"caption\": \"Figure 3: Judge biases on AIForge-Doc v2. (a) Self-judge confusion matrix: model says REAL on 84.7% of its own forgeries vs. 91.0% of authentics; the 6.3-pp gap is the entire signal. (b) DocTamper image-level score: forged and authentic distributions overlap heavily (aggregate AUC 0.585; per-source in Table 5). (c) TruFor’s separation is driven almost entirely by WildReceipt (Figure 2(b)).\", \"page\": 8, \"index\": 2, \"width\": 962, \"height\": 272}]"
motivation: GPT-Image-2能轻易伪造文档图像，但现有检测方法效果不佳。
method: 发布AIForge-Doc v2数据集，并基准测试人类检查员、TruFor、DocTamper和GPT-Image-2自判四种防御方法。
result: "人类检测准确率约为50%，计算方法准确率略高但AUC低于0.6，表明GPT-Image-2伪造难以识别。"
conclusion: GPT-Image-2伪造文档检测存在显著差距，需要开发新的检测技术。
---

## 摘要
OpenAI的GPT-Image-2已有效消除了真实文档图像与AI编辑文档图像之间的视觉边界：收据上的单个数字可在不到一秒内以几分钱的成本被替换。我们发布了AIForge-Doc v2，这是一个包含3,066份GPT-Image-2文档伪造的配对数据集，其中包含与DocTamper兼容格式的像素级精确掩码，并对四道防线进行了基准测试：人工检查员（N=120，通过公开的2AFC网站CanUSpotAI.com获得n=365对投票）、TruFor（通用取证）、DocTamper（qcf-568，文档专用），以及同一个GPT-Image-2模型作为零样本自我裁判——为避免简单的“图像大部分是真实”的解读，被询问图像中是否有任何区域是由AI图像模型生成或编辑的。人工2AFC准确率为0.501，与随机猜测无异：即使并排对比，检查员也无法区分GPT-Image-2伪造的收据与真实收据。三个计算裁判的表现仅略高于此（TruFor 0.599，DocTamper 0.585，自我裁判0.532）。自我裁判持续失败，并非偶然：在五种提示策略和四种处理模糊响应的策略下，AUC从未超过0.59。为排除两个取证检测器在我们的源域上失效而非对AI修复视而不见的可能性，我们在为其训练分布构建的同域传统篡改集上对每个检测器进行了校准：TruFor在我们数据集的跨相机拼接上达到AUC 0.962，DocTamper在两次JPEG重编码的跨文档OCR令牌拼接上达到0.852。两者在传统篡改上均保持接近已发表的性能；切换到GPT-Image-2修复后，AUC下降0.27-0.36（TruFor从0.962降至0.599；DocTamper从0.852降至0.585），这揭示了GPT-Image-2修复特有的检测差距。我们发布了数据集、流程、四裁判协议和校准集。

## Abstract
OpenAI's GPT-Image-2 has effectively erased the visual boundary between authentic and AI-edited document images: a single number on a receipt can be replaced in under a second for a few cents. We release AIForge-Doc v2, a paired dataset of 3,066 GPT-Image-2 document forgeries with pixel-precise masks in DocTamper-compatible format, and benchmark four lines of defence: human inspectors (N=120, n=365 pair-votes via the public 2AFC site CanUSpotAI.com), TruFor (generic forensic), DocTamper (qcf-568, document-specific), and the same GPT-Image-2 model as a zero-shot self-judge -- asked, to avoid the trivial "image is mostly real" reading, whether any region was generated or edited by an AI image model. Human 2AFC accuracy is 0.501, indistinguishable from chance: even side-by-side, inspectors cannot tell GPT-Image-2 receipt forgeries from authentic counterparts. The three computational judges sit only modestly above (TruFor 0.599, DocTamper 0.585, self-judge 0.532). The self-judge fails consistently, not by chance: across five prompt strategies and four policies for handling ambiguous responses, AUC never rises above 0.59. To rule out the possibility that the two forensic detectors are broken on our source domain rather than blind to AI inpainting, we calibrate each on a same-domain traditional-tampering set built for its training distribution: TruFor reaches AUC 0.962 on cross-camera splicing of our dataset, DocTamper reaches 0.852 on cross-document OCR-token splicing with two-pass JPEG re-encoding. Both retain near-published performance on traditional tampering; switching to GPT-Image-2 inpainting drops AUC by 0.27-0.36 (0.962->0.599 TruFor; 0.852->0.585 DocTamper), isolating a detection gap specific to GPT-Image-2 inpainting. We release the dataset, pipeline, four-judge protocol, and calibration sets.

---

## 论文详细总结（自动生成）

### 论文核心总结

#### 1. 核心问题与整体含义
- **研究动机**：文档欺诈已成为工业级问题，数字篡改（尤其是AI生成内容）激增。OpenAI于2026年发布的GPT-Image-2模型能以极低成本（几美分）和极快速度（不到一秒）精准替换文档图像中的单个数字（如收据金额），且生成的伪造区域与原始图像在统计上高度一致，消除了视觉边界。
- **核心问题**：当前人类和计算方法能否有效检测GPT-Image-2生成的文档伪造？模型自身能否识别其伪造输出？
- **整体含义**：研究揭示了GPT-Image-2伪造文档的“自不可区分性”——无论是人类检查员、通用/专用取证检测器，还是模型自身，其检测性能均接近随机猜测，暴露了现有防御体系在面对先进AI修复技术时的巨大漏洞。

#### 2. 方法论
- **核心思想**：构建一个标准化的评估框架，通过配对数据集和四种代表性防御策略，系统性地测量GPT-Image-2伪造文档的检测难度。
- **关键技术细节**：
    1.  **数据集构建 (AIForge-Doc v2)**：
        - 复用v1的4,062个伪造规格（源图像、目标字段、伪造值、边界框），仅将生成器替换为GPT-Image-2。
        - 生成流程包含**宽高比保持尺寸调整**、**绿色轮廓标记掩码**、**两层提示工程**（固定外层+五子句内层以确保字符保真度）以及**元数据剥离**。
        - 最终生成3,066对伪造-真实文档图像，涵盖4个源数据集、9种语言、4类字段。
    2.  **四裁判评估协议**：
        - **人类裁判**：通过公开网站CanUSpotAI.com进行2AFC（二选一强制选择）测试。
        - **通用取证检测器 (TruFor)**：利用相机传感器噪声不一致性进行检测。
        - **文档专用检测器 (DocTamper)**：利用JPEG量化历史和排版特征进行检测。
        - **模型自检 (GPT-Image-2 Self-Judge)**：以零样本方式，使用最小化提示（“Is this image AI-edited?”）询问生成模型自身。
    3.  **校准实验**：为区分检测器性能低下是源于AI修复特性还是域迁移问题，为TruFor和DocTamper分别构建了符合其训练分布的**同域传统篡改校准集**（跨相机拼接、跨文档OCR令牌拼接）。

#### 3. 实验设计
- **数据集/场景**：使用AIForge-Doc v2（3,066对）作为主测试集，涵盖收据（CORD, WildReceipt, SROIE）和表格（XFUND）场景。
- **基准**：将四种裁判的检测性能与随机猜测（AUC=0.5）进行对比。
- **对比方法**：
    1.  人类检查员（N=120，n=365对投票）。
    2.  TruFor（通用取证，零样本）。
    3.  DocTamper（文档专用，零样本，qcf-568权重）。
    4.  GPT-Image-2自检（零样本，最小化提示）。
    5.  （校准实验）TruFor和DocTamper在其各自的传统篡改校准集上的表现。

#### 4. 资源与算力
- 论文**未明确说明**训练或推理所用的具体GPU型号、数量和时长。
- 计算资源主要用于：调用OpenAI API生成伪造图像、运行TruFor和DocTamper模型进行推理、以及通过API查询GPT-Image-2进行自检。数据集生成和模型推理是主要的计算开销。

#### 5. 实验数量与充分性
- **实验数量**：
    - **主实验**：在完整v2测试集上评估4种裁判。
    - **校准实验**：为2个取证检测器各构建一个校准集并进行评估。
    - **消融实验**：对自检裁判测试了5种不同的提示策略。
    - **分析实验**：按数据源（4个）分析各裁判性能；分析裁判间分数相关性；对自检的模糊响应进行4种填充策略的敏感性分析。
- **充分性与公平性**：
    - **充分**：实验覆盖了多种防御策略、多个数据子集、不同提示变体和校准场景，结果具有统计置信区间。
    - **客观公平**：所有计算裁判均采用零样本设置，未针对v2数据进行微调。数据集构建复用v1规格，确保了跨生成器比较的公平性。校准实验设计严谨，有效隔离了AI修复特有的检测差距。

#### 6. 主要结论与发现
1.  **人类无法检测**：人类2AFC准确率为0.501，与随机猜测无异。
2.  **计算检测器效果有限**：TruFor（AUC 0.599）、DocTamper（AUC 0.585）和自检（AUC 0.532）的性能均远低于实际可用水平（AUC < 0.6）。
3.  **模型无法自检**：GPT-Image-2将自身84.7%的伪造输出判定为“真实”，仅比真实文档的91.0%低6.3个百分点，信号微弱。多种提示策略均无法将AUC提升至0.59以上。
4.  **检测差距源于AI修复**：校准实验表明，TruFor和DocTamper在传统篡改上能达到接近发表的性能（AUC 0.962和0.852），但在GPT-Image-2修复上AUC大幅下降0.27-0.36，证明了该差距是AI修复特有的。
5.  **检测器失效机制**：GPT-Image-2修复消除了传统篡改留下的局部不一致性（如传感器噪声断层、JPEG历史不匹配），且生成的像素在统计上与周围区域平滑一致。

#### 7. 优点
- **系统性评估框架**：提出了包含配对数据集、多裁判协议和校准实验的完整评估体系，可复现且可扩展。
- **高质量数据集**：AIForge-Doc v2提供了像素级精确掩码和跨生成器配对，是研究AI文档伪造的宝贵资源。
- **严谨的实验设计**：通过校准实验有力排除了域迁移的干扰，精准定位了AI修复带来的检测挑战。
- **深入的机理分析**：不仅报告了性能数字，还深入分析了检测器在PRNU和DCT通道上失效的具体原因。
- **对实践的启示**：明确指出了当前安全过滤器的局限性和行业协调的必要性。

#### 8. 不足与局限
- **生成器单一性**：研究主要聚焦于GPT-Image-2，结论是否适用于其他同期或未来的图像生成模型有待验证。
- **数据集场景有限**：主要针对收据和表格，对其他类型文档（如身份证、合同、发票）的覆盖不足。
- **检测方法未专门化**：评估的检测器均为通用或基于传统篡改训练的模型，未包含专门为检测AI生成/修复内容而设计的模型。
- **自检提示工程有限**：虽然测试了多种提示，但探索空间仍然很大，可能存在更有效的提示策略未被发现。
- **安全过滤器绕过**：论文指出OpenAI的安全过滤器可通过简单的提示或边界框扰动绕过，但这未在实验中系统测试。
- **应用限制**：研究结论基于特定API访问和图像格式（PNG），实际部署环境可能引入其他变量。

（完）
