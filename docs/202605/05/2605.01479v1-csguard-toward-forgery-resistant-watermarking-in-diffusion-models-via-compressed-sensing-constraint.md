---
title: "CSGuard: Toward Forgery-Resistant Watermarking in Diffusion Models via Compressed Sensing Constraint"
title_zh: CSGuard：通过压缩感知约束实现扩散模型中的防伪水印
authors: "Jiewei Lai, Lan Zhang, Chen Tang, Pengcheng Sun, Zhaopeng Zhang, Yunhao Wang, Hui Jin"
date: 2026-05-02
pdf: "https://arxiv.org/pdf/2605.01479v1"
tags: ["query:imgfor"]
score: 9.0
evidence: 抗伪造水印用于图像数字取证
tldr: 研究背景：基于潜空间的扩散模型水印用于内容归属和知识产权保护，但易受伪造攻击，攻击者可通过反转和重新生成图像实现错误归属。方法：提出CSGuard，首次利用压缩感知约束将水印嵌入和验证绑定到秘密矩阵，确保只有授权用户能操作。主要贡献：实现强抗伪造水印，显著降低攻击成功率，同时保持生成质量和水印完整性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.01479v1/fig-001.webp\", \"caption\": \"Table 1: Comparison of DMsMark methods. We report bit accuracy (BitAcc), true positive rate (TPR), and CLIP score on benign images, and BitAcc, attack success rate (ASR) on forged images. The subscript reports the absolute drop from performance on benign images.\", \"page\": 7, \"index\": 1, \"width\": 744, \"height\": 356}, {\"url\": \"assets/figures/arxiv/2605.01479v1/fig-002.webp\", \"caption\": \"Figure 7: TPR/ASR for benign and forged images under varying matries.\", \"page\": 22, \"index\": 2, \"width\": 817, \"height\": 241}, {\"url\": \"assets/figures/arxiv/2605.01479v1/fig-003.webp\", \"caption\": \"Figure 1: Overview of CSGuard. By enforcing CS consistency on intermediate latents, CSGuard binds generation–inversion symmetry to a secret matrix, thereby preventing adversaries from exploiting this symmetry to forge, rendering it robust against forgery attacks.\", \"page\": 4, \"index\": 3, \"width\": 740, \"height\": 335}, {\"url\": \"assets/figures/arxiv/2605.01479v1/fig-004.webp\", \"caption\": \"Figure 6: TPR/ASR for benign and forged images under varying false positive rate (FPR) thresholds.\", \"page\": 17, \"index\": 4, \"width\": 609, \"height\": 344}, {\"url\": \"assets/figures/arxiv/2605.01479v1/fig-005.webp\", \"caption\": \"Figure 5: Comparative visualization of watermarking results. For each prompt (bottom), the left image shows the watermarked output generated by the baseline method GaussShading (without CS constraints), while the right image presents the result produced by CSGuard.\", \"page\": 16, \"index\": 5, \"width\": 810, \"height\": 324}]"
motivation: 现有扩散模型水印方法存在伪造攻击漏洞，攻击者可提取水印并重新生成图像以实现错误归属。
method: CSGuard利用压缩感知将水印生成和验证绑定到秘密矩阵，防止非法用户伪造。
result: "实验表明CSGuard将攻击成功率从100%降至28.12%，并实现100%的良性水印检测率。"
conclusion: CSGuard是首个抗伪造水印方案，有效防止非法伪造，同时不损害水印效果。
---

## 摘要
基于潜空间的扩散模型水印将水印嵌入生成图像的潜空间，以实现内容归属，为知识产权保护和数字取证提供了一种无需训练的解决方案。然而，这些方法存在一个关键漏洞，即易受伪造攻击。攻击者可以通过反转带水印的图像并使用任意提示重新生成它来提取水印，从而在恶意内容上实现虚假归属。在本文中，我们提出了CSGuard，这是首个利用压缩感知将带水印图像的生成和验证绑定到秘密矩阵的防伪水印方案。这确保了只有拥有秘密矩阵的用户才能正确嵌入或验证图像水印，防止非法用户进行伪造，同时不影响生成质量和水印完整性。实验结果表明，CSGuard实现了强大的防伪能力，将攻击成功率从100.0%降低到28.12%，并在良性带水印图像上实现了100%的检测率，同时不影响水印效果。

## Abstract
Latent-based diffusion model watermarking embeds watermarks into generated images' latent space to enable content attribution, offering a training-free solution for intellectual property protection and digital forensics. However, these methods exhibit a critical vulnerability to the forgery attack, attackers can extract the watermark by inverting the watermarked image and re-generating it with an arbitrary prompt, thereby enabling false attribution on malicious content. In this paper, we propose the CSGuard, the first forgery-resistant watermarking schema that leverages compressed sensing to bind the watermarked image generation and verification to a secret matrix. This ensures that only users possessing the secret matrix can correctly embed or verify the image watermark, prevents the illegal users from forgery without compromising generation quality and watermark integrity. Experimental results demonstrate that CSGuard achieves strong forgery resistance, reduces the attack success rate from 100.0\% to 28.12\%, and achieve 100\% detection rate on benign watermarked images without compromising watermarking effectiveness.

---

## 论文详细总结（自动生成）

## 论文总结：CSGuard

### 1. 核心问题与整体含义
*   **研究动机与背景**：基于潜空间的扩散模型水印是保护AI生成内容知识产权和进行数字取证的重要手段。然而，现有方法存在一个根本性安全漏洞——**伪造攻击**。攻击者可以利用扩散模型的“生成-反转对称性”，通过反转带水印的图像恢复其初始潜变量，再结合恶意提示生成新图像。该新图像虽内容改变，但仍会携带原用户的水印，导致恶意内容被错误归属给合法用户，严重威胁水印系统的可靠性。
*   **整体含义**：本文旨在解决这一核心安全挑战，提出首个能够抵抗伪造攻击的扩散模型水印方案，在保证水印有效性和图像质量的前提下，从根本上阻断攻击者利用对称性进行伪造的路径。

### 2. 方法论
*   **核心思想**：利用**压缩感知**理论，将扩散模型的“生成-反转对称性”与一个**秘密矩阵**绑定。只有持有该秘密矩阵的授权用户才能正确执行水印的嵌入与验证，从而将公开的系统漏洞转化为私有的安全能力。
*   **关键技术细节**：
    1.  **压缩感知约束**：在图像生成过程中，强制中间潜变量 `z0|t` 满足约束 `A * z0|t ≡ y`，其中 `A` 是秘密矩阵，`y` 是观测值。此约束将生成轨迹与 `A` 绑定。
    2.  **最小扰动一致性投影**：设计投影算子 `z'0|t = z0|t - A†(A*z0|t - y)`，将潜变量投影到满足约束的仿射子空间上。该投影是满足约束的最小扰动解，最大程度保留原始信息。
    3.  **轨迹内在观测构造**：为保证图像质量，观测值 `y` 不随机生成，而是从原始去噪轨迹的中间步骤 `T_proj` 处的潜变量计算得到 (`y = A * z0|T_proj`)。这确保了投影后的潜变量仍贴近原始生成轨迹，维持语义连贯性和水印完整性。
*   **算法流程**：
    *   **生成**：在标准的DDIM去噪循环中，于步骤 `T_proj` 计算观测 `y`，并在后续步骤 (`t < T_proj`) 对预测的干净潜变量 `z0|t` 进行一致性投影，再继续去噪。
    *   **验证**：对输入图像编码得到 `z0`，计算 `y = A * z0`。在逆DDIM循环中，同样对 `t < T_proj` 步骤的 `z0|t` 进行一致性投影，以恢复初始噪声 `zT` 并提取水印。缺乏正确 `A` 的攻击者无法完成此约束反转过程。

### 3. 实验设计
*   **数据集与场景**：
    *   **良性图像生成**：使用 `Stable-Diffusion-Prompt` 数据集。
    *   **恶意伪造场景**：使用 `Inappropriate Image Prompts` 数据集生成伪造图像。
    *   **生成质量评估**：使用 `COCO` 数据集的描述生成图像，并以其真实图像作为参考计算FID等指标。
*   **基准与对比方法**：与6种主流的基于潜空间的水印方法进行对比：`Tree-Rings`, `RingID`, `WIND`, `GaussShading`, `PRCMark`, `GaussMarker`。
*   **评估指标**：
    *   **水印有效性**：比特精度、真正例率。
    *   **图像质量**：CLIP分数、FID、LPIPS。
    *   **抗伪造性**：攻击成功率、伪造图像比特精度。
    *   **鲁棒性**：在JPEG压缩、裁剪、模糊等8种图像扰动下的性能。

### 4. 资源与算力
*   论文明确指出，所有实验在**单个NVIDIA A40 GPU**上进行。
*   论文**未明确说明**具体的训练时长或总计算消耗，但强调了CSGuard引入的额外计算开销**可忽略不计**（时间开销比低于1.007倍）。

### 5. 实验数量与充分性
*   **实验数量**：进行了大量实验，包括：
    *   主实验（与6个基线对比）。
    *   针对不同图像扰动的鲁棒性实验（8种扰动，多种强度）。
    *   消融实验（观测构造方式、压缩比、投影比例、采样步骤数）。
    *   扩展实验（不同扩散模型、不同攻击-目标模型组合、不同秘密矩阵、FPR分析）。
    *   对抗额外攻击（水印移除攻击、学习型伪造攻击）的实验。
*   **充分性与公平性**：实验设计较为全面和客观。遵循了已有的伪造攻击协议，对比了主流方法，并在多种场景下验证了方法的通用性和鲁棒性。消融实验有效验证了各设计组件的必要性。

### 6. 主要结论与发现
*   CSGuard是首个能有效抵抗伪造攻击的扩散模型水印方案。
*   **抗伪造性**：将攻击成功率从基线的100%显著降低至28.12%。
*   **双保真性**：在良性图像上保持100%的检测率和具有竞争力的CLIP分数，未损害生成质量和水印有效性。
*   **安全性**：即使攻击者知晓方法原理并尝试估计秘密矩阵，伪造成功率仍很低。
*   **鲁棒性**：对常见的图像后处理操作具有较强抵抗力。
*   **局限性**：当攻击者使用与目标模型完全相同的模型进行攻击时，成功率会升高，这是当前方法的主要弱点。

### 7. 优点
*   **创新性强**：首次将压缩感知理论引入防伪水印，提出了“绑定对称性”的新颖安全范式。
*   **理论基础扎实**：提供了完整的理论分析和证明，包括最小扰动投影、轨迹保真性等。
*   **设计巧妙**：通过“轨迹内在观测”和“最小扰动投影”的结合，在施加安全约束的同时，最大限度地保持了生成轨迹和图像质量。
*   **实用性强**：方法为即插即用式，计算开销极小（<1%），易于集成到现有流程中。
*   **实验全面**：评估维度多样，对比充分，结果具有说服力。

### 8. 不足与局限
*   **对模型相同攻击的脆弱性**：当攻击者使用与生成模型架构和参数完全相同的模型时，CSGuard的防护效果会大幅下降。论文承认这是一个根本性权衡，并提出了基于距离的检测作为缓解措施，但非根本解决方案。
*   **极端后处理下的性能**：虽然对常见扰动鲁棒，但在非常剧烈的图像处理（如高强度裁剪、噪声）下，水印性能仍有下降空间。
*   **秘密矩阵管理**：引入了秘密矩阵 `A` 作为核心密钥，带来了密钥生成、存储、分发和管理的额外负担。其安全性依赖于 `A` 的保密性。
*   **比特精度**：在良性图像上的比特精度（96.15%）略低于某些基线（100%），虽不影响检测，但仍有提升空间。
*   **应用范围**：目前主要针对全局所有权验证和伪造预防，在语义级篡改定位（如人脸替换）方面的能力未作探讨。

（完）
