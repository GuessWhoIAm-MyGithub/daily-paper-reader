---
title: Secure Seed-Based Multi-bit Watermarking for Diffusion Models from First Principles
title_zh: 基于第一性原理的扩散模型安全种子多比特水印技术
authors: "Enoal Gesny, Eva Giboulot"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.06153v1"
tags: ["query:imgfor"]
score: 8.0
evidence: 扩散模型安全的理论水印分析
tldr: 生成图像模型的水印技术如种子嵌入快速发展，但当前评估主要依赖经验，缺乏理论严谨性。本文通过解耦模型依赖，提出一个基于安全性、鲁棒性和保真度的形式化评估框架，并在此基础上引入SSB方法，概括先前方法，允许在理论上实现任意权衡，为设计有理论保证的水印系统提供新途径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.06153v1/fig-001.webp\", \"caption\": \"Figure 5: Comparison between empirical and theoretical eigenvalues of the covariance matrix for different sampling of the seed. The empirical eigenvalues are computed over No = 10L samples with L = 512 and M ′ = 256.\", \"page\": 15, \"index\": 1, \"width\": 822, \"height\": 242}, {\"url\": \"assets/figures/arxiv/2605.06153v1/fig-002.webp\", \"caption\": \"Figure 3: Watermarking characteristic of (∆, δ)-SSB , showing the capacity, fidelity and security as a function of the lattice parameters as well as of the codeword size relative to the latent size α =M ′/L\", \"page\": 8, \"index\": 2, \"width\": 822, \"height\": 190}, {\"url\": \"assets/figures/arxiv/2605.06153v1/fig-003.webp\", \"caption\": \"Figure 6: Perfect security regime with (1.6, 0)-SSB.\", \"page\": 19, \"index\": 3, \"width\": 540, \"height\": 406}, {\"url\": \"assets/figures/arxiv/2605.06153v1/fig-004.webp\", \"caption\": \"Figure 7: Example of images generated with Sana. Images were selected randomly.\", \"page\": 20, \"index\": 4, \"width\": 764, \"height\": 772}]"
motivation: 当前生成模型水印技术的评估主要依赖经验，缺乏理论严谨性和安全定义，需要基于理论分析的方法。
method: 通过解耦模型依赖部分，建立一个形式化评估框架，以理论分析水印系统的安全性、鲁棒性和保真度。
result: 基于框架提出SSB方法，实现在特征表面上任意安全性-鲁棒性-保真度的权衡，独立于生成模型。
conclusion: 该研究为设计具有理论保证的现代水印系统打开大门，无需昂贵的经验评估。
---

## 摘要
生成式图像模型的快速涌现催生了专门的水印技术，尤其是基于种子的嵌入等生成过程中方法。然而，该领域的当前评估仍然主要基于经验，这使得它们严重依赖于用于生成和反演的具体模型架构。这阻碍了对任何方法性能的明确结论，尤其是在缺乏严格定义的安全性方面。与这种方法相反，我们认为水印方案的有效性应纯粹通过彻底的理论分析来建立。这通过将模型依赖部分与水印系统的实际决策机制解耦来实现。利用这种解耦，我们引入了一个基于安全性、鲁棒性和保真度的正式评估框架。这通过一个表示这三个量之间权衡的特征表面，允许对水印系统进行精确比较，独立于任何生成模型。基于此框架，我们提出了SSB，一种新颖的水印方法，它通过允许在其特征表面上达到任何安全性-鲁棒性-保真度状态，推广了先前的基于种子的方法。这项工作为设计具有理论保证的现代水印系统打开了大门，这些系统无需任何昂贵的经验评估。

## Abstract
The rapid emergence of generative image models has led to the development of specialized watermarking techniques, particularly in-generation methods such as seed-based embedding. However, current evaluations in this area remain largely empirical, making them heavily reliant on the specific model architectures used for generation and inversion. This prevents any clear conclusion on the performance of any method, especially regarding security, for which a rigorous definition is lacking. Against this approach, we argue that the effectiveness of a watermarking scheme should be established purely through a thorough theoretical analysis. This is enabled by decoupling the model-dependent part from the actual decision mechanism of the watermarking system. Using this decoupling, we introduce a formal evaluation framework based on security, robustness, and fidelity. This allows precise comparisons between watermarking systems through a characteristic surface representing the trade-off between these three quantities, independent of any generative model. Based on this framework, we propose SSB, a novel watermarking method that generalizes previous seed-based methods by allowing to reach any security-robustness-fidelity regime on its characteristic surface. This work opens the door to the design of modern watermarking systems with theoretical guarantees that do not necessitate any costly empirical evaluations.

---

## 论文详细总结（自动生成）

### 论文核心问题与整体含义

1.  **研究动机与背景**：
    *   **核心问题**：当前针对扩散模型的基于种子的水印技术，其性能评估严重依赖于经验，且与特定的生成模型架构（如扩散模型、VAE、调度器）紧密绑定。这导致无法得出关于水印方案（尤其是安全性）的普适、可靠的结论。
    *   **理论缺口**：缺乏对水印系统“鲁棒性”、“保真度”和“安全性”的严格、独立的理论定义和评估框架。现有方法（如Gaussian-Shading）虽然声称具有高保真度、高鲁棒性和密码学安全性，但这些论断在理论基础上是脆弱的。
    *   **整体含义**：论文旨在为基于种子的多比特水印技术建立一个坚实的理论基础，通过解耦模型依赖部分，提出一个独立于生成模型的通用评估框架和设计方法，从而实现具有可证明安全保证的水印系统设计。

### 论文提出的方法论

1.  **核心思想**：
    *   **解耦与理论化**：主张将水印系统的性能评估与具体的生成模型解耦。将系统分解为两个独立部分：**投影函数**（负责将图像映射到水印空间，其鲁棒性可独立衡量）和**决策机制**（负责在水印空间中嵌入和检测消息）。
    *   **定义三大属性**：
        *   **容量/鲁棒性**：通过信道容量（Shannon Capacity）来量化，建模为二进制对称信道（BSC）。决策机制的“通道特性”函数 `p(σ)` 描述了在给定噪声方差 `σ²` 下的比特翻转概率。
        *   **保真度**：定义为水印分布与标准高斯分布之间的KL散度，衡量对原始潜在分布的偏离。
        *   **水印安全性**：区别于密码学安全性，定义为攻击者通过观察水印样本来估计秘密密钥所需的最小样本数（安全比η）。特别强调**码字载体必须保密**。

2.  **关键技术细节与流程**：
    *   **形式化框架**：提出了一个基于“特征表面”的框架，该表面描述了安全性、鲁棒性和保真度之间的权衡关系，独立于任何生成模型。
    *   **SSB (Secure Seed-Based) 方法**：这是本文提出的具体水印方案。
        *   **嵌入**：使用两个嵌套的一维格（粗格 `Λ_Δ` 和细格 `Λ_δ`）将二进制码字 `c` 映射到水印空间。通过算法1（`SAMPLE_WM`）采样一个中间向量 `z_u`，使得 `Λ_Δ(z_u) = c`。然后，使用秘密矩阵 `U` 将其投影回潜在空间，得到水印种子 `z`（公式3）。
        *   **解码**：通过逆扩散从图像获得近似种子 `̂z`，计算 `U^T ̂z`，然后应用粗格函数 `Λ_Δ` 来解码码字。
        *   **理论保证**：论文给出了SSB的通道特性（命题2）、渐进保真度（命题3）和针对PCA攻击的安全比（命题4）的理论公式，证明了SSB可以通过调整格参数 `Δ` 和 `δ` 在特征表面上实现任意权衡。

### 实验设计

1.  **数据集与场景**：
    *   **生成模型**：使用了三个流行的流匹配扩散模型：**Sana**、**Z-Image Turbo** 和 **Qwen**，它们具有不同的架构（压缩率、模型大小、步数）。
    *   **提示数据集**：`Gustavosta/Stable-Diffusion-Prompts`。
    *   **评估场景**：
        1.  **投影函数鲁棒性评估**：衡量常见图像处理操作（亮度、对比度、JPEG压缩、中心裁剪等）在潜在空间引入的等效高斯噪声方差 `σ²`。
        2.  **水印系统性能验证**：在Sana模型上，生成水印图像，验证理论模型（通道特性、保真度、安全性）与实际经验表现的一致性。

2.  **基准与对比方法**：
    *   **基准**：理论分析的基准是标准高斯分布和无水印扩散过程。
    *   **对比方法**：
        *   **Gaussian-Shading**：一种基于符号函数的多比特种子水印方法。
        *   **PRC (Pseudorandom Codes)**：一种使用伪随机纠错码的多比特种子水印方法。
        *   **SSB的两个变体**：`(∞, ∞)-SSB`（决策机制与Gaussian-Shading相同）和 `(1.6, 0)-SSB`（理论上完全安全的配置）。

### 资源与算力

*   **硬件**：实验在 **NVIDIA L40s GPUs** 上进行。
*   **时长**：所有实验总耗时约 **20小时**。
*   **模型配置**：文中明确列出了不同模型的参数，如Sana（25步，512图像尺寸，潜在空间维度28192）。但未明确说明使用的GPU数量。

### 实验数量与充分性

*   **实验组数**：
    1.  **鲁棒性评估**：针对3个模型 × 5种图像变换，共15组实验，计算等效噪声方差。
    2.  **水印系统验证**：针对4种水印方法（2种SSB变体 + Gaussian-Shading + PRC），进行多维度评估：
        *   生成500张水印图像。
        *   计算经验比特准确率、理论/经验码率、保真度和安全性。
    3.  **安全性分析**：提供了PCA攻击的理论分析和可视化（图5）。
    4.  **特征表面分析**：系统性地展示了SSB在不同参数下的容量、保真度和安全性（图3、图6）。
*   **充分性与公平性**：
    *   **充分性**：实验覆盖了理论模型的关键验证点（通道特性、保真度、安全性），并在多个模型上测试了投影函数的鲁棒性假设。特征表面的绘制展示了方法的灵活性。
    *   **公平性**：所有方法在相同模型（Sana）和相同数据集提示下生成图像进行对比。理论计算为所有方法提供了统一的比较基准。但需注意，SSB与Gaussian-Shading、PRC在错误纠正码选择上有所不同，论文通过统一使用香农容量或报告各自最佳码率来进行折中比较。

### 论文的主要结论与发现

1.  **框架有效性**：成功建立了一个独立于生成模型的水印系统理论评估框架，将性能评估归结为容量、保真度和安全性三者的权衡。
2.  **SSB方法优势**：SSB方法在理论框架下设计，能够灵活地在特征表面上选择工作点。论文发现了` (1.6, 0) -SSB` 这一**完全安全**的配置，即在该参数下，无论攻击者有多少样本，都无法从水印数据中估计出秘密矩阵 `U`。
3.  **对现有方法的批判**：指出Gaussian-Shading和PRC等现有方法在**水印安全性**方面存在根本缺陷：它们未隐藏码字载体，因此易受“欺骗攻击”。其声称的密码学安全性不等同于水印安全性。
4.  **实证验证**：经验实验在很大程度上验证了理论模型的预测，证明了“解耦”和理论分析的有效性。

### 优点

1.  **理论贡献突出**：首次为基于种子的多比特水印提出了严格、完整的理论评估框架，将研究从经验驱动推向理论驱动，具有开创性。
2.  **方法设计精巧**：SSB方案设计简洁而深刻，通过嵌套格结构实现了参数可调的权衡，并揭示了完全安全的存在。
3.  **安全定义严谨**：清晰区分了密码学安全性和水印安全性，并基于真实威胁模型（欺骗攻击）给出了实用的安全比定义。
4.  **实验设计严谨**：实验旨在验证理论，而非盲目优化；在多个主流模型上测试了基础假设（投影鲁棒性），增强了结论的泛化性。

### 不足与局限

1.  **攻击模型局限**：理论分析主要针对PCA攻击和加性高斯噪声信道。对于现实世界中更复杂的攻击（如几何攻击、模型微调攻击）的鲁棒性和安全性分析不足。
2.  **假设验证**：将投影函数的鲁棒性建模为加性高斯噪声是一个简化假设，虽在实验中有初步验证，但其普适性仍需更多场景下的检验。
3.  **保真度度量**：使用KL散度和渐进高斯分布进行保真度分析，在有限维度下或分布非高斯时可能存在近似误差。
4.  **应用范围**：论文聚焦于多比特、基于种子的水印，未直接讨论如何扩展到零比特水印场景。
5.  **实验覆盖**：虽在三个模型上测试，但验证深度水印系统性能的实验仅在Sana模型上进行，对其他模型的泛化性有待进一步确认。

（完）
