---
title: "LEGO: LoRA-Enabled Generator-Oriented Framework for Synthetic Image Detection"
title_zh: LEGO：基于LoRA的生成器导向合成图像检测框架
authors: "Yutong Xiao, Ran Ran, Jiwei Wei, Shuchang Zhou, Ke Liu, Zheng Ziqiang, Caiyan Qin"
date: 2026-05-06
pdf: "https://arxiv.org/pdf/2605.04445v1"
tags: ["query:imgfor"]
score: 9.0
evidence: 提出使用LoRA检测合成图像的框架。
tldr: 随着生成技术进步，合成图像难以检测，现有方法泛化能力差。本文提出LEGO框架，通过LoRA模块捕获特定生成器特征，并用MLP和注意力机制动态融合。该框架分两阶段训练，能适应新生成器，且在少量数据下性能优于现有方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.04445v1/fig-001.webp\", \"caption\": \"Figure 1: Comparison of detector efficiency and generalization. Left: data-efficiency–performance trade-off using ACC on Chameleon. Right: mean–standard-deviation comparison across AIGIBench subsets.\", \"page\": 3, \"index\": 1, \"width\": 970, \"height\": 421}, {\"url\": \"assets/figures/arxiv/2605.04445v1/fig-002.webp\", \"caption\": \"Figure 2: Overview of the proposed LEGO framework. LEGO is built upon a frozen CLIP backbone and trained in two stages. In Generator-Specific LoRA Specialization, each LoRA branch is individually optimized on a single-generator subset to capture distinctive artifact patterns. In Generator-Aware Router Alignment, the specialized branches are frozen and integrated into a LoRA Hub, while a router learns to predict the LoRA Merge Weight under mixed-generator training. The resulting Integrated LoRA is injected into the backbone, and the representation is refined by the Forgery Detection Head for prediction.\", \"page\": 3, \"index\": 2, \"width\": 891, \"height\": 532}, {\"url\": \"assets/figures/arxiv/2605.04445v1/fig-003.webp\", \"caption\": \"Table 2: Comparison with existing methods on the Chameleon benchmark. Results for HiDA-Net are reported as provided in the original paper, where only ACC is available. The best and second-best results in each column are shown in bold and underlined, respectively.\", \"page\": 7, \"index\": 3, \"width\": 514, \"height\": 502}, {\"url\": \"assets/figures/arxiv/2605.04445v1/fig-004.webp\", \"caption\": \"Table 1: Per-dataset performance comparison on AIGIBench (Part I, II, and III). The best and second-best results in each column are shown in bold and underlined, respectively.\", \"page\": 6, \"index\": 4, \"width\": 1048, \"height\": 1074}, {\"url\": \"assets/figures/arxiv/2605.04445v1/fig-005.webp\", \"caption\": \"Table 3: Ablation on core design choices.\", \"page\": 8, \"index\": 5, \"width\": 876, \"height\": 185}]"
motivation: 现有合成图像检测方法依赖通用特征，随着生成器多样性增加，泛化能力下降，需要新方法解决。
method: 提出LEGO框架，使用MLP调制多个LoRA块，每个LoRA块预训练捕获特定生成器特征，并通过注意力机制融合，分两阶段训练。
result: "LEGO在少于30,000张训练图像和5个epochs下，性能优于现有SOTA方法。"
conclusion: LEGO的模块化设计使其可扩展以适应新生成器，同时保持高效和高性能。
---

## 摘要
生成技术的快速发展使得合成图像几乎无法与真实图像区分，从而迫切需要鲁棒的检测器来对抗虚假信息。然而，现有方法主要依赖于跨多个生成器共享的通用伪影特征。我们观察到，随着生成器多样性的增加，这些共同特征的重叠逐渐减少。这严重损害了模型的泛化能力。相比之下，只关注独特伪影往往会导致对特定伪造模式的过拟合。为了解决这一挑战，我们提出了LEGO（面向生成器的LoRA启用框架）。LEGO的核心机制采用多层感知机（MLP）来调制多个LoRA（低秩适应）块，每个块预训练以捕获特定生成器的独特伪影，然后进行基于注意力的特征融合。与寻求单一通用解决方案的传统方法不同，LEGO通过将其训练过程分为两个阶段，将独特伪影提取委托给专门的LoRA模块。每个LoRA模块在单生成器数据集上单独训练以学习生成器特定的表示，然后MLP和注意力层在混合数据集上训练以动态调节每个模块的贡献。得益于其模块化且鲁棒的设计，LEGO可以通过纳入新的LoRA模块来适应新出现的下一代数据集，同时仍然以少于30,000张训练图像、不到其训练数据的10%以及每个训练阶段仅5个epoch的条件下，实现比先前最先进方法显著更好的性能。

## Abstract
The rapid advancement of generative technologies has made synthetic images nearly indistinguishable from real ones, thereby creating an urgent need for robust detectors to counter misinformation. However, existing methods mainly rely on universal artifact features that are shared across multiple generators. We observe that as the diversity of generators increases, the overlap of these common features gradually decreases. This severely undermines model generalization. In contrast, focusing only on unique artifacts tends to cause overfitting to specific forgery patterns. To address this challenge, we propose LEGO (LoRA-Enabled Generator-Oriented Framework). The core mechanism of LEGO employs an MLP to modulate multiple LoRA (Low-Rank Adaptation) blocks, each pretrained to capture the unique artifacts of a specific generator, followed by attention-based feature fusion. Unlike conventional methods that seek a single universal solution, LEGO delegates unique artifact extraction to specialized LoRA modules by dividing its training procedure into two stages. Each LoRA module is individually trained on a single-generator dataset to learn generator-specific representations, then MLP and attention layers are trained on mixed datasets to dynamically regulate the contribution of each module. Benefiting from its modular yet robust design, LEGO can be naturally extended by incorporating new LoRA modules for adaptation to newly emerging next-generation datasets, while still achieving substantially better performance than prior SOTA methods with fewer than 30,000 training images, less than 10% of their training data, and only 5 epochs in each training stage.

---

## 论文详细总结（自动生成）

# LEGO：基于LoRA的生成器导向合成图像检测框架 论文总结

## 1. 核心问题与整体含义
*   **研究动机与背景**：随着生成式AI（如GAN、扩散模型）的快速发展，合成图像已高度逼真，对数字信息真实性构成严峻挑战。现有检测方法大多依赖于**跨不同生成器共享的通用伪影特征**。然而，随着生成器种类日益多样化，这些通用特征的重叠区域在减少，导致模型的**泛化能力严重下降**。另一方面，若只关注特定生成器的独特伪影，又容易导致**对特定伪造模式的过拟合**。
*   **核心问题**：如何设计一个既能有效学习特定生成器的独特伪造痕迹，又能灵活组合这些知识以应对多样化、未见过的生成器，从而实现鲁棒泛化的检测框架？

## 2. 方法论
*   **核心思想**：提出 **LEGO（LoRA-Enabled Generator-Oriented）框架**，其核心是**将合成图像的伪造特征空间分解为多个生成器导向的低秩子空间**，并通过一个可学习的路由器进行动态组合，而非寻求一个单一的通用特征表示。
*   **关键技术细节**：
    1.  **骨干网络**：使用冻结的预训练**CLIP ViT-L/14**视觉编码器作为特征提取骨干。
    2.  **生成器特定LoRA模块**：为每个已知的生成器家族（如Stable Diffusion, ProGAN）引入一个独立的**低秩适应（LoRA）模块**。每个LoRA模块被注入到CLIP骨干的自注意力层中，专门学习该生成器产生的独特伪造痕迹。
    3.  **两阶段训练流程**：
        *   **第一阶段：生成器特定LoRA专业化**：每个LoRA模块**单独**在对应生成器的数据子集上训练，学习其特定的伪造表示。此阶段仅优化LoRA参数。
        *   **第二阶段：生成器感知路由器对齐**：冻结所有已训练的LoRA模块，将它们集成到一个“LoRA Hub”中。训练一个**共享的MLP路由器**，它根据输入图像的特征，预测一个权重向量，用于动态融合（加权求和）所有LoRA模块的输出，形成一个**集成的、输入自适应的LoRA**。此阶段同时优化路由器和最终的分类头。
    4.  **关键公式**：
        *   **LoRA注入**：`H(X) = XW0 + (α/r) * X * Σ(πk * Ak * Bk)`，其中`πk`是路由器预测的权重，`Ak, Bk`是第k个LoRA的低秩矩阵。
        *   **路由器权重**：`π(x̄) = Softmax(MLP(x̄))`，其中`x̄`是输入特征的池化描述符。
        *   **训练损失**：第二阶段损失包含分类损失`Lcls`、路由监督损失`Lroute`（使用软标签，包括通过fake-fake融合生成的混合样本）和L1正则化损失`Lreg`，以鼓励稀疏组合。
*   **可扩展性**：当出现新的生成器时，只需训练一个新的LoRA模块并加入Hub，然后微调路由器即可，无需重新训练整个模型。

## 3. 实验设计
*   **训练数据**：为验证方法的高效性，**刻意使用了极少量的训练数据**。仅使用了**GenImage**数据集中三个生成器家族（Stable Diffusion v1.4, ProGAN, ADM）的子集，总计**少于30,000张图像**。
*   **测试数据集/Benchmark**：
    *   **AIGIBench**：一个包含多种生成器（包括GAN、扩散模型、人脸编辑、个性化生成等）的大型基准，用于评估跨生成器的泛化能力。
    *   **Chameleon**：一个更具挑战性的“野外”基准，包含语义和视觉变化更广泛的、更难检测的合成图像。
*   **对比方法**：与多种当前最先进的（SOTA）检测器进行了比较，包括：AIDE, Effort, TriDetect, HiDA-Net, CO-SPY, DFFreq, NPR, CLIPDetection, FreqNet, LaDeDa等。

## 4. 资源与算力
*   **硬件**：在单张**NVIDIA RTX A6000 GPU**上进行训练。
*   **训练时长**：论文明确指出每个训练阶段仅需**5个epoch**，但未提供完整的训练总时长（如小时数）。这强调了其训练效率高。

## 5. 实验数量与充分性
*   **实验数量**：进行了大量实验，包括：
    *   **主实验**：在AIGIBench（包含数十个子集）和Chameleon两个大型基准上与十余种SOTA方法进行全面的性能对比（报告ACC, AP, AUC, EER等多个指标）。
    *   **消融实验**：系统性地验证了框架中各个核心组件（多LoRA分支、路由器、路由损失、两阶段训练）的贡献。
    *   **策略分析**：分析了数据增强和fake-fake融合策略的效果。
    *   **参数分析**：研究了LoRA分支数量K对性能的影响。
*   **充分性与公平性**：实验设计较为充分和公平。
    *   **公平性**：在对比实验中，LEGO使用了远少于其他方法的训练数据（<30k vs. 数十万甚至更多），这凸显了其数据效率优势，但也意味着对比并非在完全相同的训练数据规模下进行。论文对此进行了明确说明。
    *   **客观性**：在多个公认的、具有挑战性的基准上进行测试，结果具有说服力。消融实验设计合理，能清晰归因性能提升的来源。

## 6. 主要结论与发现
*   **核心结论**：在复杂多样的场景下，**显式地建模生成器导向的独特伪影，比寻求单一的通用伪影特征更有效**。
*   **主要发现**：
    1.  LEGO框架在**极少训练数据**（<30k张）和**极少训练轮次**（每阶段5 epochs）的条件下，在AIGIBench和Chameleon基准上取得了**优于或媲美现有SOTA方法**的性能。
    2.  LEGO展现出**卓越的泛化能力**，尤其在处理深度伪造、人脸编辑等具有挑战性的生成器类型时优势明显。
    3.  框架的**模块化设计**使其能够像搭积木一样，通过添加新的LoRA模块来高效适应未来出现的新生成器。

## 7. 优点
*   **高效的数据与训练效率**：在极低的训练成本下实现了高性能，更具实用性和可扩展性。
*   **模块化与可扩展性**：LoRA模块的“即插即用”特性，为应对快速迭代的生成技术提供了灵活、低成本的解决方案。
*   **平衡的艺术**：通过两阶段设计，巧妙平衡了“学习特定生成器特征”与“组合特征以泛化”这两个目标，避免了过拟合和欠拟合。
*   **设计合理且可解释**：路由器权重可解释为对不同生成器特征的置信度，框架设计有清晰的数学和几何意义。

## 8. 不足与局限
*   **训练数据规模**：尽管论文强调了数据效率，但实验仅基于三个生成器家族的子集。其在更庞大、更多样化的生成器集合上的表现和扩展成本有待进一步验证。
*   **路由器的复杂性**：引入路由器和两阶段训练增加了系统的复杂性，可能带来额外的调参负担。
*   **对“生成器家族”的依赖**：方法需要预先知道训练样本属于哪个生成器家族，这在某些开放场景下可能难以获取。
*   **应用范围**：主要针对图像级的真伪二分类，未涉及视频检测、伪造区域定位等更细粒度的任务。

（完）
