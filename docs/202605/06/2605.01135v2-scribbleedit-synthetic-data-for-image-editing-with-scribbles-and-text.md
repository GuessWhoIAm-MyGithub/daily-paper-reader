---
title: "ScribbleEdit: Synthetic Data for Image Editing with Scribbles and Text"
title_zh: ScribbleEdit：用于涂鸦和文本图像编辑的合成数据
authors: "Anya Ji, George Ma, Téa Wright, Yiming Zhang, David M. Chan, Alane Suhr, Somayeh Sojoudi"
date: 2026-05-01
pdf: "https://arxiv.org/pdf/2605.01135v2"
tags: ["query:imgfor"]
score: 9.0
evidence: ScribbleEdit利用涂鸦和文本实现精确图像编辑。
tldr: 在图像编辑领域，用户难以同时精确控制空间布局和语义细节。本文引入ScribbleEdit，一个大规模合成数据集，通过自动生成源-目标图像对并配对人类涂鸦和VLM文本指令，支持涂鸦和文本的联合输入。使用该数据集微调扩散和自回归模型后，显著提高了编辑的空间对齐和语义一致性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.01135v2/fig-001.webp\", \"caption\": \"Figure 1. Overview of the ScribbleEdit pipeline for dataset construction and model finetuning.\", \"page\": 1, \"index\": 1, \"width\": 487, \"height\": 354}, {\"url\": \"assets/figures/arxiv/2605.01135v2/fig-002.webp\", \"caption\": \"Table 1. Quantitative results on our dataset. Inputs denote scribble (S), text instruction (T), and mask (M). Bold indicates the best result in each column, and underline indicates the second best.\", \"page\": 3, \"index\": 2, \"width\": 1025, \"height\": 298}, {\"url\": \"assets/figures/arxiv/2605.01135v2/fig-003.webp\", \"caption\": \"Figure 2. Comparison of editing results across methods. Each row shows one dataset example (Edit Instruction, Original, Scribble, Target) followed by results from four models.\", \"page\": 4, \"index\": 3, \"width\": 1036, \"height\": 494}]"
motivation: 现有图像编辑模型缺乏专门训练数据来联合解释抽象涂鸦和文本输入，导致精确控制困难。
method: 通过合成管道自动生成源-目标图像对，并配对人类绘制的涂鸦和VLM生成的文本指令，构建ScribbleEdit数据集。
result: 在ScribbleEdit数据集上微调后，模型生成编辑的空间对齐和语义一致性显著改善。
conclusion: ScribbleEdit数据集有效解决了涂鸦和文本联合输入的训练数据缺乏问题，提升了图像编辑模型的控制精度。
---

## 摘要
生成模型的最新进展显著提升了图像编辑能力，但精确且直观的用户控制仍然困难。具体而言，用户往往难以同时传达精确的空间布局和特定的语义细节。虽然自然语言指令能有效传达纹理和颜色等高级语义，但它们缺乏空间特异性。相反，自由手绘涂鸦提供粗略的空间边界，但无法表达详细的视觉属性。因此，实现精确控制需要结合这两种模态。然而，由于缺乏专门的训练数据，现有模型难以同时解释抽象涂鸦和文本。在这项工作中，我们引入ScribbleEdit，一个大规模合成数据集，旨在通过结合自然语言指令和自由手绘涂鸦输入来弥合这一差距，以实现更准确、可控的编辑。我们通过一个合成管道构建此数据集，该管道通过修复自动生成源-目标图像对，然后与人工绘制的涂鸦和视觉语言模型生成的文本指令配对。使用ScribbleEdit，我们评估和微调了基于扩散和自回归的统一多模态图像编辑模型。我们的实验表明，虽然现成模型难以处理抽象涂鸦输入，但在我们的合成数据集上进行微调显著提高了它们生成空间对齐和语义一致编辑的能力。

## Abstract
Recent progress in generative models has significantly advanced image editing capabilities, yet precise and intuitive user control remains difficult. Specifically, users often struggle to communicate both exact spatial layouts and specific semantic details simultaneously. While natural language instructions effectively convey high-level semantics like texture and color, they lack spatial specificity. Conversely, freehand scribbles provide rough spatial boundaries but cannot express detailed visual attributes. Consequently, achieving precise control requires combining both modalities. However, existing models struggle to jointly interpret abstract scribbles alongside text due to a lack of specialized training data.   In this work, we introduce ScribbleEdit, a large-scale synthetic dataset designed to bridge this gap by combining natural language instructions with freehand scribble inputs for more accurate, controllable edits. We construct this dataset through a synthetic pipeline that automatically generates source-target image pairs via inpainting, which are then paired with human-drawn scribbles and VLM-generated text instructions. Using ScribbleEdit, we evaluate and finetune both diffusion-based and autoregressive unified multimodal image editing models. Our experiments reveal that while off-the-shelf models struggle with abstract scribble inputs, finetuning on our synthetic dataset significantly improves their ability to generate spatially aligned and semantically consistent edits.

---

## 论文详细总结（自动生成）

# 论文总结：ScribbleEdit: Synthetic Data for Image Editing with Scribbles and Text

## 1. 核心问题与整体含义
*   **研究动机与背景**：当前图像编辑模型在实现精确、直观的用户控制方面存在困难。自然语言指令擅长描述高级语义（如颜色、纹理），但缺乏空间特异性；自由手绘涂鸦能提供粗略的空间边界，但无法表达详细的视觉属性。因此，结合两种模态是实现精确控制的关键。然而，现有模型缺乏专门的训练数据来联合解释抽象的涂鸦和文本输入，导致其性能不佳。
*   **整体含义**：本文旨在解决多模态图像编辑中的数据瓶颈问题，通过构建一个大规模合成数据集，使模型能够学习从涂鸦和文本的联合输入中生成精确的编辑结果。

## 2. 方法论
*   **核心思想**：构建一个名为 **ScribbleEdit** 的合成数据集，其核心是通过一个自动化管道，为“对象添加”任务生成配对的训练数据。每个样本包含：一个移除了目标对象的输入图像、作为目标的原始图像、对应的人类手绘涂鸦、以及描述编辑操作的文本指令。
*   **关键技术细节与流程**：
    1.  **数据源**：从 **Sketchy Database** 中采样图像-涂鸦对，该数据库包含由众包工人绘制的涂鸦。
    2.  **对象分割与移除**：使用 **Grounding DINO** 进行对象检测生成边界框，再用 **SAM** 生成精确的对象掩码。随后，使用 **LaMa** 修复模型根据掩码移除对象，生成背景合理的输入图像。
    3.  **指令生成**：使用视觉语言模型 **Gemma 3**，根据输入图像和目标图像，自动生成描述对象外观和位置的文本编辑指令。
    4.  **数据集划分**：最终数据集包含11,775个样本，按照片划分为训练集、验证集和测试集。为减少训练成本，每个照片最多配对两个涂鸦。
*   **模型微调**：
    *   **ControlNet-Scribble 改进**：提出一种图像条件化的ControlNet-Scribble，将原始图像的潜在表示与噪声目标潜在表示拼接，使模型在编辑时能保留背景。
    *   **BAGEL-7B-MoT 微调**：使用LoRA对统一多模态模型BAGEL-7B-MoT进行微调。训练中采用**空间加权MSE损失**，对编辑区域内的token赋予更高权重（5倍），以鼓励模型专注于编辑区域而非背景。

## 3. 实验设计
*   **数据集与场景**：实验完全基于本文构建的 **ScribbleEdit** 数据集，任务为**对象添加**。
*   **基准与评估指标**：
    *   **定量指标**：直方图交集（Histogram Intersection）、均方根误差（RMSE）、CLIP相似度、FID分数。其中直方图交集和RMSE分别在整张图像和编辑对象的边界框（BBOX）内计算。
    *   **定性评估**：通过可视化对比不同模型的编辑结果。
*   **对比方法**：
    *   **基线模型**：`SD Inpaint + ControlNet-Scribble`（标准扩散修复管线，使用文本、涂鸦和由涂鸦导出的掩码）。
    *   **零样本模型**：`BAGEL-7B-MoT`（未在ScribbleEdit上训练）。
    *   **微调模型**：在ScribbleEdit上微调后的 `Image-Cond. ControlNet-Scribble` 和 `BAGEL-7B-MoT`。微调模型分别测试了仅文本（T）、仅涂鸦（S）和涂鸦+文本（S+T）三种输入条件。

## 4. 资源与算力
*   论文中**未明确说明**训练所使用的具体GPU型号、数量以及训练时长等算力细节。

## 5. 实验数量与充分性
*   **实验数量**：论文进行了系统的定量评估（在测试集上计算4项指标）和定性比较（图2）。此外，还对表现最佳的微调BAGEL模型进行了小规模的错误分析（50张测试图像）。
*   **充分性与公平性**：
    *   实验设计相对公平，在相同的数据集和评估指标下对比了基线、零样本和微调模型。
    *   实验覆盖了两种主流架构（基于扩散的ControlNet和统一多模态模型BAGEL），并测试了不同模态输入（T, S, S+T）的影响。
    *   **不足**：缺乏在更广泛、不同编辑类型数据集上的泛化性实验；消融研究（如数据集规模、损失函数权重的影响）不够深入。

## 6. 主要结论与发现
1.  **预训练模型的局限性**：现成的（off-the-shelf）模型难以直接处理抽象的涂鸦输入进行编辑。
2.  **微调的有效性**：在ScribbleEdit数据集上微调后，模型在**空间对齐**（直方图交集、RMSE）和**语义一致性**（CLIP、FID）方面均获得显著提升。
3.  **最佳模型**：微调后的 **BAGEL-7B-MoT** 在多数指标上表现最佳，尤其是在结合涂鸦和文本输入时。
4.  **错误分析**：微调后的BAGEL模型在遵循涂鸦指引（82%）方面优于遵循文本指令（57%），主要失败模式包括对象方向/位置偏差和颜色/类别错误。

## 7. 优点
*   **问题定义明确**：精准地指出了多模态图像编辑中数据缺失的瓶颈。
*   **数据集构建创新**：提出了一个全自动、可扩展的合成数据生成管道，有效利用了现有资源（Sketchy Database, VLM）。
*   **方法实用**：提出的图像条件化ControlNet和空间加权损失，针对性地解决了背景保持和编辑聚焦的问题。
*   **评估系统**：对多种模型和输入条件进行了全面的定量与定性评估。

## 8. 不足与局限
*   **数据集局限性**：
    *   依赖现有的Sketchy Database涂鸦，限制了数据的可扩展性和多样性。
    *   目前仅专注于**对象添加**这一单一编辑任务，未涵盖删除、属性修改等更复杂的编辑类型。
    *   自动化生成的文本指令可能存在噪声或不准确。
*   **实验局限性**：
    *   缺乏在公开标准编辑基准上的评估，结果的外部有效性有待验证。
    *   消融实验不够充分，例如未深入研究数据集规模、损失函数权重等关键超参数的影响。
*   **应用限制**：模型对涂鸦的抽象性和文本指令的模糊性仍较敏感，实际应用中可能需要用户交互或后处理来优化结果。
*   **资源信息缺失**：未提供训练所需的算力详情，不利于复现和成本评估。

（完）
