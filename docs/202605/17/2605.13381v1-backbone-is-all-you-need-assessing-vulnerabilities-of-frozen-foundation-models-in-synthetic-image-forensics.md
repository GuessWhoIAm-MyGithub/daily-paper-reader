---
title: "Backbone is All You Need: Assessing Vulnerabilities of Frozen Foundation Models in Synthetic Image Forensics"
title_zh: 骨干网络就是你所需要的：评估冻结基础模型在合成图像取证中的脆弱性
authors: "Chiara Musso, Joy Battocchio, Andrea Montibeller, Giulia Boato"
date: 2026-05-13
pdf: "https://arxiv.org/pdf/2605.13381v1"
tags: ["query:imgfor"]
score: 9.0
evidence: 评估合成图像取证中的漏洞
tldr: 随着AI生成合成图像日益逼真，视觉Transformer（ViT）成为深度伪造检测的核心。然而，依赖冻结预训练backbone引入了关键漏洞。本文提出代理迭代对抗攻击（SIAA），一种灰盒攻击，仅利用检测器ViT backbone的知识，在特征空间中创建对抗样本。实验表明，该漏洞导致高攻击成功率，接近白盒性能，揭示backbone知识足以破坏检测器可靠性，呼吁更鲁棒防御。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.13381v1/fig-001.webp\", \"caption\": \"Figure 1: Overview of the Surrogate Iterative Adversarial Attack (SIAA). In Phase 1, a surrogate Feature-Processing (FP)head is trained to align frozen ViT features with CLIP text embeddings. In Phase 2, a Projected Gradient Descent (PGD) attack is used to optimize a perturbation 𝛿 𝑗 by iteratively shifting the initial adversarial image I𝑗 from the groundtruth cluster toward the target class. This process yields a final adversarial image I𝐽 that bypasses the unknown classification head by exploiting learned feature distributions.\", \"page\": 1, \"index\": 1, \"width\": 406, \"height\": 498}]"
motivation: AI生成图像的真实性增加，但依赖冻结backbone的检测器存在关键漏洞，需要评估其脆弱性以提升防御。
method: 提出SIAA灰盒攻击，仅利用ViT backbone知识，在目标检测器的特征空间中创建对抗性样本。
result: 实验表明，攻击成功率高，接近白盒性能，证明backbone知识足以破坏检测器可靠性。
conclusion: backbone知识能有效破坏深度伪造检测器，强调开发更鲁棒防御方法的迫切性。
---

## 摘要
随着AI生成的合成图像越来越逼真，视觉Transformer（ViTs）已成为现代深度伪造检测的基石。然而，普遍依赖冻结的预训练主干网络引入了一个微妙但关键的脆弱性。在这项工作中，我们提出了替代迭代对抗攻击（SIAA），一种灰盒攻击，它仅利用检测器的ViT主干网络的知识，并完全在目标检测器的特征空间内操作，以制作高效的对抗样本。通过我们的实验，涉及多个基于ViT的检测器和多样化的灰盒场景，包括小样本学习、完全训练不匹配和攻击迁移性测试，我们证明这种脆弱性一致地产生高攻击成功率，通常接近白盒性能。通过这样做，我们揭示仅主干网络知识就足以破坏检测器的可靠性，强调了在对抗多媒体取证中需要更鲁棒防御的紧迫性。

## Abstract
As AI-generated synthetic images become increasingly realistic, Vision Transformers (ViTs) have emerged as a cornerstone of modern deepfake detection. However, the prevailing reliance on frozen, pre-trained backbones introduces a subtle yet critical vulnerability. In this work, we present the Surrogate Iterative Adversarial Attack (SIAA), a gray-box attack that exploits knowledge of the detector's ViT backbone alone and operates entirely within the target detector's feature space to craft highly effective adversarial examples. Through our experiments, involving multiple ViT-based detectors and diverse gray-box scenarios, including few-shot learning, complete training misalignment and attack transferability tests, we demonstrate that this vulnerability consistently yields high attack success rates, often approaching white-box performance. By doing so, we reveal that backbone knowledge alone is sufficient to undermine detector reliability, highlighting the urgent need for more resilient defenses in adversarial multimedia forensics.

---

## 论文详细总结（自动生成）

### 论文详细总结

#### 1. 核心问题与整体含义
- **研究背景**：随着生成式AI（如扩散模型）的发展，合成图像日益逼真，对数字媒体可信度构成威胁。基于视觉Transformer（ViT）的深度伪造检测器因其出色的泛化能力成为主流。这类检测器通常采用“冻结的预训练ViT骨干网络 + 可训练分类头”的范式。
- **核心问题**：论文指出，这种依赖**冻结的、预训练ViT骨干网络**的范式引入了一个关键且未被充分研究的安全漏洞：仅知道检测器所使用的骨干网络（即灰盒知识），攻击者就可能有效破坏检测器。
- **研究动机与含义**：现有研究多关注白盒攻击或针对频率域等特定弱点的攻击，而专门针对ViT骨干网络可预测特征空间的灰盒攻击被忽视。本文旨在揭示这一漏洞的严重性，强调在对抗性多媒体取证领域开发更鲁棒防御策略的紧迫性。

#### 2. 提出的方法论：SIAA（代理迭代对抗攻击）
- **核心思想**：提出一种灰盒攻击SIAA。攻击者**仅知道目标检测器使用的ViT骨干网络及其权重**，而不知道其分类头的结构、参数以及训练数据。攻击通过在目标检测器的**特征空间**内操作来生成对抗样本。
- **关键技术细节**：
    1.  **代理模型构建**：攻击使用一个由**冻结的CLIP文本编码器**、**冻结的目标ViT骨干网络**和一个**可训练的特征处理头**（FP-Head）组成的代理模型。
    2.  **两阶段攻击流程**：
        - **阶段一（训练FP-Head）**：FP-Head包含两个分支（文本分支和视觉分支），通过一个定制的**对比损失函数**进行训练。该损失函数旨在将图像特征（由目标ViT提取）与相应的文本特征（“此图为真”或“此图为伪”的CLIP嵌入）在共享空间中对齐，从而学习目标分类头可能产生的决策边界。
        - **阶段二（生成对抗样本）**：对于输入图像，使用**投影梯度下降法**优化扰动。优化目标不是分类头的logits，而是在FP-Head学习的特征空间中，**最大化当前图像特征与真实文本特征的距离，同时最小化与伪造文本特征的距离**（公式J）。
- **公式/算法流程**：扰动优化基于公式 `J = ||v' - t'_y||^2 - ||v' - t'_{1-y}||^2`，其中`v'`是处理后的图像特征，`t'`是文本特征，`y`是真实标签。通过PGD迭代更新图像，使其特征表示从真实类簇移向伪造类簇。

#### 3. 实验设计
- **数据集**：
    - **训练攻击模型**：主要使用包含10k真实图像（MS-COCO）和10k伪造图像（潜在扩散模型）的数据集。
    - **评估攻击**：使用**Synthbuster**数据集，包含9个最先进生成模型（DALL-E 2/3, Adobe Firefly, Midjourney v5, Stable Diffusion系列等）生成的9000张图像和1000张真实图像。
- **基准场景**：
    1.  **标准灰盒**：攻击与检测器使用相同数据和增强策略。
    2.  **消融研究**：评估检测器分类头深度不同、攻击者仅有少量样本（100+100）、数据增强未知、训练数据集完全不同的场景。
    3.  **迁移性测试**：将在一个源骨干网络上优化的扰动，应用于使用不同目标骨干网络的检测器。
- **对比方法**：
    - **白盒PGD**：作为性能上界。
    - **PGD-ViT**：一个现有灰盒攻击基线，直接优化ViT输出特征上的扰动。
    - **PGD-L**：在PGD-ViT基础上增加一个线性层作为代理。
- **评估指标**：攻击成功率（ASR，针对初始正确分类的样本）和攻击后的曲线下面积（AUC）。

#### 4. 资源与算力
- **明确说明**：论文在“超参数与训练设置”部分明确指出了计算资源。
    - **硬件**：工作站配备**NVIDIA GeForce RTX 3090 GPU**、Intel Core i9-10940X CPU和256 GB RAM。
    - **训练细节**：FP-Head训练20个epochs，批量大小为64；PGD攻击迭代次数设为14。

#### 5. 实验数量与充分性
- **实验规模**：进行了三类主要实验，每组包含多个变体。
    1.  **主实验（表1）**：在三种不同骨干网络（CLIP, Swin, DINOv2）的检测器上，对比4种攻击方法（含白盒基线）。
    2.  **消融实验（表2）**：针对CLIP检测器，系统性地改变分类头深度、训练样本量、数据增强知识和训练集，共8组条件。
    3.  **迁移性实验（图3）**：在3x3的源-目标骨干网络组合中评估攻击的迁移能力。
- **充分性与公平性**：
    - **充分性**：实验覆盖了多种骨干网络架构、多样的攻击者知识假设（从理想到极端）以及关键的迁移性测试，较为全面。
    - **公平性**：所有对比方法（PGD-ViT， PGD-L）在相同设置下实现，并使用相同评估数据集，保证了可比性。消融实验设计严谨，逐步增加攻击者的不确定性，有效验证了方法的鲁棒性。

#### 6. 主要结论与发现
1.  **漏洞严重性**：仅知道ViT骨干网络（灰盒知识）就足以发动高效的对抗攻击，攻击成功率（ASR）常接近白盒攻击水平，表明“骨干网络知识就是你所需要的”。
2.  **方法有效性**：SIAA显著优于直接优化ViT特征的基线方法（PGD-ViT），证明FP-Head在对齐特征空间方面的关键作用。
3.  **骨干网络差异**：不同骨干网络的脆弱性不同。基于CLIP和Swin的检测器更易受攻击，而基于DINOv2的检测器因其自监督训练方式和更平滑的特征空间表现出更强的鲁棒性。
4.  **攻击鲁棒性**：即使在训练数据集和增强策略完全未知、仅有少量样本等极端条件下，SIAA仍能保持相对较高的ASR（≥0.7）。
5.  **迁移性**：攻击扰动在CLIP和Swin之间迁移性较好，但从DINOv2出发的扰动也能有效攻击其他模型。

#### 7. 优点
- **问题洞察深刻**：准确指出了当前ViT检测器范式中一个被低估的系统性安全风险。
- **方法设计巧妙**：SIAA通过引入CLIP文本对齐构建了一个有效的代理特征空间，解决了在未知分类头下进行特征空间攻击的核心难题。
- **实验设计全面**：从标准灰盒到多种限制性场景和迁移性测试，形成了完整、递进的证据链，说服力强。
- **实用性**：攻击对图像的修改不可感知（高SSIM和WPSNR），具有现实威胁性。

#### 8. 不足与局限
- **骨干网络覆盖范围**：实验主要针对三种主流ViT骨干，可能未涵盖其他新兴或小众架构。
- **防御对策探讨不足**：论文重点在揭示漏洞，对如何构建更鲁棒的防御（除了提到的未来方向）缺乏具体探讨。
- **FP-Head的依赖性**：攻击的有效性高度依赖于FP-Head能否准确学习到目标检测器的特征聚类，当特征空间不匹配（如实验中的`l4`条件）时性能下降。
- **对端到端微调模型的适用性**：论文明确将其范围限定在“冻结骨干”检测器，未评估对端到端微调的ViT检测器的效果，这是一个重要的应用场景。
- **迁移性瓶颈**：当以DINOv2为目标时，攻击成功率很低，说明该方法对具有特定鲁棒性设计的骨干网络效果有限。

（完）
