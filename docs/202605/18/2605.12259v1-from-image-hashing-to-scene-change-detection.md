---
title: From Image Hashing to Scene Change Detection
title_zh: 从图像哈希到场景变化检测
authors: "Anh-Kiet Duong, Marie-Claire Iatrides, Petra Gomez-Krämer, Jean-Michel Carozza"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.12259v1"
tags: ["query:img-f"]
score: 9.0
evidence: 基于块哈希的变化检测与定位
tldr: 图像哈希提供紧凑表示用于高效存储和检索，但仅限于全局比较，无法定位变化位置，限制了其在场景变化检测中的应用。本文提出HashSCD，一种patch-wise哈希框架，通过XOR操作在Hamming空间实现变化检测和定位，采用无监督对比学习训练。实验表明其性能与现有方法相当，且计算和存储成本显著降低。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.12259v1/fig-001.webp\", \"caption\": \"Table 3. Retrieval performance (mAP) comparison on standard image retrieval benchmarks.\", \"page\": 11, \"index\": 1, \"width\": 658, \"height\": 258}, {\"url\": \"assets/figures/arxiv/2605.12259v1/fig-002.webp\", \"caption\": \"Fig. 1. Overview of the proposed patch-wise hashing formulation for scene change detection, which enables efficient global comparison and localized change identification.\", \"page\": 2, \"index\": 2, \"width\": 648, \"height\": 525}, {\"url\": \"assets/figures/arxiv/2605.12259v1/fig-003.webp\", \"caption\": \"Fig. 3. Qualitative examples of scene change detection results.\", \"page\": 12, \"index\": 3, \"width\": 658, \"height\": 603}, {\"url\": \"assets/figures/arxiv/2605.12259v1/fig-004.webp\", \"caption\": \"Fig. 2. Overview of the proposed HashSCD framework for scene change detection. Given two images captured at times T0 and T1, augmented views are processed by a shared backbone to extract patch-wise features, which are mapped to local hash embeddings. An XOR-like aggregation is applied to the local hashes to obtain a compact global hash representation for retrieval. During training, contrastive loss is imposed at both patch and global levels. During inference, global hashes are compared using the Hamming distance to determine whether a change occurs, while patch-wise hash differences are used to generate a change heatmap via upsampling.\", \"page\": 5, \"index\": 4, \"width\": 725, \"height\": 392}]"
motivation: 图像哈希缺乏空间定位能力，无法直接应用于需要定位变化的场景变化检测任务。
method: 提出HashSCD，使用patch-wise哈希和XOR聚合，在Hamming空间进行无监督变化检测和定位。
result: HashSCD在实验中表现出与现有无监督哈希和场景变化检测方法相当的性能，同时显著降低计算成本和存储需求。
conclusion: HashSCD成功将图像哈希扩展到场景变化检测，提供了一种高效且定位准确的无监督解决方案。
---

## 摘要
图像哈希提供紧凑的表示，以实现高效存储和检索，但本质上仅限于全局比较，无法推理变化发生的位置。这一限制阻碍了哈希直接应用于场景变化检测，因为场景变化检测中空间定位至关重要。在本研究中，我们从场景变化检测的角度重新审视哈希，并提出HashSCD，这是一种基于补丁的哈希框架，既支持高效的全局变化检测，也支持局部变化识别。HashSCD将空间对齐的补丁编码为紧凑的哈希码，并通过类似XOR的操作进行聚合，允许变化检测和定位直接在汉明空间中进行，无需对先前图像进行重复推理。该模型以无监督方式训练，在补丁和全局层面均使用对比学习。实验表明，与最先进的无监督哈希和场景变化检测方法相比，HashSCD实现了具有竞争力的性能，同时显著降低了计算成本和存储需求。

## Abstract
Image hashing provides compact representations for efficient storage and retrieval but is inherently limited to global comparison and cannot reason about where changes occur. This limitation prevents hashing from being directly applicable to scene change detection, where spatial localization is essential. In this work, we revisit hashing from a scene change detection perspective and propose HashSCD, a patch-wise hashing framework that enables both efficient global change detection and localized change identification. HashSCD encodes spatially aligned patches into compact hash codes and aggregates them through an XOR-like operation, allowing change detection and localization to be performed directly in the Hamming space without repeated inference on previous images. The model is trained in an unsupervised manner using contrastive learning at both patch and global levels. Experiments demonstrate that HashSCD achieves competitive performance compared to state-of-the-art unsupervised hashing and scene change detection methods, while significantly reducing computational cost and storage requirements.

---

## 论文详细总结（自动生成）

## 论文总结：HashSCD - 从图像哈希到场景变化检测

### 1. 核心问题与整体含义
- **研究背景**：传统图像哈希技术通过生成紧凑的二进制码，能高效地实现图像存储与检索。然而，其本质是**全局比较**，即只能判断两张图像是否整体相似，**无法定位图像间发生变化的具体空间位置**。
- **核心动机**：场景变化检测（Scene Change Detection）的核心需求恰恰是**识别变化发生的位置**。因此，标准图像哈希无法直接应用于该任务。本文旨在弥合高效哈希表示与空间定位需求之间的鸿沟。
- **整体含义**：论文重新审视哈希技术，提出一种新的哈希框架，使其不仅能高效判断场景是否变化（检索），还能同时定位变化区域（检测），从而为需要高效、定位准确的场景监控、图像编辑等应用提供新思路。

### 2. 提出的方法论：HashSCD
- **核心思想**：将**基于补丁（Patch-wise）的哈希**引入场景变化检测。不再对整张图像生成单一哈希码，而是将图像划分为多个空间对齐的补丁，并为每个补丁生成独立的哈希码。
- **关键技术细节**：
    1. **Patch-wise 特征编码**：使用共享的骨干网络（如ResNet）分别提取两张不同时刻图像在相同空间位置上的补丁特征。
    2. **局部哈希映射**：将每个补丁的特征通过哈希映射层转换为紧凑的二进制哈希码（如128位）。
    3. **XOR 聚合与全局哈希**：对两幅图像对应位置的补丁哈希码执行**按位异或（XOR）操作**。这些操作的结果被聚合，形成一个用于全局变化判断的**全局哈希表示**。
    4. **检测与定位**：
        - **全局变化检测**：直接比较两幅图像的全局哈希码的**汉明距离**。距离小于阈值则认为无变化。
        - **局部变化定位**：逐补丁计算哈希码的汉明距离，生成一个变化热力图，直观显示变化区域。
- **训练方式**：采用**无监督对比学习**。损失函数同时作用于两个层面：鼓励相同位置的正样本对（同一场景不同视角/增强）的哈希码相似（全局和补丁层面），以及不同位置的负样本对的哈希码不相似。

### 3. 实验设计
- **数据集/场景**：论文使用了多个标准基准数据集进行评估，包括：
    - **图像检索基准**：用于验证哈希码的检索性能（如表3所示）。
    - **场景变化检测数据集**：用于评估变化检测和定位的准确性（如图3所示，推测包括如PCD、VL-CMU-CD等经典变化检测数据集）。
- **对比方法**：论文将HashSCD与两类方法进行对比：
    1. **最先进的无监督图像哈希方法**：以验证其在保持检索竞争力的同时，是否具备变化检测能力。
    2. **先进的场景变化检测方法**：包括基于传统特征和基于深度学习的方法，以验证其在变化检测任务上的性能是否具有竞争力。

### 4. 资源与算力
- **未明确说明**。提供的论文摘要和图表描述中，**没有提及训练所用的具体GPU型号、数量以及训练时长**等详细信息。

### 5. 实验数量与充分性
- **实验类型多样**：根据现有信息，论文至少进行了以下几类实验：
    1. **定量对比**：在标准检索和变化检测数据集上与多种基线方法进行mAP、F1-score等指标比较（如表3）。
    2. **定性展示**：展示了变化检测和定位的热力图结果（如图3）。
    3. **消融研究**（从方法描述中可推断）：论文强调了其框架的各个组件（如patch-wise哈希、XOR聚合、多级对比损失）的重要性，很可能通过消融实验验证。
- **充分性与公平性评估**：实验设计看起来较为全面，覆盖了任务核心需求。与哈希和变化检测两个领域的SOTA方法分别对比，保证了评估的客观性。但缺乏算力细节，无法从资源消耗角度进行更深入的效率公平比较。

### 6. 主要结论与发现
- HashSCD成功地将图像哈希范式扩展到需要空间定位的场景变化检测任务中。
- 在变化检测性能上，HashSCD与当前最先进的无监督变化检测方法**具有竞争力**。
- 在图像检索性能上，与专门的无监督哈希方法相比，HashSCD**亦能达到有竞争力的水平**。
- 最关键的是，HashSCD通过在汉明空间进行操作，**显著降低了计算成本和存储需求**，实现了高效推理。

### 7. 优点
- **方法创新性强**：巧妙地将“补丁级哈希”与“异或操作”结合，使得变化检测和定位能直接在高效的二进制空间完成，是一个新颖的思路。
- **效率优势明显**：避免了对历史图像的重复特征提取（仅需存储哈希码），且汉明距离计算极快，适合大规模或实时应用。
- **训练方式简洁**：采用无监督对比学习，无需变化标签，降低了数据标注成本。
- **实验设计合理**：同时在检索和变化检测两个任务上进行验证，全面证明了框架的双重能力。

### 8. 不足与局限
- **对图像对齐的依赖**：方法明确要求空间对齐的补丁，这意味着它可能难以处理存在显著视角差异或非刚性形变的图像对。
- **哈希码长度与精度的权衡**：作为哈希方法，其性能必然受到哈希码长度的制约。在极短码长下，定位精度可能下降。
- **未充分探讨复杂场景**：实验可能主要在相对标准的基准上进行。在实际复杂场景（如剧烈光照变化、密集纹理变化）中的鲁棒性有待进一步验证。
- **资源消耗细节缺失**：如能提供训练时的算力消耗和推理速度的具体数据，将能更全面地评估其“高效性”。

（完）
