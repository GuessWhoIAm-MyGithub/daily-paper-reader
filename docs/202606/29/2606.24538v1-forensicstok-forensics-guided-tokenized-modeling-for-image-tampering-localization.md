---
title: "ForensicsTok: Forensics-Guided Tokenized Modeling for Image Tampering Localization"
title_zh: ForensicsTok：取证引导的标记化建模用于图像篡改定位
authors: "Lei Xu, Haowei Wang, Shen Chen, Taiping Yao, Bin Li, Changsheng Chen"
date: 2026-06-23
pdf: "https://arxiv.org/pdf/2606.24538v1"
tags: ["query:img-f"]
score: 10.0
evidence: ForensicsTok使用自回归标记生成定位图像篡改
tldr: 多模态大语言模型在取证任务中强大，但现有图像篡改定位方法依赖外源分割解码器，导致定位不佳和信息瓶颈。本文提出ForensicsTok，将篡改定位重新表述为自回归序列生成任务，引入Token Splatting Decoder和Hierarchical Expert Fusion模块来精确预测掩码，显著提升定位性能并增强鲁棒性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.24538v1/fig-001.webp\", \"caption\": \"Figure 1: Comparison of FakeShield [1] and ForensicsTok. (a) FakeShield risks information loss via external decoders; ForensicsTok uses direct token generation for probabilistic masks. (b) We compared two methods under unified training protocols. F1 scores by tampering type, with ForensicsTok’s average 39% gain over FakeShield.\", \"page\": 2, \"index\": 1, \"width\": 972, \"height\": 529}, {\"url\": \"assets/figures/arxiv/2606.24538v1/fig-002.webp\", \"caption\": \"Figure 4: Comparison of Feature Fusion Architectures. Left (Late Fusion): Existing methods (e.g., Dimension or Sequence Concatenation) simply fuse forensic features after the visual backbone. Right (Ours): The proposed Hierarchical Expert Fusion (HEF) adopts an intermediate fusion strategy.\", \"page\": 7, \"index\": 2, \"width\": 850, \"height\": 543}, {\"url\": \"assets/figures/arxiv/2606.24538v1/fig-003.webp\", \"caption\": \"Table 2: Classification performance on SID-Set using the SIDA protocol. We report per-class Accuracy (ACC) and F1, and their macro averages (Overall); the macro-averaged ACC is equivalent to a class-balanced accuracy over the three classes.\", \"page\": 10, \"index\": 3, \"width\": 798, \"height\": 332}, {\"url\": \"assets/figures/arxiv/2606.24538v1/fig-004.webp\", \"caption\": \"Figure 2: Overview of the proposed ForensicsTok framework. The architecture integrates the Hierarchical Expert Fusion (HEF) module with the visual backbone, which efficiently incorporates expert forensic knowledge for tampering localization. ForensicsTok also employs the Token Splatting Decoder (TSD), a forensic-specific token supervision and detokenization path that maps tokens to binary masks.\", \"page\": 4, \"index\": 4, \"width\": 938, \"height\": 271}, {\"url\": \"assets/figures/arxiv/2606.24538v1/fig-005.webp\", \"caption\": \"Table 5: Ablation study on expert model selection. We report IoU scores. We compare standalone experts vs. their integration into ForensicsTok.\", \"page\": 12, \"index\": 5, \"width\": 1019, \"height\": 248}, {\"url\": \"assets/figures/arxiv/2606.24538v1/fig-006.webp\", \"caption\": \"Table 1: Comparison of tampering localization performance for ForensicsTok against traditional forensic methods and recent MLLM-based approaches, evaluated using IoU and F1 metrics across six datasets. Bold and underlined values indicate the best and second-best performance, respectively, among all competing methods.\", \"page\": 9, \"index\": 6, \"width\": 980, \"height\": 320}, {\"url\": \"assets/figures/arxiv/2606.24538v1/fig-007.webp\", \"caption\": \"Figure 3: Codebook usage and rigid supervision. (a) Code usage is balanced. (b) Distinct sequences can decode to similar masks. Treating such semantically-close codes as exclusive one-hot labels causes strict supervision and instability.\", \"page\": 5, \"index\": 7, \"width\": 953, \"height\": 345}]"
motivation: 现有方法在多模态大语言模型用于取证任务时，依赖拼接管道和外源解码器，导致空间信号稀释和语义先验限制，造成定位不佳。
method: 提出ForensicsTok框架，将图像篡改定位建模为自回归序列生成任务，并通过Token Splatting Decoder和Hierarchical Expert Fusion模块改进掩码预测。
result: 在六个基准测试中，ForensicsTok显著优于基于多模态大语言模型的基线方法，并略微超越强大的取证专家基线，同时展现出更强的扰动鲁棒性。
conclusion: ForensicsTok通过整合取证先验和序列生成方式，有效提升了图像篡改定位的精度和鲁棒性。
---

## 摘要
多模态大型语言模型（MLLMs）为取证任务提供了强大的推理能力，然而利用外部分割解码器的现有方法通常定位不佳。依赖拼接管道在反向传播过程中引入信息瓶颈，稀释了空间信号并受分割器语义先验的限制。为解决这些局限性，我们提出了ForensicsTok，它将图像操纵定位重新表述为自回归序列生成任务。ForensicsTok直接生成空间定位的标记序列，无需中介监督即可实现精确的掩码预测。具体来说，我们引入了标记溅射解码器（TSD），通过码本感知的代码平滑将标记映射到二进制掩码，缓解了确定性去标记器产生的尖锐梯度。此外，为捕捉多样的篡改线索，我们提出了分层专家融合（HEF）模块，从取证专家模型中注入多尺度特征。这种统一的架构有效地弥补了标准MLLMs中取证先验的不足。在六个基准上的广泛实验表明，ForensicsTok显著优于现有的基于MLLM的基线，并略微优于强大的取证专家基线，同时表现出对扰动的更强鲁棒性。

## Abstract
Multi-modal Large Language Models (MLLMs) offer powerful reasoning for forensic tasks, yet existing approaches utilizing exogenous segmentation decoders often suffer from suboptimal localization. The reliance on stitched pipelines introduces information bottlenecks during backpropagation, which dilutes spatial signals and is limited by semantic priors of the segmentor. To address these limitations, we propose ForensicsTok, which reformulates image manipulation localization as an autoregressive sequence generation task. ForensicsTok directly generates spatially grounded token sequences, enabling precise mask prediction without intermediary supervision. Specifically, we introduce a Token Splatting Decoder (TSD) to map tokens to binary masks via codebook-aware code smoothing, which mitigates sharp gradients from deterministic detokenizers. Furthermore, to capture diverse tampering clues, we propose a Hierarchical Expert Fusion (HEF) module that injects multi-scale features from a forensic expert model. This unified architecture effectively compensates for the lack of forensic priors in standard MLLMs. Extensive experiments on six benchmarks show that ForensicsTok substantially improves over existing MLLM-based baselines and slightly improves over strong forensic expert baselines, while exhibiting stronger robustness to perturbations.

---

## 论文详细总结（自动生成）

## 论文总结：ForensicsTok：取证引导的标记化建模用于图像篡改定位

### 1. 核心问题与整体含义
*   **研究背景与动机**：随着AI生成内容（AIGC）的快速发展，图像篡改（如DeepFakes）对信息安全和公共信任构成严峻威胁。现有的基于专家模型（如ManTra-Net、SparseViT）的图像篡改检测与定位（IMDL）方法在复杂或跨域场景中泛化能力有限。近期，多模态大型语言模型（MLLM）被引入该领域，通过与外部解码器（如SAM）拼接来生成定位掩码。
*   **核心问题**：论文指出，这种“拼接式”MLLM方法存在两大根本性缺陷：
    1.  **掩码标记表示模糊**：MLLM输出的语义嵌入用于驱动外部分割器，导致其监督信号是语义的而非精确的空间信息，容易产生定位歧义。
    2.  **取证知识融合低效**：简单的后处理或特征拼接无法有效融合MLLM的视觉编码器与取证专家模型的多尺度、异质特征，限制了定位精度。
*   **整体含义**：本文旨在解决上述问题，探索如何将MLLM的推理能力与取证专业知识更直接、有效地结合，实现端到端、空间精确的图像篡改定位。

### 2. 提出的方法论
论文提出了名为 **ForensicsTok** 的统一框架，其核心思想是将图像篡改定位任务重新定义为一个**自回归序列生成任务**，直接预测代表篡改区域的空间定位标记序列，并通过码本（Codebook）将其解码为二进制掩码。

*   **关键技术细节**：
    1.  **整体框架**：基于一个预训练的MLLM（本文使用InternVL2.5-8B）作为骨干。给定图像和文本提示，模型自回归地生成文本解释和代表篡改掩码的**离散标记序列**。
    2.  **Token Splatting Decoder (TSD)**：
        *   **作用**：将生成的离散标记序列解码为像素级的二进制掩码。
        *   **核心机制**：利用一个预训练的码本（来自HiMTok），通过最近邻匹配（Eq. 1）将标记映射到码本向量，再解码为掩码。
        *   **创新点**：提出 **码本感知标签平滑** 以稳定训练。标准的独热标签监督过于严格，因为视觉相似的局部掩码可能映射到码本中相邻但不同的索引。TSD的损失函数 `LTSD`（Eq. 6）会将部分监督信号从一个真实码本索引平滑到其语义邻居上，缓解梯度尖锐问题。
    3.  **Hierarchical Expert Fusion (HEF)**：
        *   **作用**：将一个冻结的取证专家模型（本文使用SparseViT）的多尺度特征注入到MLLM的视觉编码器中。
        *   **核心机制**：采用**中间融合**策略，而非在编码器后简单拼接。在视觉编码器的每个阶段，通过注意力机制（Eq. 8）计算专家浅层特征（纹理）和深层特征（语义）的门控权重，并与视觉特征进行加权融合（Eq. 9），输出增强后的特征继续前向传播。
        *   **目的**：弥补标准MLLM缺乏低层取证线索的不足，自适应地利用专家知识来应对不同类型的篡改。

### 3. 实验设计
*   **数据集与场景**：
    *   **训练集**：TamperCOCO, MIML, CASIA2, SID_Set。涵盖了常见的拼接、复制-移动等篡改，以及AIGC篡改。
    *   **测试集（Benchmark）**：CASIA1, NIST, Coverage, Columbia, Glide, IMD。这六个数据集均未在训练中使用，涵盖了多种篡改类型（拼接、复制-移动、移除、AIGC编辑），用于评估**跨域泛化能力**。
*   **评估指标**：像素级交并比（IoU）和F1分数。
*   **对比方法**：
    *   **传统取证专家模型**：CAT-Net, MVSS-Net, PSCC-Net, TruFor, SparseViT。
    *   **基于MLLM的方法**：FakeShield, SIDA。
    *   所有方法在统一的训练协议（相同训练数据、预处理）下重新训练或微调，确保公平对比。

### 4. 资源与算力
论文**未明确说明**所使用的具体GPU型号、数量和总训练时长。文中仅提及了模型参数量和推理时间（见Table 8），但未给出训练成本细节。

### 5. 实验数量与充分性
实验设计较为全面和充分：
*   **主实验**：在6个跨域测试集上与两类基线（专家模型、MLLM模型）进行了定量比较（IoU, F1）。
*   **消融实验**：对模型的关键组件（TSD, HEF, 码本解码器）进行了逐一有效性验证（Table 3）。
*   **专项对比实验**：
    *   对比了不同的特征融合策略（晚期拼接 vs. HEF的中间融合）（Table 4）。
    *   验证了不同取证专家模型的选择对性能的影响（Table 5）。
    *   分析了TSD中关键超参数K（邻居数量）的敏感性（Table 6）。
*   **鲁棒性实验**：测试了模型在多种常见图像扰动（JPEG压缩、高斯噪声、缩放）下的性能稳定性（Table 7）。
*   **评估公平性**：强调在统一协议下评估，并对CASIA1和CASIA2的关系进行了说明，注重跨源证据。实验充分覆盖了方法有效性的各个方面，对比客观。

### 6. 主要结论与发现
1.  **性能显著提升**：ForensicsTok在六个基准测试上，平均IoU和F1分数均优于之前的MLLM基线方法（如FakeShield, SIDA），提升巨大。
2.  **接近专家水平**：与强大的传统取证专家SparseViT相比，ForensicsTok取得了略有提升（平均IoU +0.04，F1 +0.02）的竞争性性能。论文谦逊地指出，这并非证明MLLM必然优于专家，而是展示了在MLLM框架内如何通过直接掩码标记生成来弥合与专家模型的差距。
3.  **鲁棒性更强**：在各类图像扰动下，ForensicsTok的性能下降比MLLM基线更小，表现出更好的稳定性。
4.  **架构有效性验证**：消融实验证实，TSD（通过码本解码和梯度平滑）和HEF（通过中间特征融合）对最终性能贡献显著。

### 7. 优点
1.  **问题洞察深刻**：精准地指出了现有MLLM用于IMDL时“拼接式”架构在监督信号和知识融合上的两个核心缺陷。
2.  **方法设计新颖**：
    *   将定位任务转化为自回归标记生成，实现了MLLM输出与空间掩码的**端到端、直接监督**，避免了信息瓶颈。
    *   **TSD的码本感知平滑**是一个针对码本离散解码的通用且有效的改进，缓解了优化不稳定性。
    *   **HEF的中间融合策略**比晚期融合更合理，能在特征层级上更好地对齐和融合异质信息。
3.  **实验严谨**：采用跨域评估协议，重新统一训练所有基线，确保了对比的公平性和结论的可靠性。消融实验系统全面。

### 8. 不足与局限
1.  **计算开销**：ForensicsTok作为一个大型MLLM框架（~8.5B参数），在参数量和推理延迟上远高于轻量级专家模型（如SparseViT ~50M参数），限制了其在实时或资源受限场景中的应用。
2.  **对专家模型的依赖**：HEF模块的性能受益于一个强大且特征对齐良好的取证专家（如SparseViT）。若替换为较弱的专家，性能提升有限甚至可能引入噪声（Table 5）。
3.  **性能增益的局限性**：相对于最强的专家基线，其提升幅度是“适度”的，尤其在专家本就擅长的场景（如复制-移动，Coverage数据集）提升不明显。
4.  **对强扰动的敏感**：在强JPEG压缩下，性能下降相对较大，表明模型仍依赖于易被压缩破坏的细微取证痕迹。
5.  **应用范围聚焦**：实验主要关注像素级定位，虽然提及未来可研究文本输出的解释性，但本文未深入探讨模型在生成可解释性推理文本方面的表现。

（完）
