---
title: "DR-Mamba: Automatic Inference-Time Domain Adaptation for Document Image Binarization via Sample-Conditioned Detail-Background Suppression"
title_zh: DR-Mamba：通过样本条件化细节-背景抑制实现文档图像二值化的自动推理时间域适应
authors: "Sheng-Wei Chan, Jen-Shiun Chiang"
date: 2026-06-21
pdf: "https://arxiv.org/pdf/2606.22625v1"
tags: ["query:imgfor"]
score: 6.0
evidence: 文档图像二值化与领域自适应
tldr: 退化文档图像二值化对域移位敏感，现有方法在未见退化域上不稳定。提出DR-Mamba，一个样本条件化的细节-背景抑制框架，通过输入相关门在推理时自动适应域，无需梯度更新。方法使用快速-慢速路由建模和减法门抑制背景干扰，并添加细节引导重建。实验在DIBCO基准上显示跨域鲁棒性提升，尤其在严重退化域上表现强。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.22625v1/fig-001.webp\", \"caption\": \"Table 1. Overall comparison with classical and learning-based document image binarization methods. Competing numbers are taken from prior work under differing train/test year splits and implementation details, so this table is a reference comparison rather than a strictly controlled re-implementation. Higher FM, p-FM, and PSNR are better; lower DRD is better. “Years” denotes the number of benchmark years in the reported average.\", \"page\": 10, \"index\": 1, \"width\": 458, \"height\": 311}, {\"url\": \"assets/figures/arxiv/2606.22625v1/fig-002.webp\", \"caption\": \"Fig. 1. Simplified overview of the proposed DR-Mamba framework. A ConvNeXt-Tiny backbone extracts hierarchical document features, which are restored by a multi-scale decoder. The proposed DR-Mamba block models the decoded representation through a fast detail route and a slow background route, followed by adaptive subtractive suppression to reduce degradation interference. Full-resolution edge cues are further introduced through detail-guided reconstruction to improve stroke continuity and boundary sharpness.\", \"page\": 3, \"index\": 2, \"width\": 716, \"height\": 181}, {\"url\": \"assets/figures/arxiv/2606.22625v1/fig-003.webp\", \"caption\": \"Fig. 2. Qualitative comparison on challenging held-out documents. DR-Mamba better suppresses background degradation while preserving thin strokes and stroke continuity.\", \"page\": 12, \"index\": 3, \"width\": 706, \"height\": 319}]"
motivation: 解决退化文档图像二值化中域移位导致的性能下降问题。
method: 提出DR-Mamba框架，使用样本条件化的快速-慢速路由建模和减法门，在推理时自动适应域。
result: 在DIBCO基准上评估，DR-Mamba提高了跨域鲁棒性，尤其在最严重的退化留出年份上。
conclusion: DR-Mamba通过自动推理时域适应和细节-背景抑制，有效提升了文档图像二值化在不同退化域下的泛化能力。
---

## 摘要
退化文档图像二值化对由纸张老化、渗透、污渍、阴影和不均匀光照引起的域偏移敏感，近年来基于学习的方法在前景-背景分离方面可能在未见过的退化域上变得不稳定。我们提出了DR-Mamba，一个样本条件化细节-背景抑制框架，用于文档图像二值化的自动推理时间域适应。不同于测试时适应方法需要梯度更新或辅助数据，DR-Mamba通过输入相关的门控在单个前向传递中适应每个输入文档，不需要目标域标签，无需微调，无需测试时参数更新。DR-Mamba没有使用Mamba风格的选择性扫描作为单一的通用特征路径，而是将其重新解释为快慢路由建模：快速细节路由捕获局部笔划结构，而慢速背景路由累积空间上持久的退化响应。这两个路由通过一个输入相关的减法门控集成，显式地抑制背景干扰，而不是通过加法或拼接融合特征。我们进一步添加了全分辨率细节引导重建和细笔划感知监督，以恢复下采样过程中丢失的细笔划。在DIBCO风格基准测试的留一协议下评估，其中每个留出年份被视为一个未见过的退化域，DR-Mamba表明，每文档、每位置的减法抑制提高了跨域鲁棒性，在退化最严重的留出折叠上表现出特别强的性能。

## Abstract
Degraded document image binarization is sensitive to domain shifts caused by paper aging, bleed-through, stains, shadows, and uneven illumination, and the foreground-background separation of recent learning-based methods can become unstable on unseen degradation domains. We propose DR-Mamba, a sample-conditioned detail-background suppression framework that performs automatic inference-time domain adaptation for document image binarization. Unlike test-time adaptation methods that require gradient updates or auxiliary data at inference, DR-Mamba adapts to each input document through input-dependent gates within a single forward pass, requiring no target-domain labels, no fine-tuning, and no test-time parameter updates. Instead of using Mamba-style selective scanning as a single generic feature path, DR-Mamba reinterprets it as fast-slow route modeling: a fast detail route captures local stroke structures, while a slow background route accumulates spatially persistent degradation responses. The two routes are integrated through an input-dependent subtractive gate that explicitly suppresses background interference rather than fusing features by addition or concatenation. We further add full-resolution detail-guided reconstruction and thin-stroke-aware supervision to recover fine strokes lost during downsampling. Evaluated under a leave-one-year-out protocol on DIBCO-style benchmarks, where each held-out year is treated as an unseen degradation domain, DR-Mamba shows that per-document, per-location subtractive suppression improves cross-domain robustness, with particularly strong performance on the most severely degraded held-out fold.